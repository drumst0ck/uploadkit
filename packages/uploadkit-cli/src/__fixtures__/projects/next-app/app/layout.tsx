// Fixture marker — intentionally empty. Presence of `app/layout.tsx`
// signals the Next.js App Router to the framework detector.
export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
