---
phase: 12.5-uploadkit-cli-for-existing-projects-inserted
plan: 05
type: execute
wave: 3
depends_on: [12.5-02, 12.5-03]
files_modified:
  - packages/uploadkit-cli/src/init/sveltekit.ts
  - packages/uploadkit-cli/src/init/remix.ts
  - packages/uploadkit-cli/src/init/vite-react.ts
  - packages/uploadkit-cli/src/init/templates/sveltekit-server.ts.tpl
  - packages/uploadkit-cli/src/init/templates/sveltekit-client.ts.tpl
  - packages/uploadkit-cli/src/init/templates/remix-route.tsx.tpl
  - packages/uploadkit-cli/src/init/templates/vite-provider.tsx.tpl
  - packages/uploadkit-cli/src/__tests__/init-sveltekit.test.ts
  - packages/uploadkit-cli/src/__tests__/init-remix.test.ts
  - packages/uploadkit-cli/src/__tests__/init-vite-react.test.ts
  - packages/uploadkit-cli/src/__fixtures__/projects/sveltekit/**
  - packages/uploadkit-cli/src/__fixtures__/projects/remix/**
  - packages/uploadkit-cli/src/__fixtures__/projects/vite-react/**
autonomous: true
requirements: [GROW-15]
must_haves:
  truths:
    - "SvelteKit init creates +server.ts route handler and src/lib/uploadkit.ts client"
    - "Remix init creates app/routes/api.uploadkit.$.tsx and wraps app/root.tsx"
    - "Vite+React init mounts provider in src/main.tsx and warns about server requirement"
    - "All three flows are idempotent via marker detection"
    - "All three flows back up every modified file before mutation"
  artifacts:
    - path: "packages/uploadkit-cli/src/init/sveltekit.ts"
      provides: "initSvelteKit(ctx, session)"
    - path: "packages/uploadkit-cli/src/init/remix.ts"
      provides: "initRemix(ctx, session)"
    - path: "packages/uploadkit-cli/src/init/vite-react.ts"
      provides: "initViteReact(ctx, session)"
  key_links:
    - from: "packages/uploadkit-cli/src/commands/init.ts"
      to: "packages/uploadkit-cli/src/init/sveltekit.ts"
      via: "framework switch"
      pattern: "case \"sveltekit\""
---

<objective>
Extend `init` to SvelteKit, Remix/RR7, and Vite+React using the shared
orchestrator from plan 04. Each framework gets its own module + template
pair, idempotency check, and fixture test. Scope is tight — same pattern
as `init-next` three times over.

Purpose: GROW-15 — cover the remaining supported frameworks.
Output: Three `init*` modules, three test files, three fixture
projects.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12.5-uploadkit-cli-for-existing-projects-inserted/12.5-CONTEXT.md
@.planning/phases/12.5-uploadkit-cli-for-existing-projects-inserted/12.5-03-SUMMARY.md
@.planning/phases/12.5-uploadkit-cli-for-existing-projects-inserted/12.5-04-SUMMARY.md

<interfaces>
Per D-05:

**SvelteKit**
- Route: `src/routes/api/uploadkit/[...uploadkit]/+server.ts`
- Client: `src/lib/uploadkit.ts` (typed client init, NOT a React provider)
- No JSX wrap; no layout mutation

**Remix / React Router 7**
- Route: `app/routes/api.uploadkit.$.tsx` with action + loader
- Wrap `app/root.tsx` children under `<UploadKitProvider>`

**Vite + React**
- No server route (warn user)
- Wrap `src/main.tsx`'s `<App />` with `<UploadKitProvider>`
- .env uses `VITE_UPLOADKIT_API_KEY` instead (Vite env convention)

All three share the plan 04 orchestrator — each exports
`(ctx, session) => Promise&lt;InitResult&gt;` and is dispatched from
`commands/init.ts`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SvelteKit + Remix init modules</name>
  <files>packages/uploadkit-cli/src/init/sveltekit.ts, packages/uploadkit-cli/src/init/remix.ts, packages/uploadkit-cli/src/init/templates/sveltekit-server.ts.tpl, packages/uploadkit-cli/src/init/templates/sveltekit-client.ts.tpl, packages/uploadkit-cli/src/init/templates/remix-route.tsx.tpl, packages/uploadkit-cli/src/__tests__/init-sveltekit.test.ts, packages/uploadkit-cli/src/__tests__/init-remix.test.ts, packages/uploadkit-cli/src/__fixtures__/projects/sveltekit/**, packages/uploadkit-cli/src/__fixtures__/projects/remix/**</files>
  <behavior>
    SvelteKit:
    - Deps installed: `@uploadkitdev/core@latest` (no React pkg)
    - Creates `src/routes/api/uploadkit/[...uploadkit]/+server.ts` (create action)
    - Creates `src/lib/uploadkit.ts` (create action)
    - `.env` gets `UPLOADKIT_API_KEY=uk_test_placeholder`
    - Idempotency: check for `src/lib/uploadkit.ts` existence + marker in +server.ts
    - No JSX wrap (Svelte has no React provider)

    Remix/RR7:
    - Deps: `@uploadkitdev/react@latest` + core
    - Creates `app/routes/api.uploadkit.$.tsx` (create)
    - Backs up and wraps `app/root.tsx` body children with provider
      (reuse wrapChildrenOf)
    - `.env` key: `UPLOADKIT_API_KEY`
    - Idempotency: marker check in root.tsx + route file exists

    Fixtures: minimal SvelteKit (svelte.config.js, src/routes/+layout.svelte,
    package.json with @sveltejs/kit); minimal Remix (app/root.tsx with
    Outlet, app/routes/_index.tsx, package.json with @remix-run/node).

    Tests: same pattern as init-next — fresh run creates expected files,
    second run is a no-op.
  </behavior>
  <action>
    Mirror plan 04 structure. Keep template files as plain `.tpl` reads.
    Svelte client stub exports a typed `uploadkit` instance users
    customize. Do NOT attempt to mutate `.svelte` files.
  </action>
  <verify>
    <automated>pnpm --filter uploadkit-cli test -- "init-sveltekit|init-remix"</automated>
  </verify>
  <done>Both flows pass fixture tests + idempotency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Vite+React init + dispatcher wiring</name>
  <files>packages/uploadkit-cli/src/init/vite-react.ts, packages/uploadkit-cli/src/init/templates/vite-provider.tsx.tpl, packages/uploadkit-cli/src/commands/init.ts, packages/uploadkit-cli/src/__tests__/init-vite-react.test.ts, packages/uploadkit-cli/src/__fixtures__/projects/vite-react/**</files>
  <behavior>
    Vite+React:
    - Deps: `@uploadkitdev/react@latest`
    - Prints warning: "Vite has no server — uploads require BYOS or a
      separate backend. See docs."
    - Backs up `src/main.tsx`, uses addImport + a JSX wrap helper to
      wrap the root render call: `&lt;UploadKitProvider&gt;&lt;App /&gt;&lt;/UploadKitProvider&gt;`
    - `.env` key: `VITE_UPLOADKIT_API_KEY=uk_test_placeholder`
    - Idempotency via marker check in main.tsx

    Also finalize `commands/init.ts` dispatch table so all four
    frameworks are wired. Remove the TODO from plan 04.

    Fixture: minimal Vite+React (vite.config.ts, src/main.tsx with
    ReactDOM.createRoot, src/App.tsx, package.json with react + vite).

    Test: fresh run wraps App in provider, writes env, creates backup;
    second run is a no-op.
  </behavior>
  <action>
    The JSX wrap for `src/main.tsx` is different from layout.tsx — we
    wrap the argument to `.render(...)`, not a body child. Extend
    `jsx-wrap.ts` with a `wrapRenderArgument` helper if needed, or handle
    inline with magicast. Keep the warning message consistent with docs.
  </action>
  <verify>
    <automated>pnpm --filter uploadkit-cli test -- init-vite-react</automated>
  </verify>
  <done>Vite+React init works; commands/init.ts routes to all 4 frameworks.</done>
</task>

</tasks>

<verification>
- `pnpm --filter uploadkit-cli test` green across all init-* files
- Fixtures under `__fixtures__/projects/` produce deterministic output
</verification>

<success_criteria>
All four supported frameworks have working `init`. Running
`node dist/index.js init --yes --skip-install` inside any fixture
produces the expected file set, a manifest, and a backup session.
</success_criteria>

<output>
Create `12.5-05-SUMMARY.md` with per-framework output file lists, env
var conventions used, and any SDK helper name deviations encountered.
</output>
