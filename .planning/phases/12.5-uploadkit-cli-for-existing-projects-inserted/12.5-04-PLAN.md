---
phase: 12.5-uploadkit-cli-for-existing-projects-inserted
plan: 04
type: execute
wave: 3
depends_on: [12.5-02, 12.5-03]
files_modified:
  - packages/uploadkit-cli/src/commands/init.ts
  - packages/uploadkit-cli/src/init/next-app.ts
  - packages/uploadkit-cli/src/init/install-deps.ts
  - packages/uploadkit-cli/src/init/templates/next-route-handler.ts.tpl
  - packages/uploadkit-cli/src/init/types.ts
  - packages/uploadkit-cli/src/__tests__/init-next.test.ts
  - packages/uploadkit-cli/src/__fixtures__/projects/next-app/app/layout.tsx
autonomous: true
requirements: [GROW-15]
must_haves:
  truths:
    - "`uploadkit init` on a Next.js App Router project installs @uploadkitdev/next + @uploadkitdev/react"
    - "Route handler `app/api/uploadkit/[...uploadkit]/route.ts` is created"
    - "`app/layout.tsx` has UploadKitProvider wrapping body children + import added"
    - "`.env.local` has UPLOADKIT_API_KEY=uk_test_placeholder (or preserved if existing)"
    - "Running init a second time emits 'already configured' with zero file changes"
    - "All modifications are backed up before write; if any step fails, backup exists"
  artifacts:
    - path: "packages/uploadkit-cli/src/init/next-app.ts"
      provides: "initNextApp(ctx): Promise<InitResult>"
    - path: "packages/uploadkit-cli/src/init/install-deps.ts"
      provides: "installPackages(pm, root, pkgs, { skipInstall })"
    - path: "packages/uploadkit-cli/src/init/templates/next-route-handler.ts.tpl"
      provides: "Template for the route handler contents"
  key_links:
    - from: "packages/uploadkit-cli/src/commands/init.ts"
      to: "packages/uploadkit-cli/src/init/next-app.ts"
      via: "framework switch"
      pattern: "case \"next-app\""
    - from: "packages/uploadkit-cli/src/init/next-app.ts"
      to: "packages/uploadkit-cli/src/codemods/index.ts"
      via: "addImport, wrapChildrenOf, mergeEnv"
      pattern: "addImport|wrapChildrenOf|mergeEnv"
---

<objective>
Implement the `init` command for Next.js App Router — the flagship flow
and the one that runs in the majority of user projects. This plan also
establishes the shared `installPackages` helper and the `init`
orchestrator that plan 05 will extend for other frameworks.

Purpose: GROW-15 — deliver a working `npx uploadkit init` on the most
common stack, with backup, idempotency, and sub-60s runtime.
Output: Full `init` flow for Next.js App Router + integration tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12.5-uploadkit-cli-for-existing-projects-inserted/12.5-CONTEXT.md
@.planning/phases/12.5-uploadkit-cli-for-existing-projects-inserted/12.5-02-SUMMARY.md
@.planning/phases/12.5-uploadkit-cli-for-existing-projects-inserted/12.5-03-SUMMARY.md
@packages/next/src/index.ts
@packages/react/src/index.ts

<interfaces>
Per D-05 for Next.js App Router:
- Route handler path: `app/api/uploadkit/[...uploadkit]/route.ts`
- Provider wrap target: `app/layout.tsx` → wrap `<body>` children

Route handler template (expected exports — verify against
`packages/next/src/index.ts` for the actual helper name):
```typescript
// uploadkit:start
import { createRouteHandler } from "@uploadkitdev/next";
import { ukRouter } from "@/lib/uploadkit";

export const { GET, POST } = createRouteHandler({ router: ukRouter });
// uploadkit:end
```

A companion `lib/uploadkit.ts` stub is also created if absent (user
fills router definition). This is a CREATE action (no backup needed
beyond manifest entry).

Provider import:
```typescript
import { UploadKitProvider } from "@uploadkitdev/react";
```

Dependencies installed via `execa`:
- Dashboard pkgs: `@uploadkitdev/next@latest`, `@uploadkitdev/react@latest`
- PM command map: pnpm add / npm install / yarn add / bun add
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dependency installer + init orchestrator skeleton</name>
  <files>packages/uploadkit-cli/src/init/install-deps.ts, packages/uploadkit-cli/src/init/types.ts, packages/uploadkit-cli/src/commands/init.ts</files>
  <behavior>
    install-deps.ts:
    - `installPackages(pm, root, pkgs, { skipInstall }): Promise&lt;void&gt;`
    - Builds the right argv for each PM; streams stdout/stderr via `execa`
    - `skipInstall` flag → only write to package.json deps block (for CI/tests)
    - Throws on non-zero exit
    - Unit test mocks execa and asserts argv composition per PM

    commands/init.ts:
    - Calls detectFramework(cwd)
    - On `next-pages`/`unknown`: print reason via picocolors.yellow; exit 1
    - On supported framework: branch to per-framework implementation
      (plan 04 implements next-app; plan 05 implements rest — use a TODO
      + dispatch map for now)
    - Creates a backup session via `createBackupSession(root)` and passes
      it to the framework impl
    - On success: call `session.finalize()` and print summary with
      picocolors.green
    - Supports `--yes` (skip confirmation prompts) and `--skip-install`
  </behavior>
  <action>
    Extract the "scan → confirm → backup → mutate → finalize" flow into
    `commands/init.ts` so it's reusable. Per-framework modules receive
    `{ ctx: InitContext, session: BackupSession }` and return
    `{ installed: string[], modified: string[], created: string[] }`.
  </action>
  <verify>
    <automated>pnpm --filter uploadkit-cli test -- install-deps</automated>
  </verify>
  <done>Installer unit tests pass; init command branches correctly (placeholder for non-Next frameworks).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Next.js App Router implementation</name>
  <files>packages/uploadkit-cli/src/init/next-app.ts, packages/uploadkit-cli/src/init/templates/next-route-handler.ts.tpl, packages/uploadkit-cli/src/__tests__/init-next.test.ts, packages/uploadkit-cli/src/__fixtures__/projects/next-app/app/layout.tsx</files>
  <behavior>
    `initNextApp(ctx, session)`:
    1. Check idempotency: if `app/layout.tsx` contains `uploadkit:start`
       AND route handler exists → print "UploadKit already configured in
       this project" and return {skipped: true}
    2. Back up `app/layout.tsx` via `session.save`
    3. Use `addImport` + `wrapChildrenOf` to insert provider (per D-06
       markers); write back via `fs.writeFile`
    4. Create `app/api/uploadkit/[...uploadkit]/route.ts` from template
       (record create action in session)
    5. Create `lib/uploadkit.ts` stub if absent (create action)
    6. `mergeEnv(.env.local, { UPLOADKIT_API_KEY: "uk_test_placeholder" })`
    7. Unless `skipInstall`, call `installPackages(pm, root,
       ["@uploadkitdev/next@latest", "@uploadkitdev/react@latest"])`
    8. Return modification summary

    Fixture: augment `__fixtures__/projects/next-app/` with a realistic
    `app/layout.tsx` (font import, metadata, body with existing class).

    Tests (using `--skip-install` + tmpdir clone of fixture):
    - Fresh run: creates route handler, wraps layout, writes .env.local,
      creates backup session
    - Second run: prints "already configured", zero file changes
      (assert via file mtimes + manifest absence)
    - Backup contents match original layout.tsx byte-for-byte
    - After typecheck (use project tsc against the mutated layout), file
      is valid TypeScript (spawn tsc as subprocess)
  </behavior>
  <action>
    Template file is a plain .tpl read at runtime — NO runtime
    templating engine (no mustache/eta dep). Use simple `String.replace`
    for `{{tokens}}` if needed; most of it is static.
    Verify the exact SDK export name by reading `packages/next/src/index.ts`
    before writing the template; if the helper is named differently,
    note it in the SUMMARY.
  </action>
  <verify>
    <automated>pnpm --filter uploadkit-cli test -- init-next</automated>
  </verify>
  <done>Fixture-driven init test passes; idempotency verified; mutations survive tsc.</done>
</task>

</tasks>

<verification>
- `pnpm --filter uploadkit-cli test` all green
- Clone `__fixtures__/projects/next-app/` into tmpdir, run
  `node dist/index.js init --yes --skip-install` → exits 0, creates
  expected files, writes manifest
- Run again → "already configured"
</verification>

<success_criteria>
Next.js App Router `init` ships. GROW-15 is satisfied for the most
common framework. Plan 05 extends the same orchestrator to the other
three frameworks.
</success_criteria>

<output>
Create `12.5-04-SUMMARY.md` with: exact SDK helper names referenced,
any deviations from D-05 (e.g. if `createRouteHandler` is actually named
something else), measured init runtime on a reference fixture.
</output>
