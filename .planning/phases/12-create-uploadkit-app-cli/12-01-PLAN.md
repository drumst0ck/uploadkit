---
phase: 12-create-uploadkit-app-cli
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/create-uploadkit-app/package.json
  - packages/create-uploadkit-app/tsconfig.json
  - packages/create-uploadkit-app/tsup.config.ts
  - packages/create-uploadkit-app/src/index.ts
  - packages/create-uploadkit-app/src/version.ts
  - packages/create-uploadkit-app/.npmignore
  - packages/create-uploadkit-app/README.md
  - pnpm-workspace.yaml
autonomous: true
requirements: [GROW-01]
complexity: S

must_haves:
  truths:
    - "packages/create-uploadkit-app exists in the workspace and pnpm recognises it"
    - "pnpm --filter create-uploadkit-app build produces a runnable dist/index.js with a Node shebang"
    - "./dist/index.js --version prints the package version"
  artifacts:
    - path: packages/create-uploadkit-app/package.json
      provides: "bin entry create-uploadkit-app, files=[dist,templates], publishConfig.access=public, type=module"
      contains: '"bin"'
    - path: packages/create-uploadkit-app/src/index.ts
      provides: "entry point with --version / --help handling; exits cleanly"
      min_lines: 20
    - path: packages/create-uploadkit-app/tsup.config.ts
      provides: "builds ESM + CJS with banner '#!/usr/bin/env node' on ESM"
  key_links:
    - from: packages/create-uploadkit-app/package.json
      to: dist/index.js
      via: "bin field"
      pattern: '"bin":\s*\{'
---

<objective>
Create the `create-uploadkit-app` package skeleton and build pipeline so
later waves can add prompts, template engine, and templates. Nothing user-
facing ships here besides `--version` and `--help`.

Purpose: establish the publishable unit and tsup build that every other
plan in this phase builds on.

Output: a new workspace package that builds with tsup and exposes a
`create-uploadkit-app` bin.
</objective>

<context>
@CLAUDE.md
@.planning/phases/12-create-uploadkit-app-cli/CONTEXT.md
@.planning/phases/12-create-uploadkit-app-cli/PLAN.md
@packages/react/package.json
@packages/react/tsup.config.ts
@pnpm-workspace.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold package.json, tsconfig, tsup config</name>
  <files>
    packages/create-uploadkit-app/package.json,
    packages/create-uploadkit-app/tsconfig.json,
    packages/create-uploadkit-app/tsup.config.ts,
    packages/create-uploadkit-app/.npmignore,
    packages/create-uploadkit-app/README.md,
    pnpm-workspace.yaml
  </files>
  <action>
    Create `packages/create-uploadkit-app/package.json` (per D-01):
    - `"name": "create-uploadkit-app"` (unscoped)
    - `"type": "module"`
    - `"bin": { "create-uploadkit-app": "./dist/index.js" }`
    - `"files": ["dist", "templates"]`
    - `"publishConfig": { "access": "public" }`
    - `"engines": { "node": ">=20" }`
    - Scripts: `build` (tsup), `dev` (tsup --watch), `typecheck`, `lint`, `test` (vitest run)
    - Dependencies (install at `@latest` per CLAUDE.md): `@clack/prompts`, `mri`, `picocolors`, `execa`
    - Dev dependencies at `@latest`: `tsup`, `typescript`, `vitest`, `@types/node`, `@uploadkitdev/config` (workspace:*)

    Create `tsconfig.json` extending `@uploadkitdev/config/tsconfig/base.json` (check existing config package name — use whatever is present) with strict mode and `moduleResolution: "bundler"`.

    Create `tsup.config.ts`:
    ```ts
    import { defineConfig } from 'tsup';
    export default defineConfig({
      entry: ['src/index.ts'],
      format: ['esm', 'cjs'],
      target: 'node20',
      clean: true,
      dts: false,
      splitting: false,
      shims: false,
      banner: ({ format }) => (format === 'esm' ? { js: '#!/usr/bin/env node' } : {}),
    });
    ```

    Create `.npmignore`: exclude `src`, `tsconfig.json`, `tsup.config.ts`, `vitest.config.ts`, `node_modules`, `.turbo`. Keep `dist/` and `templates/`.

    Create `README.md` with a short usage snippet (mirrors what we'll ship on npm).

    Update `pnpm-workspace.yaml` only if `packages/*` glob doesn't already cover it (it almost certainly does — verify first).

    Reference D-01 and D-03.
  </action>
  <verify>
    <automated>pnpm install --frozen-lockfile=false &amp;&amp; pnpm --filter create-uploadkit-app typecheck</automated>
  </verify>
  <done>Package is recognised by pnpm; typecheck passes; no runtime code yet.</done>
</task>

<task type="auto">
  <name>Task 2: Entry point with --version and --help</name>
  <files>
    packages/create-uploadkit-app/src/index.ts,
    packages/create-uploadkit-app/src/version.ts,
    packages/create-uploadkit-app/src/index.test.ts
  </files>
  <behavior>
    - Running `node dist/index.js --version` prints the version from package.json and exits 0
    - Running `node dist/index.js --help` prints a short usage block and exits 0
    - Running with no args does NOT yet launch the full flow — prints "coming soon" and exits 0 (Wave 2 replaces this)
  </behavior>
  <action>
    Create `src/version.ts` that reads the package version at build time
    (tsup inlines `process.env.npm_package_version` via `define` — or
    simplest: import a generated `./version.generated.ts`; for this task
    read from a hard-coded constant and wire real injection in 12-08).

    Create `src/index.ts`:
    - Parse `process.argv.slice(2)` with `mri`
    - Handle `--version` / `-v` → print version, exit
    - Handle `--help` / `-h` → print usage
    - Otherwise: print `"create-uploadkit-app <VERSION> — run again from Wave 2"` and exit 0
    - Wrap in a top-level async `main()` with try/catch that prints errors via `picocolors.red` and exits 1

    Create a minimal Vitest test that imports the help-text constant and asserts it mentions `"create-uploadkit-app"` and all four template names.
  </action>
  <verify>
    <automated>pnpm --filter create-uploadkit-app build &amp;&amp; node packages/create-uploadkit-app/dist/index.js --version &amp;&amp; pnpm --filter create-uploadkit-app test</automated>
  </verify>
  <done>Bin runs, prints version, help text references all four templates, tests pass.</done>
</task>

</tasks>

<verification>
- `pnpm --filter create-uploadkit-app build` succeeds
- Resulting `dist/index.js` starts with `#!/usr/bin/env node`
- `node dist/index.js --version` prints a semver
- Package appears in `pnpm ls -r`
</verification>

<success_criteria>
Publishable skeleton exists; later plans can focus on behaviour without fighting tooling.
</success_criteria>

<output>
Write `.planning/phases/12-create-uploadkit-app-cli/12-01-SUMMARY.md` with package metadata, chosen versions of `@clack/prompts` / `mri`, and any deviations from the plan.
</output>
