---
phase: 10-testing-publishing-launch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/__tests__/with-api-key.test.ts
  - apps/api/__tests__/errors.test.ts
  - apps/api/__tests__/upload-request.test.ts
  - apps/api/__tests__/upload-complete.test.ts
  - apps/api/__tests__/multipart.test.ts
  - apps/api/__tests__/files.test.ts
  - apps/api/__tests__/projects.test.ts
  - apps/api/__tests__/keys.test.ts
  - apps/api/__tests__/tier-enforcement.test.ts
  - apps/api/__tests__/stripe-webhooks.test.ts
  - apps/api/__tests__/logs.test.ts
  - apps/api/__tests__/cleanup.test.ts
  - apps/api/__tests__/setup.ts
  - apps/api/package.json
autonomous: true
requirements:
  - INFRA-08

must_haves:
  truths:
    - "All .todo test stubs in apps/api/__tests__ are replaced with passing implementations"
    - "Upload request/complete/multipart routes have unit tests covering success and error paths"
    - "Tier enforcement logic is tested for FREE vs PRO behavior"
    - "Stripe webhook handler is tested for checkout.session.completed, subscription.updated, invoice events"
    - "All CRUD endpoints (files, projects, keys) have basic test coverage"
  artifacts:
    - path: "apps/api/__tests__/with-api-key.test.ts"
      provides: "withApiKey middleware tests (6 cases from .todo stubs)"
      contains: "returns 401"
    - path: "apps/api/__tests__/errors.test.ts"
      provides: "Error serializer tests (7 cases from .todo stubs)"
      contains: "serializeError"
    - path: "apps/api/__tests__/upload-request.test.ts"
      provides: "Presigned URL request endpoint tests"
      contains: "upload/request"
    - path: "apps/api/__tests__/stripe-webhooks.test.ts"
      provides: "Stripe webhook handler tests"
      contains: "checkout.session.completed"
    - path: "apps/api/__tests__/tier-enforcement.test.ts"
      provides: "Tier limit enforcement tests"
      contains: "FREE"
  key_links:
    - from: "apps/api/__tests__/with-api-key.test.ts"
      to: "apps/api/src/lib/with-api-key.ts"
      via: "import and mock"
      pattern: "import.*with-api-key"
    - from: "apps/api/__tests__/stripe-webhooks.test.ts"
      to: "apps/api/src/app/api/v1/webhooks"
      via: "import route handler"
      pattern: "webhook"
---

<objective>
Implement comprehensive Vitest unit tests for the API layer (~35-40 tests), filling in all existing .todo stubs and adding new test files for upload routes, CRUD endpoints, tier enforcement, Stripe webhooks, and the cleanup cron job.

Purpose: Per D-01, the project needs ~60-80 total Vitest tests. This plan covers the API half (~35-40). Combined with existing 37 tests (core+next) and Plan 02's additions, the total will reach the target range.
Output: 12 test files in apps/api/__tests__/ with all tests passing via `pnpm turbo test`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/api/__tests__/setup.ts
@apps/api/__tests__/with-api-key.test.ts
@apps/api/__tests__/errors.test.ts
@apps/api/vitest.config.ts
@apps/api/src/lib/with-api-key.ts
@apps/api/src/lib/errors.ts
@apps/api/src/lib/schemas.ts
@apps/api/src/lib/presign.ts
@apps/api/src/lib/stripe.ts
@apps/api/src/lib/stripe-meters.ts
@apps/api/src/app/api/v1/upload/request/route.ts
@apps/api/src/app/api/v1/upload/complete/route.ts
@apps/api/src/app/api/v1/upload/multipart/init/route.ts
@apps/api/src/app/api/v1/files/route.ts
@apps/api/src/app/api/v1/projects/route.ts
@apps/api/src/app/api/v1/keys/route.ts
@apps/api/src/app/api/v1/webhooks/route.ts
@apps/api/src/app/api/v1/logs/route.ts
@apps/api/src/app/api/cron/cleanup/route.ts
@packages/shared/src/constants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement .todo stubs and foundational API tests</name>
  <files>
    apps/api/__tests__/setup.ts
    apps/api/__tests__/with-api-key.test.ts
    apps/api/__tests__/errors.test.ts
    apps/api/__tests__/tier-enforcement.test.ts
    apps/api/__tests__/logs.test.ts
    apps/api/__tests__/cleanup.test.ts
    apps/api/package.json
  </files>
  <action>
    First, read the source files to understand the exact function signatures and behavior:
    - apps/api/src/lib/with-api-key.ts (withApiKey wrapper)
    - apps/api/src/lib/errors.ts (serializeError, serializeValidationError)
    - packages/shared/src/constants.ts (TIER_LIMITS)

    1. Update apps/api/__tests__/setup.ts: Add any missing env vars needed for new test files (e.g., STRIPE_SECRET_KEY='sk_test_fake', STRIPE_WEBHOOK_SECRET='whsec_test_fake'). Keep existing vars.

    2. Implement apps/api/__tests__/with-api-key.test.ts — replace all 6 .todo stubs:
       - Mock @uploadkit/db models (ApiKey.findOne), @upstash/ratelimit (Ratelimit), bcryptjs (compare)
       - Test: returns 401 when Authorization header is missing
       - Test: returns 401 when Bearer token format is invalid (no "Bearer " prefix)
       - Test: returns 429 when rate limit is exceeded (mock ratelimit.limit() returning success:false)
       - Test: returns 401 when API key hash is not found in DB (findOne returns null)
       - Test: returns 401 when API key is revoked (revokedAt is set)
       - Test: calls handler with ApiContext when key is valid (mock returns valid key, verify handler called with project/user context)

    3. Implement apps/api/__tests__/errors.test.ts — replace all 7 .todo stubs:
       - Import serializeError, serializeValidationError from source
       - Test: maps UploadKitError to Stripe-style { error: { type, code, message } }
       - Test: returns 500 with generic message for unknown errors
       - Test: maps 401 to authentication_error type
       - Test: maps 429 to rate_limit_error type
       - Test: includes suggestion field when present
       - Test: serializeValidationError maps Zod fieldErrors to details
       - Test: serializeValidationError returns 400 with VALIDATION_ERROR code

    4. Create apps/api/__tests__/tier-enforcement.test.ts (~6 tests):
       - Import TIER_LIMITS from @uploadkit/shared and the tier check logic from the upload request route or lib
       - Test: FREE tier allows upload when under storage limit
       - Test: FREE tier rejects upload when storage limit exceeded (returns 429-like error)
       - Test: PRO tier allows upload even when over FREE limits
       - Test: effectiveMaxSize enforces Math.min(routeMax, tierMax)
       - Test: FREE tier rejects when daily upload count exceeded
       - Test: file type validation rejects disallowed MIME types

    5. Create apps/api/__tests__/logs.test.ts (~3 tests):
       - Mock the UploadLog model
       - Test: GET returns logs filtered by since timestamp
       - Test: GET returns empty array when no logs match
       - Test: GET validates since parameter format

    6. Create apps/api/__tests__/cleanup.test.ts (~3 tests):
       - Mock File model and R2 client
       - Test: marks stale UPLOADING files (>1hr) as FAILED
       - Test: deletes corresponding R2 objects for stale uploads
       - Test: requires valid cron secret header

    Mock strategy: Mock all external dependencies (Mongoose models, R2 client, Upstash, Stripe, bcryptjs). Use vi.mock() at module level. Never make real DB or network calls. For Next.js route handlers, construct Request objects with `new Request()` and call the exported GET/POST functions directly.
  </action>
  <verify>
    <automated>cd /Users/drumstock/Developer/GitHub/uploadkit/apps/api && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>All 6 withApiKey .todo tests pass, all 7 error serializer .todo tests pass, tier enforcement has 6 passing tests, logs has 3 passing tests, cleanup has 3 passing tests. Zero .todo stubs remain in the API test suite.</done>
</task>

<task type="auto">
  <name>Task 2: Upload routes and CRUD endpoint tests</name>
  <files>
    apps/api/__tests__/upload-request.test.ts
    apps/api/__tests__/upload-complete.test.ts
    apps/api/__tests__/multipart.test.ts
    apps/api/__tests__/files.test.ts
    apps/api/__tests__/projects.test.ts
    apps/api/__tests__/keys.test.ts
    apps/api/__tests__/stripe-webhooks.test.ts
  </files>
  <action>
    Read the source route handlers first to understand exact request/response shapes:
    - apps/api/src/app/api/v1/upload/request/route.ts
    - apps/api/src/app/api/v1/upload/complete/route.ts
    - apps/api/src/app/api/v1/upload/multipart/init/route.ts
    - apps/api/src/app/api/v1/files/route.ts
    - apps/api/src/app/api/v1/projects/route.ts
    - apps/api/src/app/api/v1/keys/route.ts (or wherever key CRUD lives)
    - apps/api/src/app/api/v1/webhooks/route.ts

    1. Create apps/api/__tests__/upload-request.test.ts (~4 tests):
       - Mock withApiKey to inject valid ApiContext, mock presign utility, mock File/Project/FileRouter models
       - Test: returns presigned URL and file record on valid request
       - Test: returns 400 on invalid body (missing fileName or fileSize)
       - Test: returns 400 when file exceeds route maxFileSize
       - Test: returns 400 when file type not in allowedTypes

    2. Create apps/api/__tests__/upload-complete.test.ts (~3 tests):
       - Mock R2 HeadObject, File model, usage increment
       - Test: marks file UPLOADED and returns file data on valid complete
       - Test: returns 404 when file record not found
       - Test: returns 400 when R2 HEAD fails (file not actually uploaded)

    3. Create apps/api/__tests__/multipart.test.ts (~3 tests):
       - Mock R2 CreateMultipartUpload, CompleteMultipartUpload
       - Test: init returns uploadId and presigned part URLs
       - Test: complete assembles ETags and finalizes upload
       - Test: abort deletes the multipart upload and file record

    4. Create apps/api/__tests__/files.test.ts (~4 tests):
       - Mock File model with cursor pagination
       - Test: GET returns paginated file list with nextCursor
       - Test: GET filters by projectId query param
       - Test: DELETE removes file from R2 and soft-deletes in DB
       - Test: DELETE decrements storageUsed atomically

    5. Create apps/api/__tests__/projects.test.ts (~3 tests):
       - Mock Project model
       - Test: GET returns user's projects
       - Test: POST creates project with slug
       - Test: DELETE cascades (deletes files, keys, routers, project)

    6. Create apps/api/__tests__/keys.test.ts (~3 tests):
       - Mock ApiKey model, bcryptjs hash
       - Test: POST creates key, returns plaintext once, stores hash
       - Test: GET returns keys with masked display (uk_live_xxx...xxx)
       - Test: DELETE sets revokedAt timestamp

    7. Create apps/api/__tests__/stripe-webhooks.test.ts (~4 tests):
       - Mock Stripe constructEvent, Subscription/User models
       - Test: checkout.session.completed updates user tier and creates Subscription
       - Test: customer.subscription.updated syncs plan changes
       - Test: customer.subscription.deleted downgrades to FREE
       - Test: returns 400 on invalid webhook signature

    Each test file mocks withApiKey to skip auth and inject a fake ApiContext. Use `new Request()` to construct HTTP requests and call the route handler exports (GET, POST, DELETE) directly.
  </action>
  <verify>
    <automated>cd /Users/drumstock/Developer/GitHub/uploadkit/apps/api && npx vitest run --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <done>Upload request (4), complete (3), multipart (3), files (4), projects (3), keys (3), stripe webhooks (4) tests all pass. Total API tests: ~35-40 passing. `pnpm turbo test` from root succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test mocks vs production code | Tests must mock external deps, never call real services |
| CI env vs production env | Test env vars must be fake values, never real credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | I (Information Disclosure) | apps/api/__tests__/setup.ts | mitigate | All env vars in setup.ts use clearly fake values (sk_test_fake, whsec_test_fake) — never real credentials |
| T-10-02 | T (Tampering) | Test mocks | accept | Tests mock external services; no real DB/R2/Stripe calls. Acceptable: mocks test logic not integration |
</threat_model>

<verification>
Run from repo root:
```bash
pnpm turbo test
```
All API tests pass. Zero .todo stubs remain.
</verification>

<success_criteria>
- apps/api/__tests__/ contains 12 test files with ~35-40 total test cases
- All existing .todo stubs (withApiKey: 6, errors: 7) are implemented and passing
- Upload flow (request, complete, multipart) has test coverage
- CRUD endpoints (files, projects, keys) have test coverage
- Tier enforcement and Stripe webhooks have test coverage
- `pnpm turbo test` passes from root
</success_criteria>

<output>
After completion, create `.planning/phases/10-testing-publishing-launch/10-01-SUMMARY.md`
</output>
