---
phase: 10-testing-publishing-launch
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/README.md
  - packages/react/README.md
  - packages/next/README.md
  - .changeset/initial-release.md
  - package.json
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - INFRA-09

must_haves:
  truths:
    - "Each publishable SDK package has a polished README with badges, install, quickstart, API overview"
    - "A changeset entry exists for the initial 0.1.0 release of all three packages"
    - "Root package.json has a verify script that runs lint + typecheck + test + build"
    - "CI pipeline includes the test step and is confirmed to run test on push"
  artifacts:
    - path: "packages/core/README.md"
      provides: "Polished README for @uploadkit/core"
      contains: "npm install @uploadkit/core"
    - path: "packages/react/README.md"
      provides: "Polished README for @uploadkit/react"
      contains: "npm install @uploadkit/react"
    - path: "packages/next/README.md"
      provides: "Polished README for @uploadkit/next"
      contains: "npm install @uploadkit/next"
    - path: ".changeset/initial-release.md"
      provides: "Changeset entry for v0.1.0 beta"
      contains: "minor"
    - path: "package.json"
      provides: "verify script in root"
      contains: "verify"
  key_links:
    - from: ".changeset/initial-release.md"
      to: ".changeset/config.json"
      via: "Changesets reads entries"
      pattern: "changeset"
    - from: ".github/workflows/ci.yml"
      to: "package.json"
      via: "CI runs pnpm turbo test"
      pattern: "pnpm turbo test"
---

<objective>
Prepare for npm publish: create polished READMEs for all 3 SDK packages, add the initial changeset entry for v0.1.0 beta, add a root `pnpm verify` script, and ensure CI pipeline runs tests.

Purpose: Per D-03 and D-04, the SDK publishes as v0.1.0 beta with full polished READMEs. The verify script and CI update ensure the publish gate catches issues.
Output: 3 READMEs, 1 changeset entry, updated package.json and CI workflow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/core/package.json
@packages/react/package.json
@packages/next/package.json
@.changeset/config.json
@.github/workflows/ci.yml
@.github/workflows/release.yml
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Polished SDK READMEs with badges, install, quickstart, API overview</name>
  <files>
    packages/core/README.md
    packages/react/README.md
    packages/next/README.md
  </files>
  <action>
    Read existing READMEs if they exist (may be minimal or absent):
    - packages/core/README.md
    - packages/react/README.md
    - packages/next/README.md

    Also read source exports to document the API accurately:
    - packages/core/src/index.ts (exported functions/types)
    - packages/react/src/index.ts (exported components/hooks)
    - packages/next/src/index.ts (exported handler/types)

    Create polished READMEs per D-04. Each README follows this structure:

    **packages/core/README.md:**
    - Badges: npm version, npm downloads, license (MIT), CI status, bundle size
    - One-line description: "Lightweight TypeScript client for UploadKit file uploads"
    - Features list (bullet points): presigned URL uploads, multipart, progress tracking, retry, abort, BYOS
    - Install: `npm install @uploadkit/core` (show pnpm/yarn/npm)
    - Quickstart: createUploadKit + upload a file (8-12 lines of code)
    - API Overview table: createUploadKit, client.upload, client.listFiles, client.deleteFile with brief descriptions
    - Configuration options table
    - Links: Full docs, React components, Next.js adapter
    - License: MIT

    **packages/react/README.md:**
    - Badges: npm version, npm downloads, license, CI, bundle size
    - One-line: "Premium React upload components for UploadKit"
    - Features: UploadButton, UploadDropzone, UploadModal, FileList, FilePreview, CSS theming, dark mode, a11y
    - Install: `npm install @uploadkit/react` (peer dep: react >=18)
    - Quickstart: Provider + UploadButton example (10-15 lines)
    - Components table: each component with one-line description
    - Theming section: CSS custom properties (--uk-primary, etc.)
    - Links: Full docs, Core SDK, Next.js adapter
    - License: MIT

    **packages/next/README.md:**
    - Badges: npm version, npm downloads, license, CI
    - One-line: "Next.js App Router adapter for UploadKit with typed file routes"
    - Features: file router pattern, typed routes, middleware, BYOS, App Router native
    - Install: `npm install @uploadkit/next` (peer dep: next >=14)
    - Quickstart: define fileRouter + createUploadKitHandler + route.ts (12-15 lines)
    - API Overview: createUploadKitHandler, FileRouter type, middleware
    - BYOS section: brief mention with link to docs
    - Links: Full docs, Core SDK, React components
    - License: MIT

    Badge format: `![npm version](https://img.shields.io/npm/v/@uploadkit/core)` etc.
    CI badge: `![CI](https://github.com/uploadkit/uploadkit/actions/workflows/ci.yml/badge.svg)`
    Use real npm badge URLs (they'll work once published).

    Code examples should be TypeScript with syntax highlighting (```typescript).
    Keep each README under 200 lines — concise but complete.
  </action>
  <verify>
    <automated>test -f /Users/drumstock/Developer/GitHub/uploadkit/packages/core/README.md && test -f /Users/drumstock/Developer/GitHub/uploadkit/packages/react/README.md && test -f /Users/drumstock/Developer/GitHub/uploadkit/packages/next/README.md && echo "All READMEs exist" && grep -l "npm install" /Users/drumstock/Developer/GitHub/uploadkit/packages/*/README.md | wc -l</automated>
  </verify>
  <done>3 polished READMEs exist with badges, install instructions, quickstart code, API overview, and doc links. Each contains npm install command for its package.</done>
</task>

<task type="auto">
  <name>Task 2: Changeset entry, verify script, CI update</name>
  <files>
    .changeset/initial-release.md
    package.json
    .github/workflows/ci.yml
  </files>
  <action>
    1. Create .changeset/initial-release.md:
       ```markdown
       ---
       "@uploadkit/core": minor
       "@uploadkit/react": minor
       "@uploadkit/next": minor
       ---

       Initial beta release of UploadKit SDK packages.

       - @uploadkit/core: TypeScript upload client with presigned URLs, multipart, retry, abort, BYOS
       - @uploadkit/react: Premium React components (UploadButton, UploadDropzone, UploadModal, FileList, FilePreview) with CSS theming
       - @uploadkit/next: Next.js App Router adapter with typed file routes and middleware
       ```
       Per D-03: version 0.1.0 (minor bump from 0.0.0 or patch from existing 0.1.0 — Changesets will handle semver correctly). All three packages already have "version": "0.1.0" in their package.json, so this changeset entry sets up the NEXT version bump. If packages are at 0.1.0 already, the minor changeset will bump them to 0.2.0 on next release. Review the current versions and adjust if needed — if 0.1.0 is the target FIRST publish, the changeset should be a patch to keep it at 0.1.0, OR the changeset is for post-initial-publish. Since packages already have 0.1.0 in package.json and have never been published, the changeset entry documents the initial release and Changesets will create 0.2.0 on the next PR. This is correct workflow — the initial publish uses the version already in package.json (0.1.0), and this changeset is for the NEXT release.

       Alternative: If the goal is to publish 0.1.0 as the first version, simply run `pnpm changeset publish` without a changeset entry (publishes current versions). Create the changeset for documentation purposes and future versioning.

    2. Add verify script to root package.json:
       Read current package.json, add to scripts:
       ```json
       "verify": "turbo lint && turbo typecheck && turbo test && turbo build"
       ```
       This is the pre-publish gate per CONTEXT.md specifics.

    3. Update .github/workflows/ci.yml:
       The CI already has lint, typecheck, build, and test steps — verify this is correct. If the test step is already present (it is: `pnpm turbo test`), no changes needed to CI. However, verify the test step runs AFTER build (it does in current config).

       Only add if missing: ensure vitest is discoverable by turbo. Check that apps/api and packages/react have "test" scripts in their package.json. If any workspace package that has tests is missing a "test" script, add it so `turbo test` picks it up.

    4. Verify turbo.json includes "test" task. Read turbo.json and confirm.
  </action>
  <verify>
    <automated>test -f /Users/drumstock/Developer/GitHub/uploadkit/.changeset/initial-release.md && grep -q "verify" /Users/drumstock/Developer/GitHub/uploadkit/package.json && echo "Changeset and verify script exist"</automated>
  </verify>
  <done>Changeset entry exists for initial release. Root package.json has "verify" script. CI workflow runs lint, typecheck, test, build. All workspace packages with tests have "test" scripts for turbo discovery.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm publish | Packages published publicly must not contain credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06 | I (Information Disclosure) | SDK packages published to npm | mitigate | "files" field in each package.json restricts published contents to "dist" only; no source/test/config files published |
| T-10-07 | T (Tampering) | npm publish pipeline | mitigate | Release workflow requires NPM_TOKEN secret and uses changesets/action for controlled publish; id-token: write enables npm provenance |
</threat_model>

<verification>
Run from repo root:
```bash
pnpm verify
```
All 4 steps (lint, typecheck, test, build) pass.
</verification>

<success_criteria>
- 3 SDK packages have polished READMEs with badges, install, quickstart, API overview
- .changeset/initial-release.md exists with all 3 packages listed
- Root package.json has "verify" script that runs lint + typecheck + test + build
- CI workflow includes test step (already present, confirmed)
- `pnpm verify` passes from root
</success_criteria>

<output>
After completion, create `.planning/phases/10-testing-publishing-launch/10-04-SUMMARY.md`
</output>
