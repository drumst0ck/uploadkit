---
phase: 10-testing-publishing-launch
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/tests/validation.test.ts
  - packages/core/tests/multipart.test.ts
  - packages/core/tests/single.test.ts
  - packages/core/tests/http.test.ts
  - packages/react/tests/use-upload-kit.test.ts
  - packages/react/tests/setup.ts
  - packages/react/package.json
  - packages/react/vitest.config.ts
autonomous: true
requirements:
  - INFRA-08

must_haves:
  truths:
    - "Core SDK has unit tests for validation, single upload, multipart upload, and HTTP layer"
    - "React hook useUploadKit has tests covering state transitions (idle, uploading, success, error)"
    - "All new tests pass alongside existing 37 tests (17 core + 20 next)"
  artifacts:
    - path: "packages/core/tests/validation.test.ts"
      provides: "Client-side file validation tests"
      contains: "validateFile"
    - path: "packages/core/tests/single.test.ts"
      provides: "Single upload flow tests"
      contains: "singleUpload"
    - path: "packages/core/tests/multipart.test.ts"
      provides: "Multipart upload flow tests"
      contains: "multipartUpload"
    - path: "packages/react/tests/use-upload-kit.test.ts"
      provides: "React hook state machine tests"
      contains: "useUploadKit"
  key_links:
    - from: "packages/core/tests/validation.test.ts"
      to: "packages/core/src/validation.ts"
      via: "import"
      pattern: "import.*validation"
    - from: "packages/react/tests/use-upload-kit.test.ts"
      to: "packages/react/src"
      via: "import hook"
      pattern: "useUploadKit"
---

<objective>
Add unit tests for the core SDK internals (validation, single upload, multipart, HTTP) and the React useUploadKit hook state machine, bringing the total SDK test count from 37 to ~60+.

Purpose: Per D-01, complete the unit test coverage target. Plan 01 covers API tests; this plan covers SDK + React tests. Combined total should reach 70-80 tests.
Output: 4 new core test files + 1 React hook test file, all passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/core/src/validation.ts
@packages/core/src/single.ts
@packages/core/src/multipart.ts
@packages/core/src/http.ts
@packages/core/src/types.ts
@packages/core/tests/client.test.ts
@packages/core/tests/upload.test.ts
@packages/core/tests/retry.test.ts
@packages/react/src/use-upload-kit.ts
@packages/react/src/provider.tsx
@packages/react/src/index.ts
@packages/react/package.json
@vitest.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Core SDK internal tests (validation, single, multipart, HTTP)</name>
  <files>
    packages/core/tests/validation.test.ts
    packages/core/tests/single.test.ts
    packages/core/tests/multipart.test.ts
    packages/core/tests/http.test.ts
  </files>
  <action>
    Read source files first:
    - packages/core/src/validation.ts
    - packages/core/src/single.ts
    - packages/core/src/multipart.ts
    - packages/core/src/http.ts
    - packages/core/src/types.ts

    1. Create packages/core/tests/validation.test.ts (~6 tests):
       - Test: accepts valid file within size and type constraints
       - Test: rejects file exceeding maxFileSize
       - Test: rejects file with disallowed MIME type
       - Test: accepts file when no type restrictions (allowedTypes undefined/empty)
       - Test: rejects file with size 0 (empty file)
       - Test: handles maxFileSize as string ("4MB") by parsing to bytes

    2. Create packages/core/tests/single.test.ts (~4 tests):
       - Mock the http module (fetchApi) and XHR for progress
       - Test: calls POST /api/v1/upload/request then PUTs to presigned URL then POSTs /api/v1/upload/complete
       - Test: passes onProgress callback that receives 0-100 percentage
       - Test: propagates abort signal to XHR
       - Test: throws UploadKitError on presign failure (non-2xx)

    3. Create packages/core/tests/multipart.test.ts (~4 tests):
       - Mock fetchApi for multipart init/complete/abort endpoints
       - Test: splits file into 5MB chunks and uploads each part
       - Test: collects ETags from each part upload
       - Test: calls multipart/complete with uploadId and ETags on success
       - Test: calls multipart/abort on error and cleans up

    4. Create packages/core/tests/http.test.ts (~4 tests):
       - Mock global fetch
       - Test: fetchApi sets Authorization header with Bearer + apiKey
       - Test: fetchApi prepends baseUrl to path
       - Test: fetchApi throws UploadKitError on non-2xx response
       - Test: fetchApi parses JSON response body

    Mock strategy: Use vi.mock() for internal module dependencies. Mock global fetch for http.test.ts. Mock XHR constructor for single.test.ts progress tracking. No real network calls.
  </action>
  <verify>
    <automated>cd /Users/drumstock/Developer/GitHub/uploadkit && npx vitest run --config vitest.config.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>validation (6), single (4), multipart (4), http (4) tests all pass. Combined with existing client (7), upload (4), retry (6) = 31 core tests total.</done>
</task>

<task type="auto">
  <name>Task 2: React useUploadKit hook tests</name>
  <files>
    packages/react/tests/setup.ts
    packages/react/tests/use-upload-kit.test.ts
    packages/react/package.json
    packages/react/vitest.config.ts
  </files>
  <action>
    Read source files first:
    - packages/react/src/use-upload-kit.ts (or wherever the hook is exported)
    - packages/react/src/provider.tsx
    - packages/react/src/index.ts
    - packages/react/package.json

    1. Create packages/react/vitest.config.ts:
       ```typescript
       import { defineConfig } from 'vitest/config';
       export default defineConfig({
         test: {
           globals: true,
           environment: 'jsdom',
           setupFiles: ['tests/setup.ts'],
           include: ['tests/**/*.test.ts', 'tests/**/*.test.tsx'],
         },
       });
       ```

    2. Add test deps to packages/react/package.json devDependencies if missing:
       - "vitest": "latest"
       - "@testing-library/react": "latest"
       - "@testing-library/jest-dom": "latest"
       - "jsdom": "latest"

    3. Add "test": "vitest run" to packages/react/package.json scripts.

    4. Create packages/react/tests/setup.ts:
       - Import '@testing-library/jest-dom' for matchers
       - Mock @uploadkit/core's createUploadKit

    5. Create packages/react/tests/use-upload-kit.test.ts (~8 tests):
       - Use @testing-library/react renderHook
       - Wrap with UploadKitProvider (or mock the context)
       - Test: hook returns idle status initially
       - Test: hook returns upload, progress, status, error, abort functions/values
       - Test: calling upload() transitions status to "uploading"
       - Test: successful upload transitions status to "success" and returns file data
       - Test: failed upload transitions status to "error" with error message
       - Test: progress updates during upload (onProgress callback)
       - Test: calling abort() during upload transitions to "idle" or "error"
       - Test: hook throws if used outside UploadKitProvider (if applicable)

    Mock @uploadkit/core entirely — the hook test verifies state machine transitions, not actual upload logic.

    Update root vitest.config.ts include pattern to also match `packages/*/tests/**/*.test.tsx` if not already covered. The existing pattern `packages/*/tests/**/*.test.ts` should match .tsx as well since vitest resolves them, but verify.
  </action>
  <verify>
    <automated>cd /Users/drumstock/Developer/GitHub/uploadkit/packages/react && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>8 useUploadKit hook tests pass. packages/react has its own vitest config with jsdom environment. `pnpm turbo test` from root runs all workspace tests (core 31 + next 20 + react 8 + api ~35 = ~94 total).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test environment isolation | jsdom environment for React tests, node for core |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | I (Information Disclosure) | packages/react/tests/setup.ts | accept | React hook tests mock the SDK client entirely; no API keys or credentials in test code |
</threat_model>

<verification>
Run from repo root:
```bash
pnpm turbo test
```
All core + react + next + api tests pass. Total test count across workspace should be 70-90+.
</verification>

<success_criteria>
- packages/core/tests/ has 7 test files with ~31 total tests (existing 17 + 14 new)
- packages/react/tests/ has vitest config, setup, and useUploadKit test file with ~8 tests
- `pnpm turbo test` passes all workspace tests from root
- No test makes real network calls or requires env credentials
</success_criteria>

<output>
After completion, create `.planning/phases/10-testing-publishing-launch/10-02-SUMMARY.md`
</output>
