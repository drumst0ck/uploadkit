---
phase: quick-260418-ns8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/page.tsx
  - apps/web/src/app/layout.tsx
  - apps/web/src/app/globals.css
  - apps/web/src/lib/fonts.ts
  - apps/web/src/components/nav/navbar.tsx
  - apps/web/src/components/hero/hero-dropzone-demo.tsx
  - apps/web/src/components/hero/hero-dropzone-demo.css
  - apps/web/src/components/logos/logos-strip.tsx
  - apps/web/src/components/features/feature-mosaic.tsx
  - apps/web/src/components/features/design-features.tsx
  - apps/web/src/components/byos/byos-section.tsx
  - apps/web/src/components/tweaks/tweaks-panel.tsx
  - apps/web/src/components/footer/footer.tsx
  - apps/web/src/components/ui/design-icon.tsx
autonomous: true
requirements:
  - QUICK-260418-NS8
must_haves:
  truths:
    - "Landing page at apps/web root (/) renders with the Claude Design visual shell (Geist + JetBrains Mono typography, card chrome, feature grid with 1px borders, mosaic, BYOS, 5-col footer)"
    - "Violet accent palette (#6366f1 / #818cf8) is preserved — no yellow tokens anywhere; design's --accent / --accent-soft / --accent-glow map to violet"
    - "The hero shows a live, working drag-drop demo: drop or click triggers simulated upload with per-file progress bars, and each file ends in a 'done' state with a checkmark"
    - "Bottom-right Tweaks panel cycles theme (dark/light) and accent (violet, blue, green, orange, pink) and persists both to localStorage; values apply via data-theme / data-accent on documentElement"
    - "Default theme is dark and default accent is violet; refresh restores the last user choice"
    - "Existing LandingShowcase (real @uploadkitdev/react components) remains embedded in the page unchanged"
    - "Existing McpSection, PricingSection, and InstallCommand continue to render; no duplicate pricing or MCP sections are introduced"
    - "Logos strip, Component Mosaic, and BYOS section all appear in their intended order between hero/features and pricing"
    - "Footer renders as a 5-column grid (Brand / Product / Developers / Resources / Company) with status indicator"
    - "Zero runtime errors, zero hydration warnings, Next.js build passes"
  artifacts:
    - path: "apps/web/src/app/page.tsx"
      provides: "Redesigned landing composition per Claude Design order"
      contains: "HeroDropzoneDemo"
    - path: "apps/web/src/app/globals.css"
      provides: "Design tokens: --accent/--accent-soft/--accent-glow mapped to violet; data-accent[violet|blue|green|orange|pink] variants; data-theme[light|dark] overrides; Geist/JetBrains Mono vars; design section/nav/hero/mosaic/byos/tweaks/footer classes"
    - path: "apps/web/src/lib/fonts.ts"
      provides: "Geist + Geist Mono (or JetBrains Mono) loaded via next/font alongside existing Satoshi + Inter"
    - path: "apps/web/src/components/hero/hero-dropzone-demo.tsx"
      provides: "Client component: drag+click to add files, simulated progress via setInterval, per-file progress bar + pct + done state, 'try with sample files' action, demo chrome + footer kbd hint"
    - path: "apps/web/src/components/logos/logos-strip.tsx"
      provides: "Logos strip server component (section with label + row of 7 brand marks)"
    - path: "apps/web/src/components/features/design-features.tsx"
      provides: "Server component: 6-feature grid using 1px gap-on-line design (zap/layers/shield/globe/sparkle/palette)"
    - path: "apps/web/src/components/features/feature-mosaic.tsx"
      provides: "Server component: ComponentMosaic with mini previews (button/dropzone/radial/avatar/gallery/bars)"
    - path: "apps/web/src/components/byos/byos-section.tsx"
      provides: "Client component: provider cards + tabbed code snippet (R2/S3/GCS)"
    - path: "apps/web/src/components/tweaks/tweaks-panel.tsx"
      provides: "Client component: fixed bottom-right panel, theme segmented control + 5 accent swatches, localStorage persistence, sets documentElement[data-theme|data-accent]"
    - path: "apps/web/src/components/footer/footer.tsx"
      provides: "5-column footer grid: Brand/Product/Developers/Resources/Company + status row"
    - path: "apps/web/src/components/ui/design-icon.tsx"
      provides: "Stroke-based SVG icon set (upload/zap/shield/globe/sparkle/palette/layers/check/copy/x/arrow/code/sun/moon/gh/image/file/video)"
  key_links:
    - from: "apps/web/src/app/page.tsx"
      to: "apps/web/src/components/hero/hero-dropzone-demo.tsx"
      via: "import + render inside hero section (replaces HeroCodeWindow)"
      pattern: "HeroDropzoneDemo"
    - from: "apps/web/src/app/page.tsx"
      to: "apps/web/src/components/tweaks/tweaks-panel.tsx"
      via: "import + render at end of tree"
      pattern: "TweaksPanel"
    - from: "apps/web/src/components/tweaks/tweaks-panel.tsx"
      to: "document.documentElement"
      via: "useEffect setAttribute('data-theme'|'data-accent') + localStorage.setItem"
      pattern: "setAttribute\\('data-(theme|accent)'"
    - from: "apps/web/src/app/layout.tsx"
      to: "apps/web/src/app/globals.css"
      via: "font variable classes + default data-theme='dark' + data-accent='violet'"
      pattern: "data-accent=\"violet\""
    - from: "apps/web/src/app/page.tsx"
      to: "apps/web/src/components/showcase/landing-showcase.tsx"
      via: "existing import is preserved (LandingShowcase kept intact)"
      pattern: "LandingShowcase"
---

<objective>
Redesign apps/web landing page to match the Claude Design bundle at `/tmp/design-fetch/extracted/todolol/project/` while preserving the violet accent palette and all existing real-component surfaces (LandingShowcase, McpSection, PricingSection).

Purpose:
- Upgrade the landing to a premium, editorial layout (Geist + mono, card chrome, 1px line grid, mosaic tiles, BYOS provider cards, 5-col footer) that can compete with Vercel / Linear / Resend.
- Replace the static HeroCodeWindow with a working drag-drop demo that demonstrates the product live.
- Ship a real Tweaks panel (theme + accent cycler) as a first-class feature, not a placeholder.

Output:
- A fully redesigned `/` in `apps/web` using design-system tokens from the bundle, mapped onto the project's existing violet accent (#6366f1/#818cf8).
- New client component `HeroDropzoneDemo` with actual drag/drop, file input, simulated upload progress, and done state.
- New client component `TweaksPanel` persisting choices in localStorage and flipping documentElement data attrs.
- New sections: Logos strip, Component Mosaic, BYOS, redesigned Footer.
- All existing sections (Features — restyled; LandingShowcase — untouched; InstallCommand — kept inside a new Install card; McpSection — kept; PricingSection — kept or re-skinned inline) continue to work.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

# Project files executor must read before editing
@apps/web/src/app/page.tsx
@apps/web/src/app/layout.tsx
@apps/web/src/app/globals.css
@apps/web/src/lib/fonts.ts
@apps/web/src/components/nav/navbar.tsx
@apps/web/src/components/hero/hero-code-window.tsx
@apps/web/src/components/hero/install-command.tsx
@apps/web/src/components/footer/footer.tsx
@apps/web/src/components/showcase/landing-showcase.tsx
@apps/web/src/components/mcp/mcp-section.tsx
@apps/web/src/components/logo.tsx

# Design bundle (READ-ONLY reference — do not copy verbatim, translate to TS/Next)
@/tmp/design-fetch/extracted/todolol/project/styles.css
@/tmp/design-fetch/extracted/todolol/project/showcase.css
@/tmp/design-fetch/extracted/todolol/project/app.jsx
@/tmp/design-fetch/extracted/todolol/project/components/Hero.jsx
@/tmp/design-fetch/extracted/todolol/project/components/Nav.jsx
@/tmp/design-fetch/extracted/todolol/project/components/Logos.jsx
@/tmp/design-fetch/extracted/todolol/project/components/Features.jsx
@/tmp/design-fetch/extracted/todolol/project/components/ComponentMosaic.jsx
@/tmp/design-fetch/extracted/todolol/project/components/Install.jsx
@/tmp/design-fetch/extracted/todolol/project/components/Byos.jsx
@/tmp/design-fetch/extracted/todolol/project/components/Mcp.jsx
@/tmp/design-fetch/extracted/todolol/project/components/Pricing.jsx
@/tmp/design-fetch/extracted/todolol/project/components/Footer.jsx
@/tmp/design-fetch/extracted/todolol/project/components/Tweaks.jsx
@/tmp/design-fetch/extracted/todolol/project/components/Icons.jsx

<interfaces>
<!-- Key contracts the executor will need. Extract the rest from files above. -->

Next.js (App Router) font loading pattern in the project:
```ts
// apps/web/src/lib/fonts.ts — current
export const satoshi = localFont({ ..., variable: '--font-satoshi' })
export const inter  = Inter({ ..., variable: '--font-inter' })
```
Plan adds `geist` + `geistMono` (or JetBrains Mono) from `next/font/google` and exports them as `--font-geist` / `--font-mono`. They must be spread into `<html className>` alongside the existing satoshi/inter variables so nothing else on the site breaks.

LandingShowcase (must NOT be modified):
```tsx
// apps/web/src/components/showcase/landing-showcase.tsx
export function LandingShowcase(): JSX.Element
```

McpSection (must render unchanged):
```tsx
// apps/web/src/components/mcp/mcp-section.tsx
export function McpSection(): JSX.Element
```

InstallCommand (reused inside Install/CLI card in new design):
```tsx
// apps/web/src/components/hero/install-command.tsx — client
export function InstallCommand(): JSX.Element
```

Design tokens to add in globals.css (VIOLET, not yellow):
```css
:root {
  /* design bundle tokens, mapped to violet */
  --bg: #0a0a0b;
  --bg-elev: #111113;
  --bg-card: #141417;
  --bg-card-2: #17171b;
  --line: #23232a;
  --line-2: #2d2d35;
  --fg: #f4f4f5;
  --fg-muted: #a1a1aa;
  --fg-dim: #71717a;
  --fg-faint: #52525b;

  /* DEFAULT accent = violet, not yellow */
  --accent: #6366f1;          /* was oklch yellow — map to project violet */
  --accent-ink: #ffffff;
  --accent-soft: rgba(99, 102, 241, 0.15);
  --accent-glow: rgba(99, 102, 241, 0.45);

  --radius-sm: 6px;
  --radius:    10px;
  --radius-lg: 14px;
  --radius-xl: 20px;

  --font-sans: var(--font-geist), ui-sans-serif, system-ui, -apple-system, sans-serif;
  --font-mono: var(--font-mono), ui-monospace, "JetBrains Mono", Menlo, monospace;

  --max-w: 1240px;
  --shadow-sm: 0 1px 0 rgba(255,255,255,0.04) inset, 0 1px 2px rgba(0,0,0,0.4);
  --shadow-lg: 0 30px 60px -20px rgba(0,0,0,0.6), 0 1px 0 rgba(255,255,255,0.04) inset;
}

[data-theme="light"] { /* light overrides from bundle */ }

[data-accent="violet"] { --accent: #6366f1; --accent-soft: rgba(99,102,241,0.15); --accent-glow: rgba(99,102,241,0.45); --accent-ink:#fff; }
[data-accent="blue"]   { --accent: #3b82f6; --accent-soft: rgba(59,130,246,0.15);  --accent-glow: rgba(59,130,246,0.45);  --accent-ink:#fff; }
[data-accent="green"]  { --accent: #22c55e; --accent-soft: rgba(34,197,94,0.15);   --accent-glow: rgba(34,197,94,0.40);   --accent-ink:#0a0a0b; }
[data-accent="orange"] { --accent: #f97316; --accent-soft: rgba(249,115,22,0.15);  --accent-glow: rgba(249,115,22,0.40);  --accent-ink:#0a0a0b; }
[data-accent="pink"]   { --accent: #ec4899; --accent-soft: rgba(236,72,153,0.15);  --accent-glow: rgba(236,72,153,0.40);  --accent-ink:#fff; }
/* NO yellow variant — project keeps violet as default and forbids yellow */
```

Tweaks panel contract:
```ts
type Theme = 'dark' | 'light';
type Accent = 'violet' | 'blue' | 'green' | 'orange' | 'pink';
// on change -> document.documentElement.setAttribute('data-theme', theme)
// on change -> document.documentElement.setAttribute('data-accent', accent)
// on change -> localStorage.setItem('uk-theme', theme); localStorage.setItem('uk-accent', accent)
// on mount  -> read from localStorage and apply (guard for SSR: only in useEffect)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Design tokens, fonts, and layout scaffolding</name>
  <files>
    apps/web/src/lib/fonts.ts,
    apps/web/src/app/layout.tsx,
    apps/web/src/app/globals.css
  </files>
  <action>
Foundation pass — ship the design tokens and typography required by every new component BEFORE touching the page.

1) `apps/web/src/lib/fonts.ts`:
   - Add `Geist` and `Geist_Mono` (or `JetBrains_Mono`) imports from `next/font/google`, with `variable: '--font-geist'` and `variable: '--font-mono'` and `display: 'swap'`.
   - Keep the existing `satoshi` and `inter` exports untouched (other pages still import them).

2) `apps/web/src/app/layout.tsx`:
   - Import the new `geist` + `geistMono` alongside existing `satoshi`, `inter`.
   - Combine all four variables in `<html className>`.
   - Keep `data-theme="dark"` and ADD `data-accent="violet"` to `<html>` (default state).
   - Add an inline pre-hydration script (`<Script strategy="beforeInteractive" />` or `dangerouslySetInnerHTML` on a `<script>` inside `<head>`) that reads `localStorage` keys `uk-theme`/`uk-accent` and, if present, sets the matching `data-theme`/`data-accent` on `document.documentElement`. This prevents FOUC when TweaksPanel rehydrates.
     - Script must be defensive: wrap in `try { ... } catch {}`, never throw if localStorage is unavailable.
     - Allowed accent values: `violet|blue|green|orange|pink`. Allowed theme values: `dark|light`. Anything else is ignored.

3) `apps/web/src/app/globals.css`:
   - Append (do NOT replace) a new section `/* ─── design bundle tokens (Claude Design) ─── */` that adds the tokens listed in the `<interfaces>` block above: `--bg*`, `--bg-card*`, `--line*`, `--fg*`, `--accent*`, `--radius*`, `--font-sans`, `--font-mono`, `--max-w`, `--shadow-*`. DO NOT add `--accent: oklch(yellow…)` — default is violet `#6366f1`.
   - Add `[data-theme="light"]` override block (bg becomes `#fafaf7`, fg `#0a0a0b`, etc. — translate from `/tmp/design-fetch/extracted/todolol/project/styles.css`).
   - Add the 5 `[data-accent="violet|blue|green|orange|pink"]` variants (NO yellow).
   - Translate class blocks from the design bundle into globals.css with the EXACT class names the new components will use (one-to-one with the design):
     - `.nav`, `.nav-inner`, `.nav-left`, `.nav-right`, `.nav-links`, `.brand`, `.brand-mark`, `.brand-beta`, `.icon-btn`
     - `.btn`, `.btn-primary`, `.btn-ghost`, `.btn-lg`
     - `section` base + `.eyebrow` + h1/h2/h3 sizing + `.lead`
     - `.hero`, `.hero-grid`, `.hero-lead`, `.hero-cta`, `.hero-meta`, `.hero::before` glow
     - `.demo-card`, `.demo-chrome`, `.demo-chrome-dots`, `.demo-chrome-label`, `.demo-body`, `.demo-footer`
     - `.dropzone`, `.dropzone.dragover`, `.dz-icon`, `.dz-title`, `.dz-sub`, `.dz-rules`, `.dz-rule`
     - `.file-list`, `.file-row`, `.file-thumb`, `.file-info`, `.file-name`, `.file-meta`, `.file-pct`, `.file-status`, `.file-row .progress`
     - `.logos`, `.logos-label`, `.logos-row`
     - `.section-head`, `.features-grid`, `.feature`, `.feature-icon`, `.feature-tag`
     - `.mosaic-grid`, `.mosaic-cell`, `.mosaic-cell.span-{4,6,8}`, `.mini-btn/.mini-dz/.mini-radial/.mini-avatar/.mini-gallery/.mini-progress-bars`
     - `.install-grid`, `.install-card`, `.install-terminal`, `.install-pms`, `.install-pm-btn`, `.install-copy`
     - `.byos-grid`, `.providers`, `.provider`, `.provider-mark`, `.provider-name`, `.provider-desc`
     - `.code-block`, `.code-tabs`, `.code-tab`, `.code-body`, `.tok-k/.tok-s/.tok-f/.tok-c/.tok-v/.tok-p`
     - `.mcp-card`, `.mcp-grid`, `.mcp-clients`, `.mcp-client`, `.mcp-client-dot`, `.mcp-log`
     - `.pricing-grid`, `.tier`, `.tier.featured`, `.tier-head/.tier-name/.tier-badge/.tier-price/.tier-sub`
     - `footer` (scoped via descendant or data attr to avoid clobbering the OLD `.footer` class), `.footer-grid` (5 cols: `1.5fr 1fr 1fr 1fr 1fr`), `.footer-col`, `.footer-blurb`, `.footer-bottom`, `.status`, `.status-dot`
     - `.tweaks`, `.tweaks-row`, `.tweaks-row-label`, `.tweaks-seg`, `.tweaks-colors`, `.tweaks-swatch`
   - IMPORTANT: Because `apps/web/src/app/globals.css` already has `.footer-*` / `.pricing-*` / `.feature-*` classes from the previous design, give the new classes a scope to avoid collisions. Use a parent data attribute: wrap the landing page body in `<main data-surface="design-v2">` and prefix every NEW rule with `[data-surface="design-v2"]` (e.g. `[data-surface="design-v2"] .feature { ... }`). This preserves Pricing/Changelog/Blog pages on other routes.
   - Do NOT remove existing CSS. Append only.

Defensive note: Geist is a Google Font with known import name `Geist`. If npm/Next refuses the import, fall back to `Inter` for `--font-geist` and `JetBrains_Mono` for `--font-mono` — flag in the summary.
  </action>
  <verify>
    <automated>cd apps/web &amp;&amp; pnpm exec tsc --noEmit</automated>
    Then `pnpm --filter @uploadkitdev/web build` must succeed.
    Visual: load `/` and confirm Geist is loading (no FOUT to system font), `:root` contains new CSS vars via DevTools, `<html data-theme="dark" data-accent="violet">`.
  </verify>
  <done>
    `pnpm build` succeeds in apps/web. DevTools shows `--accent: #6366f1` computed on `:root`. No yellow anywhere. `<html>` has `data-accent="violet"`. Font variables `--font-geist` and `--font-mono` are populated.
  </done>
</task>

<task type="auto">
  <name>Task 2: Shared design-system primitives (Icon set + Tweaks panel + Logos + Navbar restyle)</name>
  <files>
    apps/web/src/components/ui/design-icon.tsx,
    apps/web/src/components/tweaks/tweaks-panel.tsx,
    apps/web/src/components/logos/logos-strip.tsx,
    apps/web/src/components/nav/navbar.tsx
  </files>
  <action>
Build the small reusable pieces the rest of the page will consume.

1) `apps/web/src/components/ui/design-icon.tsx` (Server Component, no 'use client'):
   - Port the stroke-based SVG icon set from `/tmp/design-fetch/extracted/todolol/project/components/Icons.jsx`.
   - Named export: `DesignIcon` with props `{ name: IconName; size?: number; className?: string }`.
   - Export `IconName` as a union of all supported icons: `'upload' | 'cloud' | 'zap' | 'lock' | 'code' | 'globe' | 'check' | 'copy' | 'x' | 'image' | 'file' | 'video' | 'gh' | 'moon' | 'sun' | 'layers' | 'terminal' | 'palette' | 'sparkle' | 'shield' | 'git' | 'gauge' | 'plug' | 'arrow'`.
   - Use `fill="none" stroke="currentColor" strokeWidth={1.75} strokeLinecap="round" strokeLinejoin="round"`.
   - Keep it zero-dependency (no lucide-react, no framer-motion).

2) `apps/web/src/components/tweaks/tweaks-panel.tsx` (Client Component):
   - `'use client'` at top.
   - State: `theme: 'dark'|'light'` (default 'dark'), `accent: 'violet'|'blue'|'green'|'orange'|'pink'` (default 'violet').
   - On mount: read `localStorage.getItem('uk-theme')` and `localStorage.getItem('uk-accent')` with guards; set state + `document.documentElement.setAttribute('data-theme'|'data-accent', ...)`.
   - `setTheme` / `setAccent` handlers: update state, setAttribute, setItem.
   - UI from `/tmp/design-fetch/extracted/todolol/project/components/Tweaks.jsx` and `.tweaks*` CSS: fixed bottom-right card with a title row ("Tweaks · live"), segmented theme toggle (dark | light), 5 swatches with oklch-matching colors for each accent (see interfaces block — map oklch to the project hexes: violet #6366f1, blue #3b82f6, green #22c55e, orange #f97316, pink #ec4899). Active swatch gets `.active` class (outline via `box-shadow: 0 0 0 2px var(--bg), 0 0 0 3px var(--fg)`).
   - Fully keyboard accessible: buttons, aria-labels on swatches (`aria-label={accent}`), the root div is `role="dialog" aria-label="Tweaks"`.
   - NO window.parent postMessage code (the design bundle had an edit-mode protocol — strip it, this is a plain production component).
   - The panel is ALWAYS visible (not gated by `active` prop) — this is a real user-facing feature, not an editor-only control.

3) `apps/web/src/components/logos/logos-strip.tsx` (Server Component):
   - Renders `<section className="logos">` wrapper per design CSS, with `.logos-label` + `.logos-row` containing 7 pseudo-brand rows (reuse the exact labels from `Logos.jsx`: "◆ Linear-ish", "⎈ helmet.io", "∿ driftwood", "◉ orbit.dev", "▲ vercel-ish", "⬡ honeycomb", "✺ novastack").
   - This is a decorative strip — no real logos needed.

4) `apps/web/src/components/nav/navbar.tsx` (edit existing):
   - Re-skin using the new design CSS classes (`.nav`, `.nav-inner`, `.brand`, `.brand-mark`, `.nav-links`, `.nav-right`, `.icon-btn`, `.btn`, `.btn-ghost`, `.btn-primary`).
   - Add a theme-toggle icon button (sun/moon) in `.nav-right` that dispatches to the same localStorage keys the TweaksPanel uses (e.g. call a small helper `setPreference('theme', next)` or directly setAttribute + setItem — either the Navbar and TweaksPanel BOTH read the same key, OR share a tiny `usePreferences` hook in `apps/web/src/components/tweaks/use-preferences.ts`). Chosen approach: create `use-preferences.ts` exporting `{ theme, accent, setTheme, setAccent }` and have BOTH Navbar and TweaksPanel consume it. Guarantees a single source of truth.
   - Keep existing nav links + GitHub button + "Sign in" + "Get Started" CTA. Nav link labels from the design: Features, Components, Docs, Pricing, Changelog.
   - Preserve the existing mobile menu (hamburger + slide-in panel). Just update the CTA button styling to use `.btn .btn-primary` and `.btn .btn-ghost` classes.
   - Must still be a client component (`'use client'`) because of state + effects.

Add `apps/web/src/components/tweaks/use-preferences.ts` as part of this task (same dir as tweaks-panel). Plain client hook. No context provider needed — both components read+write localStorage directly, with a `storage` event listener so a change in one tab/component reflects in the other instantly.
  </action>
  <verify>
    <automated>cd apps/web &amp;&amp; pnpm exec tsc --noEmit</automated>
    Boot dev server (`pnpm --filter @uploadkitdev/web dev`), open `/`:
    - Click nav sun/moon → page flips to light mode, bg-card becomes white, accent violet remains.
    - Open Tweaks panel (bottom-right) → click blue swatch → accent flips to #3b82f6 live. Refresh page → choices persist.
    - `document.documentElement.getAttribute('data-accent')` === 'blue' after selection, and `localStorage.getItem('uk-accent')` === 'blue'.
  </verify>
  <done>
    Nav renders with new design classes. TweaksPanel shows bottom-right on every scroll position. Theme + accent persist across reload. Navbar's sun/moon stays in sync with the TweaksPanel's segmented control (both write the same key).
  </done>
</task>

<task type="auto">
  <name>Task 3: Hero drag-drop demo (HeroDropzoneDemo) — the showpiece</name>
  <files>
    apps/web/src/components/hero/hero-dropzone-demo.tsx
  </files>
  <action>
Port `/tmp/design-fetch/extracted/todolol/project/components/Hero.jsx` demo portion into a strict-typed Next.js client component.

- `'use client'` at top.
- Named export: `HeroDropzoneDemo`. Default export optional.
- Types:
  ```ts
  type UploadStatus = 'uploading' | 'done' | 'error';
  interface DemoFile {
    id: number;
    name: string;
    size: number;
    type: string;
    progress: number;          // 0..100
    status: UploadStatus;
    preview: string | null;    // objectURL for images only
  }
  ```
- State: `files: DemoFile[]`, `dragging: boolean`, refs for the hidden file input and a drag counter.
- Behaviours:
  - Drag enter / over / leave with counter-based flicker prevention (match Hero.jsx).
  - Drop → add up to 4 files (slice last 4), each starts at `progress:0`, `status:'uploading'`. Image files get a `URL.createObjectURL` preview.
  - Click on `.dropzone` (or the underlined "browse" span) → opens the hidden `<input type="file" multiple>`.
  - "▸ try with sample files" button in `.demo-footer` → seeds 3 sample rows (hero-dark.png 2.4MB / onboarding.mp4 18.9MB / pitch-deck.pdf 4.2MB) as uploading.
  - `useEffect` progress tick: every 140ms, for each 'uploading' file increment `progress` by `max(1.2, 120/(sizeMB+1)) * (0.6 + Math.random()*0.8)`; when it hits 100 → set `status:'done'`.
  - Per-row remove button (× icon). On done, show `DesignIcon name="check"` instead.
- Cleanup: revoke object URLs in an unmount effect or when files array shrinks.
- Layout: render inside `<div className="demo-card">` with `.demo-chrome` (three dots span + `<UploadDropzone route="media" />` label), `.demo-body` containing `.dropzone` and `.file-list`, `.demo-footer` with the sample-files trigger and the "press ⌘ K to open upload modal" kbd hint.
- Use `DesignIcon` from Task 2 for upload / x / check / image / video / file.
- File status text:
  - `done` → `"uploaded · 312 ms"`
  - `uploading` → `"uploading…"`
  - `error` → `"failed"`
- Accessibility: dropzone has `role="button" tabIndex={0}`, Enter/Space also opens the file input. Remove buttons have `aria-label="Remove <filename>"`. Progress bar has `role="progressbar" aria-valuenow={progress} aria-valuemin={0} aria-valuemax={100}`.
- Respect `prefers-reduced-motion`: if set, skip the interval and jump each sample file to `status:'done'` immediately.

Size formatter util local to the file: bytes → "B / KB / MB". File-kind helper: image/video/other → pick icon.

DO NOT call any real upload API — this is purely client-side simulation. DO NOT import `@uploadkitdev/react` here (the real SDK is demo'd in the LandingShowcase section below).
  </action>
  <verify>
    <automated>cd apps/web &amp;&amp; pnpm exec tsc --noEmit</automated>
    Boot dev server, open `/`:
    - Drag a file over hero card → dropzone border flips to violet + background tints `var(--accent-soft)`.
    - Drop → file appears with 0% → progress bar animates to 100% → check icon appears + "done" text.
    - Click "try with sample files" → 3 rows animate in parallel.
    - No console errors, no memory leaks (no stray intervals after all done).
  </verify>
  <done>
    Hero demo works autonomously — drop, click, or sample seeds all drive progress to completion. Object URLs are revoked. Passes tsc strict mode.
  </done>
</task>

<task type="auto">
  <name>Task 4: Content sections — design Features grid, Component Mosaic, BYOS</name>
  <files>
    apps/web/src/components/features/design-features.tsx,
    apps/web/src/components/features/feature-mosaic.tsx,
    apps/web/src/components/byos/byos-section.tsx
  </files>
  <action>
Three new sections, each mapped 1:1 to a component from the design bundle, using the CSS classes established in Task 1 and the `DesignIcon` from Task 2.

1) `apps/web/src/components/features/design-features.tsx` (Server Component):
   - Port `Features.jsx`. Data array of 6 items: `{ icon: IconName; title: string; desc: string; tag: string }` using:
     - zap / "Presigned uploads" / "Files go directly from the browser to storage. No proxy hop, no memory ceilings." / "direct-to-bucket"
     - layers / "Multipart + resumable" / "Ship 5 GB video uploads with chunked transfer and pause/resume out of the box." / "up to 5 TB per file"
     - shield / "Type-safe file routes" / "Define allowed MIME types, size caps, and metadata once — get end-to-end types." / "FileRouter API"
     - globe / "Bring your own storage" / "Same SDK, your bucket. AWS S3, Cloudflare R2, GCS, Backblaze B2. Credentials server-side." / "BYOS mode"
     - sparkle / "MCP-native" / "Install an MCP server and let Claude Code, Cursor, and Windsurf wire the whole thing up." / "@uploadkitdev/mcp"
     - palette / "40+ premium components" / "Glass, terminal, brutal, neon, aurora — themeable via CSS custom properties, dark mode default." / "@uploadkitdev/react"
   - Section id: `features`. Uses `.section-head` + `.features-grid` + `.feature` per the scoped design CSS (remember: scoped under `[data-surface="design-v2"]`).

2) `apps/web/src/components/features/feature-mosaic.tsx` (Server Component):
   - Port `ComponentMosaic.jsx`. 6 cells each `span-4` (evenly balanced) with small MiniPreview renders. Data:
     - UploadButton / "The classic. One line, opinionated defaults." / button
     - UploadDropzone / "Drag, drop, done. Themeable via CSS custom properties." / dropzone
     - UploadProgressRadial / "Circular progress with live percentage readout." / radial
     - UploadAvatar / "Crop, preview, and replace. Optimized for profile photos." / avatar
     - UploadGalleryGrid / "Masonry grid with lazy loading and reordering." / gallery
     - UploadProgressStacked / "Multi-file progress with per-file speed and ETA." / bars
   - Includes the CTA "Browse all 40+ components →" button (`<a className="btn btn-ghost btn-lg" href="/#components-all">`). Section id: `components`.
   - Small `MiniPreview` local component renders the 6 visual previews exactly as in design (mini-btn / mini-dz / mini-radial / mini-avatar / mini-gallery / mini-progress-bars).
   - IMPORTANT: This component mosaic is a marketing preview and SITS ABOVE the existing `<LandingShowcase />` (which demos real SDK components). Keep both: Mosaic = marketing catalog, LandingShowcase = live interactive previews.

3) `apps/web/src/components/byos/byos-section.tsx` (Client Component, tabs need state):
   - `'use client'`. Port `Byos.jsx`: left column has eyebrow, h2, lead, 4-provider grid (R2 / S3 / GCS / B2) with provider marks; right column has `.code-block` with tabs (r2 / s3 / gcs) and plain `<pre>` snippets.
   - For provider mark colors map oklch swatches to hex: R2 → #f97316, S3 → #eab308, GCS → #3b82f6, B2 → #ef4444. Mark pill uses `color-mix(in oklab, …)` background — keep as-is (it's CSS color-mix supported modern).
   - Code snippets: three real-looking TS snippets for `createUploadKitHandler` + `createR2Storage` / `createS3Storage` / `createGCSStorage` (copy the snippet text from `Byos.jsx` verbatim — they're representative of the actual SDK API shape).
   - Syntax tokens: wrap manually with classes `.tok-k` / `.tok-s` / `.tok-c` / `.tok-v` for keyword / string / comment / var — OR keep as plain preformatted text inside `.code-body` for v1. Pick plain preformatted for simplicity; apply tokens only if the executor has context to spare.

Each section is an independent export; no cross-component state.
  </action>
  <verify>
    <automated>cd apps/web &amp;&amp; pnpm exec tsc --noEmit</automated>
    Visual: all three sections render; 6-item features grid shows icons in accent color, 1px dividers; mosaic cells hover-lift; BYOS tabs switch between R2/S3/GCS snippets with no flicker.
  </verify>
  <done>
    Three new sections compile and render. Icons colored via `var(--accent)`. No layout overflow on mobile (`<= 600px` collapses features to single column, mosaic cells to full width).
  </done>
</task>

<task type="auto">
  <name>Task 5: Footer 5-col redesign + Page composition wiring</name>
  <files>
    apps/web/src/components/footer/footer.tsx,
    apps/web/src/app/page.tsx
  </files>
  <action>
Final pass: replace the 4-col footer with the 5-col design footer, and stitch the page together in the correct order.

1) `apps/web/src/components/footer/footer.tsx`:
   - Replace the current 2-column + brand layout with a 5-column grid per `Footer.jsx`:
     Brand (1.5fr) / Product / Developers / Resources / Company (each 1fr).
     Columns + links:
     - Product: Features (`/#features`), Components (`/#components`), Pricing (`/pricing`), Changelog (`/changelog`)
     - Developers: Documentation (external docs.uploadkit.dev), REST API (docs.uploadkit.dev/api-reference), MCP server (`/#mcp`), CLI (docs.uploadkit.dev/getting-started)
     - Resources: Blog (`/blog`), Examples (external github examples link — use the real repo path if available, else `/blog`), Migration guide (docs.uploadkit.dev/migration-from-uploadthing), Discord (external — placeholder `#` is acceptable if no Discord exists)
     - Company: About (`/about` placeholder `#`), Privacy (`/privacy`), Terms (`/terms`), Contact (`mailto:support@uploadkit.dev`)
   - Brand column: brand mark (same `.brand-mark` + `DesignIcon name="upload"`), "uploadkit" wordmark, blurb "File uploads for developers. Open-source SDK, premium components, managed storage, BYOS. MIT."
   - `.footer-bottom`: left "© {year} UploadKit · MIT license", right `<span class="status"><span class="status-dot"/> All systems operational</span>`.
   - Remove the old `.footer-brand`, `.footer-tagline`, `.footer-copyright`, `.footer-col-heading` classes from the component (the old globals.css rules can stay — they simply won't be targeted anymore since the new footer lives under `[data-surface="design-v2"]`).
   - Server Component (no 'use client'), no React state.

2) `apps/web/src/app/page.tsx` — recompose:
   - Replace the existing section order (HeroSection → FeaturesSection → ShowcaseSection → McpSection → PricingSection → CtaSection) with:
     1. `<Navbar />` (existing, restyled in Task 2)
     2. `<main data-surface="design-v2">` opener — IMPORTANT: all NEW css rules are scoped to this parent
     3. Hero section using the design's `.hero` + `.hero-grid`. Left column: eyebrow ("v1.0 · 5 GB free forever"), h1 with `<em>` accent word, lead paragraph, `.hero-cta` (primary "Start for free" + ghost "Read the docs" buttons), `.hero-meta` checkmark row. Right column: `<HeroDropzoneDemo />`. Remove DarkVeil + BlurText — the new hero uses the design's radial glow via `.hero::before`.
     4. `<LogosStrip />`
     5. `<DesignFeatures />` (new 6-feature grid, replaces old SpotlightCard features — remove the old `FeaturesSection` function entirely)
     6. `<FeatureMosaic />`
     7. `<LandingShowcase />` (UNCHANGED — keep the existing component import)
     8. Install section: wrap the existing `<InstallCommand />` inside a new `.install-grid` + `.install-card` layout. Keep InstallCommand as-is; add two companion cards "Use the CLI" (`npx uploadkit init`) + "Starting fresh" (`npx create-uploadkit-app my-app`) using plain `.install-terminal` markup (no need for full copy-to-clipboard — InstallCommand already handles the main card). Actually simpler: remove the standalone InstallCommand from hero composition and place all 3 cards in this section. Main card uses the existing InstallCommand component (its pm-tabs + copy still works). The two secondary cards are inline-rendered terminal snippets.
     9. `<ByosSection />`
    10. `<McpSection />` (UNCHANGED)
    11. `<PricingSection />` — keep the existing PricingSection function from the current page.tsx (3 tiers Free/Pro/Team). Do NOT duplicate with a second pricing section. No changes needed; it will visually blend because `[data-surface="design-v2"]` scoping does not apply to its classes.
    12. `</main>`
    13. `<Footer />` (redesigned 5-col, outside `<main>` but still inside the page)
    14. `<TweaksPanel />` — render LAST (sits fixed bottom-right, positioned via CSS)
   - Delete the old `HeroCodeWindow` import; leave the file in place (don't delete — other potential consumers). Same for `DarkVeil`, `BlurText`, `SpotlightCard`, `AnimatedButton` — remove only from page.tsx imports if no longer used. Preserve `CtaSection` only if desired; safe to delete since the hero already has CTAs and pricing has CTAs.
   - Keep `JsonLd()` at top of return.
   - Metadata export stays unchanged.

Because the old globals.css still defines `.feature-card`, `.pricing-card`, etc. from the OLD design, the `[data-surface="design-v2"]` scoping on `<main>` ensures the NEW `.feature` / `.feature-grid` etc. apply to new sections WITHOUT overriding the pricing/changelog/blog pages on other routes. The OLD landing classes can be removed in a future cleanup task if desired (out of scope here).
  </action>
  <verify>
    <automated>cd apps/web &amp;&amp; pnpm exec tsc --noEmit &amp;&amp; pnpm --filter @uploadkitdev/web build</automated>
    Visual smoke test checklist (dev server):
    - `/` renders top-to-bottom: Nav → Hero (with working dropzone demo) → Logos → Features(6) → Mosaic(6) → LandingShowcase (real components) → Install(3 cards) → BYOS → MCP → Pricing(3 tiers) → Footer(5 cols) → Tweaks panel fixed bottom-right.
    - No overlapping sections, no horizontal scroll on desktop or `375px` mobile.
    - Accent stays violet default; cycling via Tweaks or Nav toggle flips accent across Hero glow, feature icons, CTA buttons, MCP accent dot, Pricing featured border, Footer status dot.
    - Light theme flip (via sun icon) inverts all bgs including demo card. No white-on-white or black-on-black.
    - `/pricing`, `/changelog`, `/blog` pages render UNCHANGED (because scope guard is on `<main data-surface="design-v2">` only).
    - No hydration warnings in console.
  </verify>
  <done>
    Page composes new design. Build passes. LandingShowcase still mounts real components. TweaksPanel visible + functional. Footer has 5 columns with status indicator.
  </done>
</task>

</tasks>

<verification>
End-to-end verification after all 5 tasks:

1. Build: `pnpm --filter @uploadkitdev/web build` succeeds with zero errors/warnings.
2. TypeScript: `pnpm -w exec tsc --noEmit` or `cd apps/web && pnpm exec tsc --noEmit` passes.
3. Visual diff (dev server `pnpm --filter @uploadkitdev/web dev`, open http://localhost:3000):
   - Landing uses Geist sans + JetBrains/Geist Mono typography.
   - Hero dropzone: drop a file → watches upload progress → turns into ✓ done.
   - Tweaks (bottom-right): click each of 5 accent swatches → page updates live. Refresh → last selection persists.
   - Navbar sun/moon icon toggles dark/light in sync with TweaksPanel segmented control.
   - LandingShowcase (real @uploadkitdev/react components) still renders unchanged below the mosaic.
   - McpSection + PricingSection still render (no duplicates from design import).
   - Footer shows 5 columns + status dot.
4. Regressions: `/pricing`, `/changelog`, `/blog`, `/blog/[slug]` pages all render unchanged (scope guard on `<main data-surface="design-v2">`).
5. No yellow anywhere — `grep -rni 'yellow\|oklch(.* 95)' apps/web/src/app/globals.css` should return zero matches in the NEW blocks (legacy grep hits outside the new blocks are acceptable but none should exist).
6. Mobile (375px): hero grid collapses to single column, features-grid to 1 column, mosaic to full-width cells, footer to 2-col, Tweaks panel stays visible (may shrink).
</verification>

<success_criteria>
- Landing page at `apps/web/src/app/page.tsx` (`/`) matches the visual shape of the Claude Design bundle with the violet accent palette.
- HeroDropzoneDemo is a live, working drag-drop simulation with progress bars and done state.
- TweaksPanel is a persistent user-facing feature: theme + accent cycler, localStorage-backed, synced with Navbar sun/moon.
- Five new reusable components exist: DesignIcon, TweaksPanel (+ usePreferences), LogosStrip, DesignFeatures, FeatureMosaic, ByosSection (+ redesigned Footer, restyled Navbar).
- LandingShowcase, McpSection, PricingSection, InstallCommand all still render.
- Next.js build + TypeScript strict mode pass.
- Other routes (`/pricing`, `/changelog`, `/blog*`) are visually unchanged.
- No yellow tokens introduced anywhere.
</success_criteria>

<output>
After completion, create `.planning/quick/260418-ns8-redesign-landing-page-per-claude-design-/260418-ns8-SUMMARY.md` summarizing:
- Files created/modified (absolute paths).
- Design token mapping (oklch → hex) used for the 5 accents.
- Any fallback fonts triggered if Geist failed to install.
- Screenshots or dev URL confirming the 10-section layout + Tweaks panel behavior.
- Any deferred cleanup (e.g. removing now-unused DarkVeil/BlurText/SpotlightCard from `apps/web/src/components/react-bits/` or `ui/` — note them, don't delete in this PR).
</output>
