---
phase: 06-dashboard
plan: 03
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - apps/dashboard/src/app/dashboard/projects/[slug]/files/page.tsx
  - apps/dashboard/src/app/api/internal/projects/[slug]/files/route.ts
  - apps/dashboard/src/app/api/internal/projects/[slug]/keys/route.ts
  - apps/dashboard/src/app/api/internal/projects/[slug]/routes/route.ts
  - apps/dashboard/src/app/dashboard/projects/[slug]/keys/page.tsx
  - apps/dashboard/src/app/dashboard/projects/[slug]/routes/page.tsx
  - apps/dashboard/src/components/data-table/data-table.tsx
  - apps/dashboard/src/components/data-table/data-table-toolbar.tsx
  - apps/dashboard/src/components/data-table/data-table-pagination.tsx
  - apps/dashboard/src/components/file-browser/file-browser.tsx
  - apps/dashboard/src/components/file-browser/file-preview-cell.tsx
  - apps/dashboard/src/components/file-browser/bulk-actions-bar.tsx
  - apps/dashboard/src/components/api-keys-table.tsx
  - apps/dashboard/src/components/file-routes-table.tsx
  - apps/dashboard/src/hooks/use-files.ts
autonomous: true
requirements:
  - DASH-04
  - DASH-05
  - DASH-06

must_haves:
  truths:
    - "File browser shows DataTable with columns: preview, name, size, type, date, actions"
    - "Image files show thumbnail previews in the preview column; non-images show file type icons"
    - "User can select multiple files via checkboxes and bulk delete with confirmation"
    - "File browser supports search by name, filter by type, and cursor-based pagination"
    - "API Keys page shows masked keys (uk_live_xxx...xxx), copy button, create and revoke with confirmation"
    - "File Routes page shows configured routes with create/edit/delete"
  artifacts:
    - path: "apps/dashboard/src/components/data-table/data-table.tsx"
      provides: "Generic TanStack Table wrapper"
      contains: "useReactTable"
    - path: "apps/dashboard/src/components/file-browser/file-browser.tsx"
      provides: "File browser composing DataTable with SWR"
      contains: "useSWR"
    - path: "apps/dashboard/src/components/file-browser/file-preview-cell.tsx"
      provides: "Thumbnail for images, icon for other types"
      contains: "img"
    - path: "apps/dashboard/src/components/file-browser/bulk-actions-bar.tsx"
      provides: "Delete selected button with confirmation"
      contains: "AlertDialog"
  key_links:
    - from: "apps/dashboard/src/components/file-browser/file-browser.tsx"
      to: "apps/dashboard/src/app/api/internal/projects/[slug]/files/route.ts"
      via: "SWR fetch"
      pattern: "/api/internal/projects/"
    - from: "apps/dashboard/src/components/data-table/data-table.tsx"
      to: "@tanstack/react-table"
      via: "useReactTable"
      pattern: "useReactTable"
---

<objective>
Build the file browser with TanStack Table (search, filter, sort, cursor pagination, bulk delete), API keys management page, and file routes configuration page. All three are project-scoped pages under /dashboard/projects/[slug]/*.

Purpose: The file browser is the most complex interactive component in the dashboard. API keys and file routes complete the project management feature set. All three require internal API routes for session-based data access.
Output: Working file browser with DataTable, API keys page, file routes page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-dashboard/06-CONTEXT.md
@.planning/phases/06-dashboard/06-RESEARCH.md
@.planning/phases/06-dashboard/06-01-SUMMARY.md

@packages/db/src/models/file.ts
@packages/db/src/models/api-key.ts
@packages/db/src/models/file-router.ts
@packages/db/src/models/project.ts

<interfaces>
From packages/db:
```typescript
export interface IFile extends Document {
  name: string;
  key: string;
  size: number;
  type: string;          // MIME type e.g. "image/png"
  status: 'UPLOADING' | 'UPLOADED' | 'FAILED' | 'DELETED';
  projectId: Types.ObjectId;
  userId: string;
  url?: string;          // CDN URL for the file
  metadata?: Record<string, unknown>;
  deletedAt?: Date;
  createdAt: Date;
  updatedAt: Date;
}

export interface IApiKey extends Document {
  keyPrefix: string;     // "uk_live_abc123" first 12 chars
  keyHash: string;       // SHA256 hex of full key — never return this to client
  projectId: Types.ObjectId;
  userId: string;
  name?: string;
  lastUsedAt?: Date;
  revokedAt?: Date;
  createdAt: Date;
}

export interface IFileRouter extends Document {
  slug: string;
  projectId: Types.ObjectId;
  allowedTypes: string[];
  maxFileSize: number;
  maxFileCount: number;
  metadata?: Record<string, unknown>;
  createdAt: Date;
  updatedAt: Date;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generic DataTable component + File browser with SWR, preview, bulk delete</name>
  <files>
    apps/dashboard/src/components/data-table/data-table.tsx,
    apps/dashboard/src/components/data-table/data-table-toolbar.tsx,
    apps/dashboard/src/components/data-table/data-table-pagination.tsx,
    apps/dashboard/src/components/file-browser/file-browser.tsx,
    apps/dashboard/src/components/file-browser/file-preview-cell.tsx,
    apps/dashboard/src/components/file-browser/bulk-actions-bar.tsx,
    apps/dashboard/src/hooks/use-files.ts,
    apps/dashboard/src/app/api/internal/projects/[slug]/files/route.ts,
    apps/dashboard/src/app/dashboard/projects/[slug]/files/page.tsx
  </files>
  <read_first>
    packages/db/src/models/file.ts,
    packages/db/src/models/project.ts,
    apps/dashboard/src/lib/format.ts,
    apps/dashboard/src/lib/fetcher.ts
  </read_first>
  <action>
1. Create `apps/dashboard/src/app/api/internal/projects/[slug]/files/route.ts`:
   - GET: auth() guard + connectDB(). Find project by slug + userId. Query files:
     - Filter: `{ projectId: project._id, status: 'UPLOADED', deletedAt: null }`
     - Optional query params: search (name regex), type (MIME prefix filter), cursor (for pagination)
     - Cursor pagination: if cursor provided, add `{ _id: { $lt: cursor } }` to filter
     - Sort by `{ _id: -1 }`, limit 20+1 to detect hasMore
     - Return `{ files: [...], nextCursor, hasMore }`
   - DELETE: auth() guard + connectDB(). Parse body `{ fileIds: string[] }`. Validate all files belong to project+user. Soft-delete by setting `deletedAt: new Date()`. Return 200 with count deleted.
   - `export const dynamic = 'force-dynamic'`.

2. Create `apps/dashboard/src/hooks/use-files.ts`:
   - useSWR with dynamic key: `/api/internal/projects/${slug}/files?${params}`.
   - Accepts: slug, search, type filter, cursor.
   - Returns { files, isLoading, error, nextCursor, hasMore, mutate }.

3. Create `apps/dashboard/src/components/data-table/data-table.tsx` ('use client'):
   - Generic component: `<DataTable<TData, TValue>>`.
   - Props: columns (ColumnDef[]), data (TData[]), isLoading (boolean).
   - Uses useReactTable with getCoreRowModel, getSortedRowModel, getFilteredRowModel.
   - manualPagination: true (cursor-based from API).
   - getRowId from _id field.
   - Renders shadcn Table component with TableHeader, TableBody, TableRow, TableCell.
   - Loading state: skeleton rows when isLoading.
   - Empty state: "No results" centered message.

4. Create `apps/dashboard/src/components/data-table/data-table-toolbar.tsx` ('use client'):
   - Props: table instance, onSearch (callback), onTypeFilter (callback), selectedCount (number).
   - Renders: search Input with debounce (300ms), type Select filter (All, Images, Documents, Videos, Audio, Other), BulkActionsBar when selectedCount > 0.

5. Create `apps/dashboard/src/components/data-table/data-table-pagination.tsx` ('use client'):
   - Props: hasMore, isLoading, onNextPage, onPreviousPage (if we track cursor history), showing count.
   - "Next" button enabled when hasMore. "Previous" managed via cursor stack.
   - Shows "Showing X files" text.

6. Create `apps/dashboard/src/components/file-browser/file-preview-cell.tsx` per D-05:
   - Props: file ({ name, type, url }).
   - If type starts with "image/": render `<img src={file.url} alt={file.name} className="h-8 w-8 rounded object-cover" />`. Use regular img (not next/image) for simplicity with CDN URLs, or use next/image with remotePatterns configured in Plan 01.
   - Otherwise: map MIME type prefix to lucide icon — FileText (text/*), Video (video/*), Music (audio/*), FileSpreadsheet (application/pdf), File (default).
   - Icon rendered at 20x20 in zinc-400.

7. Create `apps/dashboard/src/components/file-browser/bulk-actions-bar.tsx` per D-06:
   - Props: selectedCount (number), onDelete (callback).
   - Shows when selectedCount > 0: "{N} file(s) selected" + "Delete selected" Button variant="destructive".
   - Delete triggers AlertDialog: "Are you sure? This will permanently delete {N} file(s)." Confirm/Cancel buttons.
   - On confirm: calls onDelete which triggers the DELETE /api/internal/projects/[slug]/files endpoint.

8. Create `apps/dashboard/src/components/file-browser/file-browser.tsx` ('use client'):
   - Composes everything: useFiles hook, DataTable with file columns, DataTableToolbar, DataTablePagination.
   - Column definitions per D-04:
     - select: Checkbox column for bulk selection
     - preview: FilePreviewCell (D-05)
     - name: sortable text
     - size: formatBytes
     - type: MIME type
     - createdAt: formatDate
     - actions: DropdownMenu with "Copy URL", "Delete" options
   - Row selection state via TanStack Table rowSelection. Reset on cursor change (Pitfall 6).
   - Search triggers SWR refetch with new search param.
   - Pagination triggers SWR refetch with new cursor.

9. Create `apps/dashboard/src/app/dashboard/projects/[slug]/files/page.tsx`:
   - Server component shell: `export const dynamic = 'force-dynamic'`.
   - Renders heading "Files" and the FileBrowser client component, passing slug from params.
  </action>
  <verify>
    <automated>cd /Users/drumstock/Developer/GitHub/uploadkit && pnpm turbo build --filter=@uploadkit/dashboard 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - apps/dashboard/src/components/data-table/data-table.tsx uses useReactTable with manualPagination:true and getRowId
    - apps/dashboard/src/components/file-browser/file-preview-cell.tsx renders img for image/* MIME types and lucide icons for others
    - apps/dashboard/src/components/file-browser/bulk-actions-bar.tsx shows AlertDialog on delete with count
    - apps/dashboard/src/components/file-browser/file-browser.tsx defines 7 columns: select, preview, name, size, type, createdAt, actions
    - apps/dashboard/src/hooks/use-files.ts uses useSWR with slug and cursor params
    - apps/dashboard/src/app/api/internal/projects/[slug]/files/route.ts exports GET (with cursor pagination) and DELETE (bulk soft-delete)
    - Row selection resets when cursor changes
    - `pnpm turbo build --filter=@uploadkit/dashboard` passes
  </acceptance_criteria>
  <done>File browser renders DataTable with preview thumbnails, search, type filter, cursor pagination, and bulk delete with confirmation dialog.</done>
</task>

<task type="auto">
  <name>Task 2: API Keys management and File Routes configuration pages</name>
  <files>
    apps/dashboard/src/app/api/internal/projects/[slug]/keys/route.ts,
    apps/dashboard/src/app/api/internal/projects/[slug]/routes/route.ts,
    apps/dashboard/src/app/dashboard/projects/[slug]/keys/page.tsx,
    apps/dashboard/src/app/dashboard/projects/[slug]/routes/page.tsx,
    apps/dashboard/src/components/api-keys-table.tsx,
    apps/dashboard/src/components/file-routes-table.tsx
  </files>
  <read_first>
    packages/db/src/models/api-key.ts,
    packages/db/src/models/file-router.ts,
    packages/db/src/models/project.ts,
    packages/shared/src/types.ts
  </read_first>
  <action>
1. Create `apps/dashboard/src/app/api/internal/projects/[slug]/keys/route.ts`:
   - GET: auth() + connectDB(). Find project by slug+userId. Return ApiKey.find({ projectId: project._id, revokedAt: null }).select('-keyHash').sort({ _id: -1 }).lean(). Return array with keyPrefix, name, lastUsedAt, createdAt. NEVER return keyHash.
   - POST: auth() + connectDB(). Find project. Generate new API key: full key = `uk_live_${nanoid(32)}`, compute SHA256 hash, store keyHash + keyPrefix (first 12 chars). Return 201 with the FULL plaintext key (one-time display) + keyPrefix + createdAt. After this response, the full key is never available again.
   - DELETE: auth() + connectDB(). Parse body { keyId: string }. Verify key belongs to project+user. Set revokedAt: new Date(). Return 200.
   - `export const dynamic = 'force-dynamic'`.

2. Create `apps/dashboard/src/components/api-keys-table.tsx` ('use client') per DASH-05:
   - Uses SWR to fetch keys from /api/internal/projects/{slug}/keys.
   - Table with columns: Name (or "Untitled"), Key (masked: `${keyPrefix}...` — show first 12 chars + "..."), Last Used (formatDate or "Never"), Created (formatDate), Actions.
   - "Create API Key" button opens Dialog with optional name input. On submit, POST to API. Display the returned full key in a highlighted box with "Copy" button (navigator.clipboard.writeText). Warning text: "This key will only be shown once."
   - Actions dropdown per key: "Copy prefix", "Revoke". Revoke opens AlertDialog: "Revoke this API key? Any applications using this key will stop working." Confirm triggers DELETE.
   - After create/revoke, mutate SWR cache.

3. Create `apps/dashboard/src/app/dashboard/projects/[slug]/keys/page.tsx`:
   - `export const dynamic = 'force-dynamic'`.
   - Renders heading "API Keys" + description "Manage API keys for this project." + ApiKeysTable client component with slug prop.

4. Create `apps/dashboard/src/app/api/internal/projects/[slug]/routes/route.ts`:
   - GET: auth() + connectDB(). Return FileRouter.find({ projectId: project._id }).lean().
   - POST: auth() + connectDB(). Create new file router config: { slug, projectId, allowedTypes, maxFileSize, maxFileCount }. Validate slug is unique within project. Return 201.
   - PUT: auth() + connectDB(). Update existing router by _id. Validate ownership.
   - DELETE: auth() + connectDB(). Delete router by _id. Validate ownership.
   - `export const dynamic = 'force-dynamic'`.

5. Create `apps/dashboard/src/components/file-routes-table.tsx` ('use client') per DASH-06:
   - Uses SWR to fetch routes from /api/internal/projects/{slug}/routes.
   - Table: Slug, Allowed Types (Badge for each), Max File Size (formatBytes), Max Count, Actions.
   - "Create Route" button opens Dialog with form: slug Input, allowed types multi-select (common MIME types), max file size Input (MB), max file count Input.
   - Actions: Edit (opens same Dialog pre-filled), Delete (AlertDialog confirmation).
   - After mutations, mutate SWR cache.

6. Create `apps/dashboard/src/app/dashboard/projects/[slug]/routes/page.tsx`:
   - `export const dynamic = 'force-dynamic'`.
   - Renders heading "File Routes" + description "Configure upload routes for this project." + FileRoutesTable with slug prop.
  </action>
  <verify>
    <automated>cd /Users/drumstock/Developer/GitHub/uploadkit && pnpm turbo build --filter=@uploadkit/dashboard 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - apps/dashboard/src/app/api/internal/projects/[slug]/keys/route.ts never returns keyHash, POST returns full plaintext key once
    - apps/dashboard/src/components/api-keys-table.tsx shows masked keys, copy button uses navigator.clipboard, revoke has AlertDialog
    - apps/dashboard/src/app/api/internal/projects/[slug]/routes/route.ts exports GET, POST, PUT, DELETE
    - apps/dashboard/src/components/file-routes-table.tsx shows routes with Badge for allowed types
    - Both pages render with slug from params
    - `pnpm turbo build --filter=@uploadkit/dashboard` passes
  </acceptance_criteria>
  <done>API Keys page shows masked keys with create (one-time full key display), copy, revoke with confirmation. File Routes page shows configured routes with CRUD. Both project-scoped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> /api/internal/projects/[slug]/files | File operations scoped to project+user |
| client -> /api/internal/projects/[slug]/keys | API key operations scoped to project+user |
| client -> clipboard API | One-time API key copy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09 | Information Disclosure | API keys GET endpoint | mitigate | select('-keyHash') on all queries. keyHash NEVER returned to client. Full plaintext key returned only on POST create (one-time display). |
| T-06-10 | Elevation of Privilege | File/key deletion | mitigate | All delete operations verify projectId + userId ownership before mutation. Never trust client-provided projectId. |
| T-06-11 | Tampering | Bulk file delete | mitigate | Validate every fileId in the array belongs to the authenticated user's project before soft-deleting. |
| T-06-12 | Spoofing | Internal API routes | mitigate | auth() check at top of every handler. 401 if no session. Project looked up by slug + session.user.id. |
</threat_model>

<verification>
1. `pnpm turbo build --filter=@uploadkit/dashboard` passes
2. File browser renders with all 7 columns including preview thumbnails
3. Bulk select + delete works with confirmation dialog
4. API keys display masked, create shows full key once, revoke works
5. File routes CRUD functional with validation
</verification>

<success_criteria>
- File browser DataTable with TanStack Table: search, type filter, cursor pagination, sort
- Image thumbnails in preview column, file type icons for non-images
- Checkbox bulk select with "Delete selected" and confirmation
- API Keys: masked display, one-time full key on create, copy to clipboard, revoke with confirmation
- File Routes: list, create, edit, delete with dialog forms
</success_criteria>

<output>
After completion, create `.planning/phases/06-dashboard/06-03-SUMMARY.md`
</output>
