---
phase: 12.5-uploadkit-cli-for-existing-projects-inserted
plan: 02
type: execute
wave: 2
depends_on: [12.5-01]
files_modified:
  - packages/uploadkit-cli/src/detect/framework.ts
  - packages/uploadkit-cli/src/detect/index.ts
  - packages/uploadkit-cli/src/detect/types.ts
  - packages/uploadkit-cli/src/detect/package-manager.ts
  - packages/uploadkit-cli/src/__tests__/detect.test.ts
  - packages/uploadkit-cli/src/__fixtures__/projects/next-app/package.json
  - packages/uploadkit-cli/src/__fixtures__/projects/next-pages/package.json
  - packages/uploadkit-cli/src/__fixtures__/projects/sveltekit/package.json
  - packages/uploadkit-cli/src/__fixtures__/projects/remix/package.json
  - packages/uploadkit-cli/src/__fixtures__/projects/vite-react/package.json
  - packages/uploadkit-cli/src/__fixtures__/projects/unknown/package.json
autonomous: true
requirements: [GROW-15]
must_haves:
  truths:
    - "Detector correctly identifies Next.js App Router vs Pages Router"
    - "Detector identifies SvelteKit, Remix, and Vite+React projects"
    - "Detector returns 'unknown' and a reason string for unsupported stacks"
    - "Detector returns the project's package manager (pnpm/npm/yarn/bun)"
  artifacts:
    - path: "packages/uploadkit-cli/src/detect/framework.ts"
      provides: "detectFramework(cwd) → { framework, root, reason? }"
    - path: "packages/uploadkit-cli/src/detect/package-manager.ts"
      provides: "detectPackageManager(cwd) → 'pnpm'|'npm'|'yarn'|'bun'"
    - path: "packages/uploadkit-cli/src/detect/types.ts"
      provides: "Framework union type shared across plans"
  key_links:
    - from: "packages/uploadkit-cli/src/detect/index.ts"
      to: "packages/uploadkit-cli/src/detect/framework.ts"
      via: "re-export"
      pattern: "export \\* from"
---

<objective>
Build the framework detector and package-manager detector. Given a
working directory, return a typed verdict used by `init` and `add` to
decide which insertion strategy to apply — or refuse gracefully.

Purpose: Every other command branches on the detection result. Must be
deterministic and testable via fixture directories.
Output: `detectFramework(cwd)` + `detectPackageManager(cwd)` with full
test coverage across 6 fixture projects.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12.5-uploadkit-cli-for-existing-projects-inserted/12.5-CONTEXT.md
@.planning/phases/12.5-uploadkit-cli-for-existing-projects-inserted/12.5-01-SUMMARY.md
@packages/create-uploadkit-app/src/pm.ts

<interfaces>
Re-implement PM detection following the pattern from
`packages/create-uploadkit-app/src/pm.ts` (inspect lockfiles:
pnpm-lock.yaml, package-lock.json, yarn.lock, bun.lock* — in that order,
fall back to `process.env.npm_config_user_agent` parsing).

The Framework union (exported from `detect/types.ts`):
```typescript
export type Framework =
  | "next-app"
  | "next-pages"
  | "sveltekit"
  | "remix"
  | "vite-react"
  | "unknown";

export type PackageManager = "pnpm" | "npm" | "yarn" | "bun";

export interface DetectionResult {
  framework: Framework;
  root: string;            // absolute path where package.json lives
  packageManager: PackageManager;
  reason?: string;         // populated when framework === "unknown" or "next-pages"
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fixture projects + detection rules</name>
  <files>packages/uploadkit-cli/src/__fixtures__/projects/*/package.json, packages/uploadkit-cli/src/detect/framework.ts, packages/uploadkit-cli/src/detect/types.ts, packages/uploadkit-cli/src/detect/index.ts</files>
  <behavior>
    Per D-04:
    - Has `next` in deps AND `app/` directory (no `pages/_app.*`) → `next-app`
    - Has `next` in deps AND `pages/_app.*` (no `app/`) → `next-pages` with reason "Pages Router not supported — migrate to App Router"
    - Has `@sveltejs/kit` in deps → `sveltekit`
    - Has `@remix-run/*` OR `@react-router/dev` → `remix`
    - Has `vite` AND `react` in deps (no next/sveltekit/remix) → `vite-react`
    - Otherwise → `unknown` with reason listing what was missing
    Fixture projects are minimal: each has a package.json with the right
    deps and a single marker file (e.g. `app/layout.tsx` empty, or
    `svelte.config.js` empty) to exercise file-system checks.
  </behavior>
  <action>
    Implement `detectFramework(cwd: string): DetectionResult`:
    1. Walk up from cwd to find nearest package.json (bail at fs root)
    2. Parse deps + devDeps
    3. Run rules in order (next → sveltekit → remix → vite+react)
    4. For Next.js: check for `app/` dir vs `pages/` dir via fs.existsSync
    5. Return DetectionResult; populate `reason` on unknown/next-pages
    Create 6 fixture directories under `src/__fixtures__/projects/` each
    containing just enough to match the rule.
  </action>
  <verify>
    <automated>pnpm --filter uploadkit-cli test -- detect</automated>
  </verify>
  <done>All 6 fixtures resolve to the expected framework verdict.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Package-manager detector + integration test</name>
  <files>packages/uploadkit-cli/src/detect/package-manager.ts, packages/uploadkit-cli/src/__tests__/detect.test.ts</files>
  <behavior>
    - `detectPackageManager(cwd)` checks for lockfiles in priority order:
      `pnpm-lock.yaml` → pnpm, `bun.lock*` → bun, `yarn.lock` → yarn,
      `package-lock.json` → npm
    - Fallback: parse `process.env.npm_config_user_agent` ("pnpm/8.x" etc.)
    - Final fallback: "npm"
    - Integration test combines detectFramework + detectPackageManager
      across fixtures (each fixture has a lockfile stub)
  </behavior>
  <action>
    Port logic from `packages/create-uploadkit-app/src/pm.ts` but make
    it tree-shakeable — export a single pure function.
    Augment each fixture in `__fixtures__/projects/` with an empty
    lockfile matching the PM you want to assert.
  </action>
  <verify>
    <automated>pnpm --filter uploadkit-cli test -- detect</automated>
  </verify>
  <done>Detection result carries the correct packageManager per fixture.</done>
</task>

</tasks>

<verification>
- `pnpm --filter uploadkit-cli test` passes with 100% coverage on detect/*.ts
- No network or spawn calls inside detectors (pure fs+env reads)
</verification>

<success_criteria>
Detectors ship as pure functions with typed output, exhaustively tested
against fixture projects. Downstream plans consume
`DetectionResult` without any additional logic.
</success_criteria>

<output>
Create `12.5-02-SUMMARY.md` listing fixtures, detection rules, and any
edge cases (e.g. monorepo detection bail-out behavior).
</output>
