---
phase: 01-monorepo-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - turbo.json
  - .env.example
  - .gitignore
  - .npmrc
  - packages/config/eslint/index.js
  - packages/config/typescript/tsconfig.base.json
  - packages/config/typescript/tsconfig.nextjs.json
  - packages/config/typescript/tsconfig.library.json
  - packages/config/tailwind/base.css
  - packages/config/package.json
  - apps/api/package.json
  - apps/api/next.config.ts
  - apps/api/tsconfig.json
  - apps/api/src/app/layout.tsx
  - apps/api/src/app/page.tsx
  - apps/api/src/app/globals.css
  - apps/dashboard/package.json
  - apps/dashboard/next.config.ts
  - apps/dashboard/tsconfig.json
  - apps/dashboard/src/app/layout.tsx
  - apps/dashboard/src/app/page.tsx
  - apps/dashboard/src/app/globals.css
  - apps/web/package.json
  - apps/web/next.config.ts
  - apps/web/tsconfig.json
  - apps/web/src/app/layout.tsx
  - apps/web/src/app/page.tsx
  - apps/web/src/app/globals.css
  - apps/docs/package.json
  - apps/docs/next.config.ts
  - apps/docs/tsconfig.json
  - apps/docs/src/app/layout.tsx
  - apps/docs/src/app/page.tsx
  - packages/core/package.json
  - packages/core/tsup.config.ts
  - packages/core/src/index.ts
  - packages/core/tsconfig.json
  - packages/react/package.json
  - packages/react/tsup.config.ts
  - packages/react/src/index.ts
  - packages/react/tsconfig.json
  - packages/next/package.json
  - packages/next/tsup.config.ts
  - packages/next/src/index.ts
  - packages/next/tsconfig.json
  - packages/ui/package.json
  - packages/ui/tsconfig.json
  - packages/ui/src/index.ts
  - vitest.config.ts
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
user_setup: []

must_haves:
  truths:
    - "Running `pnpm install` from repo root succeeds with zero errors"
    - "Running `pnpm build` from repo root builds all packages and apps in correct dependency order"
    - "All apps and packages can import from @uploadkit/config shared configs without errors"
    - "A single .env.example documents every env var needed for local dev"
  artifacts:
    - path: "package.json"
      provides: "Root workspace package with turbo/typescript/changesets devDependencies"
      contains: "turbo"
    - path: "pnpm-workspace.yaml"
      provides: "Workspace package declarations"
      contains: "apps/*"
    - path: "turbo.json"
      provides: "Task pipeline with dependency ordering"
      contains: "dependsOn"
    - path: "packages/config/typescript/tsconfig.base.json"
      provides: "Shared strict TypeScript config"
      contains: "strict"
    - path: "packages/config/tailwind/base.css"
      provides: "Shared Tailwind v4 theme tokens"
      contains: "@theme"
  key_links:
    - from: "apps/dashboard/tsconfig.json"
      to: "packages/config/typescript/tsconfig.nextjs.json"
      via: "extends"
      pattern: "extends.*config/typescript"
    - from: "apps/dashboard/src/app/globals.css"
      to: "packages/config/tailwind/base.css"
      via: "@import"
      pattern: "@import.*config/tailwind"
---

<objective>
Scaffold the Turborepo + pnpm monorepo skeleton with all 4 apps and 7 packages, shared configs (ESLint, TypeScript, Tailwind v4), and the root .env.example.

Purpose: Every subsequent plan and phase depends on this foundation existing. No code can be written until the monorepo structure, workspace references, and build pipeline are in place.
Output: Working monorepo where `pnpm install && pnpm build` succeeds, all workspace packages are linked, and shared configs are consumed by all apps.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-monorepo-infrastructure/01-CONTEXT.md
@.planning/phases/01-monorepo-infrastructure/01-RESEARCH.md
@UPLOADKIT-GSD.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create monorepo root, workspace config, shared configs package, and .env.example</name>
  <files>
    package.json, pnpm-workspace.yaml, turbo.json, .env.example, .gitignore, .npmrc,
    packages/config/package.json,
    packages/config/eslint/index.js,
    packages/config/typescript/tsconfig.base.json,
    packages/config/typescript/tsconfig.nextjs.json,
    packages/config/typescript/tsconfig.library.json,
    packages/config/tailwind/base.css,
    vitest.config.ts
  </files>
  <read_first>
    UPLOADKIT-GSD.md
    .planning/phases/01-monorepo-infrastructure/01-RESEARCH.md
    .planning/phases/01-monorepo-infrastructure/01-CONTEXT.md
  </read_first>
  <action>
Create the root `package.json` with:
- `"name": "uploadkit"`, `"private": true`
- `"packageManager": "pnpm@10.26.2"`
- devDependencies: `turbo`, `typescript@latest`, `@changesets/cli`, `prettier`, `vitest`, `@vitest/coverage-v8`
- scripts: `"build": "turbo build"`, `"dev": "turbo dev"`, `"lint": "turbo lint"`, `"typecheck": "turbo typecheck"`, `"test": "turbo test"`, `"seed": "tsx scripts/seed.ts"`, `"format": "prettier --write ."`

Create `pnpm-workspace.yaml`:
```yaml
packages:
  - "apps/*"
  - "packages/*"
```

Create `.npmrc`:
```
auto-install-peers=true
strict-peer-dependencies=false
```

Create `turbo.json` with the exact pattern from 01-RESEARCH.md Pattern 1:
- `build`: `dependsOn: ["^build"]`, outputs: `["dist/**", ".next/**", "!.next/cache/**"]`
- `dev`: `dependsOn: ["^build"]`, persistent: true, cache: false
- `lint`: `dependsOn: ["^build"]`
- `typecheck`: `dependsOn: ["^build"]`
- `test`: `dependsOn: ["^build"]`, outputs: `["coverage/**"]`
- `globalEnv`: `["MONGODB_URI", "UPSTASH_REDIS_REST_URL", "UPSTASH_REDIS_REST_TOKEN", "R2_ACCOUNT_ID", "R2_ACCESS_KEY_ID", "R2_SECRET_ACCESS_KEY", "R2_BUCKET_NAME", "SENTRY_DSN", "NODE_ENV", "NEXTAUTH_SECRET", "NEXTAUTH_URL", "GITHUB_CLIENT_ID", "GITHUB_CLIENT_SECRET", "GOOGLE_CLIENT_ID", "GOOGLE_CLIENT_SECRET", "STRIPE_SECRET_KEY", "STRIPE_WEBHOOK_SECRET", "RESEND_API_KEY"]`

Create `.env.example` (per D-02, single root .env — all apps read from here):
```
# Database
MONGODB_URI=mongodb://root:password@localhost:27017/uploadkit?authSource=admin

# Upstash Redis (rate limiting)
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Cloudflare R2 (storage)
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_NAME=uploadkit-dev

# CDN (production only)
CDN_URL=https://cdn.uploadkit.dev

# Auth.js v5
NEXTAUTH_SECRET=generate-a-random-secret-here
NEXTAUTH_URL=http://localhost:3000
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Stripe
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# Resend (email)
RESEND_API_KEY=

# Sentry
SENTRY_DSN=

# App URLs
NEXT_PUBLIC_APP_URL=http://localhost:3001
NEXT_PUBLIC_API_URL=http://localhost:3002
NEXT_PUBLIC_DOCS_URL=http://localhost:3003
```

Create `.gitignore` with: `node_modules`, `dist`, `.next`, `.turbo`, `coverage`, `.env`, `.env.local`, `*.tsbuildinfo`

Create `packages/config/package.json`:
```json
{
  "name": "@uploadkit/config",
  "version": "0.1.0",
  "private": true,
  "exports": {
    "./eslint": "./eslint/index.js",
    "./typescript/base": "./typescript/tsconfig.base.json",
    "./typescript/nextjs": "./typescript/tsconfig.nextjs.json",
    "./typescript/library": "./typescript/tsconfig.library.json",
    "./tailwind/base.css": "./tailwind/base.css"
  }
}
```

Create `packages/config/eslint/index.js` — ESLint 9 flat config exporting an array:
```javascript
import js from '@eslint/js';
import tseslint from 'typescript-eslint';

export default [
  js.configs.recommended,
  ...tseslint.configs.recommended,
  {
    rules: {
      '@typescript-eslint/no-unused-vars': ['error', { argsIgnorePattern: '^_' }],
      '@typescript-eslint/no-explicit-any': 'warn',
    },
  },
  { ignores: ['dist/', '.next/', 'node_modules/', 'coverage/'] },
];
```

Create `packages/config/typescript/tsconfig.base.json` — exact content from 01-RESEARCH.md Pattern 3 (target ES2022, strict true, exactOptionalPropertyTypes true, moduleResolution bundler, etc.).

Create `packages/config/typescript/tsconfig.nextjs.json` — extends base, adds DOM libs, jsx preserve, incremental true, next plugin.

Create `packages/config/typescript/tsconfig.library.json` — extends base, adds `outDir: "./dist"`, `rootDir: "./src"`, `declaration: true`.

Create `packages/config/tailwind/base.css` — exact content from 01-RESEARCH.md Pattern 5:
```css
@import "tailwindcss";

@theme {
  --color-surface: #0a0a0b;
  --color-surface-elevated: #141416;
  --color-text-primary: #fafafa;
  --color-text-secondary: #a1a1aa;
  --color-accent: #6366f1;
  --radius-sm: 6px;
  --radius-md: 12px;
  --radius-lg: 20px;
  --font-sans: "Geist", system-ui, sans-serif;
  --font-mono: "Geist Mono", monospace;
}
```

Create root `vitest.config.ts`:
```typescript
import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    globals: true,
    environment: 'node',
    include: ['packages/*/tests/**/*.test.ts', 'packages/*/src/**/*.test.ts'],
  },
});
```
  </action>
  <verify>
    <automated>cd /Users/drumstock/Developer/GitHub/uploadkit && cat package.json | grep turbo && cat turbo.json | grep dependsOn && cat pnpm-workspace.yaml | grep "apps/*" && cat packages/config/typescript/tsconfig.base.json | grep strict && cat packages/config/tailwind/base.css | grep "@theme" && cat .env.example | grep MONGODB_URI</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `"turbo"` in devDependencies
    - package.json contains `"seed": "tsx scripts/seed.ts"`
    - turbo.json contains `"dependsOn": ["^build"]` for the build task
    - turbo.json contains `"globalEnv"` array with `MONGODB_URI`
    - pnpm-workspace.yaml contains `"apps/*"` and `"packages/*"`
    - packages/config/typescript/tsconfig.base.json contains `"strict": true` and `"exactOptionalPropertyTypes": true`
    - packages/config/typescript/tsconfig.nextjs.json contains `"extends": "./tsconfig.base.json"`
    - packages/config/tailwind/base.css contains `@import "tailwindcss"` and `@theme`
    - .env.example contains `MONGODB_URI=mongodb://root:password@localhost:27017/uploadkit?authSource=admin`
    - .gitignore contains `node_modules`, `.next`, `.turbo`, `.env`
  </acceptance_criteria>
  <done>Root monorepo files exist with correct Turborepo pipeline, pnpm workspace, shared configs (ESLint flat config, TypeScript strict base/nextjs/library, Tailwind v4 CSS-first theme), and .env.example documenting all environment variables.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold all 4 apps and 4 SDK/UI package skeletons with correct workspace references</name>
  <files>
    apps/api/package.json, apps/api/next.config.ts, apps/api/tsconfig.json, apps/api/src/app/layout.tsx, apps/api/src/app/page.tsx, apps/api/src/app/globals.css,
    apps/dashboard/package.json, apps/dashboard/next.config.ts, apps/dashboard/tsconfig.json, apps/dashboard/src/app/layout.tsx, apps/dashboard/src/app/page.tsx, apps/dashboard/src/app/globals.css,
    apps/web/package.json, apps/web/next.config.ts, apps/web/tsconfig.json, apps/web/src/app/layout.tsx, apps/web/src/app/page.tsx, apps/web/src/app/globals.css,
    apps/docs/package.json, apps/docs/next.config.ts, apps/docs/tsconfig.json, apps/docs/src/app/layout.tsx, apps/docs/src/app/page.tsx,
    packages/core/package.json, packages/core/tsup.config.ts, packages/core/src/index.ts, packages/core/tsconfig.json,
    packages/react/package.json, packages/react/tsup.config.ts, packages/react/src/index.ts, packages/react/tsconfig.json,
    packages/next/package.json, packages/next/tsup.config.ts, packages/next/src/index.ts, packages/next/tsconfig.json,
    packages/ui/package.json, packages/ui/tsconfig.json, packages/ui/src/index.ts
  </files>
  <read_first>
    .planning/phases/01-monorepo-infrastructure/01-RESEARCH.md
    .planning/phases/01-monorepo-infrastructure/01-CONTEXT.md
    UPLOADKIT-GSD.md
    packages/config/package.json
    packages/config/typescript/tsconfig.base.json
    packages/config/typescript/tsconfig.nextjs.json
    packages/config/typescript/tsconfig.library.json
  </read_first>
  <action>
Note on file count: This task touches ~37 files but all are boilerplate stubs (package.json, tsconfig.json, minimal layout/page files). There is no business logic to unit-test. The authoritative verification is `pnpm install && pnpm build` — TypeScript compilation fails immediately if any tsconfig `extends`, workspace reference, or import path is wrong, making the build the complete catch-all for wiring issues across all files.

**Apps (all Next.js 16 App Router):**

For each app (api, dashboard, web, docs), create:

`apps/{name}/package.json`:
- name: `@uploadkit/{name}`
- private: true
- dependencies: `next@latest`, `react@latest`, `react-dom@latest`
- devDependencies: `@types/react`, `@types/react-dom`, `typescript@latest`, `tailwindcss@latest`, `@tailwindcss/postcss`
- For dashboard and web: add `@uploadkit/ui: "workspace:*"`, `@uploadkit/shared: "workspace:*"`
- For api: add `@uploadkit/db: "workspace:*"`, `@uploadkit/shared: "workspace:*"`
- For docs: add `fumadocs-ui`, `fumadocs-mdx`, `fumadocs-core`
- scripts: `"dev": "next dev --turbopack"`, `"build": "next build"`, `"start": "next start"`, `"lint": "next lint"`
- Each app gets a different port: web=3000, dashboard=3001, api=3002, docs=3003

`apps/{name}/next.config.ts`:
```typescript
import type { NextConfig } from 'next';
const nextConfig: NextConfig = {
  output: 'standalone',
  transpilePackages: ['@uploadkit/shared', '@uploadkit/ui', '@uploadkit/db'],
};
export default nextConfig;
```
(Per D-10/D-13: output standalone for Docker deployment)

`apps/{name}/tsconfig.json`:
```json
{
  "extends": "@uploadkit/config/typescript/nextjs",
  "compilerOptions": {
    "paths": { "@/*": ["./src/*"] }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx"],
  "exclude": ["node_modules"]
}
```

`apps/{name}/src/app/layout.tsx` — minimal root layout with html/body tags.
`apps/{name}/src/app/page.tsx` — minimal page returning `<h1>{App Name}</h1>`.
`apps/{name}/src/app/globals.css` — for api/dashboard/web: `@import "@uploadkit/config/tailwind/base.css";`. For docs: minimal CSS.

**SDK Package Skeletons:**

`packages/core/package.json` — exact content from 01-RESEARCH.md Pattern 2 (name `@uploadkit/core`, sideEffects false, exports with ESM+CJS+types, files: ["dist"], peerDependencies empty, devDependencies: tsup, typescript). Add `"scripts": { "build": "tsup", "dev": "tsup --watch" }`.

`packages/core/tsup.config.ts` — exact content from 01-RESEARCH.md (entry src/index.ts, format esm+cjs, dts true, clean true, external empty).

`packages/core/src/index.ts` — `export const VERSION = '0.1.0';`

`packages/core/tsconfig.json` — extends `@uploadkit/config/typescript/library`.

`packages/react/package.json` — name `@uploadkit/react`, sideEffects false, same exports pattern, peerDependencies: `react >= 18`, `react-dom >= 18`, dependencies: `@uploadkit/core: "workspace:*"`. devDeps: tsup, typescript.

`packages/react/tsup.config.ts` — format esm+cjs, dts, external: ['react', 'react-dom', '@uploadkit/core'], splitting: true.

`packages/react/src/index.ts` — `export const VERSION = '0.1.0';`

`packages/next/package.json` — name `@uploadkit/next`, sideEffects false, peerDependencies: `next >= 14`, dependencies: `@uploadkit/core: "workspace:*"`. devDeps: tsup, typescript.

`packages/next/tsup.config.ts` — format esm+cjs, dts, external: ['next', 'react', '@uploadkit/core'].

`packages/next/src/index.ts` — `export const VERSION = '0.1.0';`

`packages/ui/package.json` — name `@uploadkit/ui`, private: true, exports `"./": "./src/index.ts"`, dependencies: `@uploadkit/config: "workspace:*"`. devDeps: typescript.

`packages/ui/tsconfig.json` — extends library config.

`packages/ui/src/index.ts` — `export {};` (placeholder per D-05, shadcn primitives added in later phases).

After creating all files, run `pnpm install` and `pnpm build` to verify the full pipeline works.
  </action>
  <verify>
    <!-- ~37 boilerplate files — pnpm build is the authoritative catch-all.
         TypeScript compilation fails immediately if any tsconfig extends, workspace
         reference, or import path is broken across any of the 37 files. -->
    <automated>cd /Users/drumstock/Developer/GitHub/uploadkit && pnpm install --frozen-lockfile 2>&1 | tail -5 && pnpm build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm install` exits with code 0
    - `pnpm build` exits with code 0 (all packages and apps build successfully)
    - apps/dashboard/next.config.ts contains `output: 'standalone'`
    - apps/api/tsconfig.json contains `"extends"` referencing `@uploadkit/config/typescript/nextjs`
    - packages/core/package.json contains `"sideEffects": false`
    - packages/core/package.json contains `"@uploadkit/core"` as name
    - packages/react/package.json contains `"react"` in peerDependencies
    - packages/react/tsup.config.ts contains `external` array with `react`
    - packages/ui/package.json contains `"private": true`
    - apps/dashboard/src/app/globals.css contains `@import "@uploadkit/config/tailwind/base.css"`
  </acceptance_criteria>
  <done>All 4 Next.js apps (web, dashboard, api, docs) and 4 package skeletons (core, react, next, ui) are scaffolded with correct workspace references, tsconfig extends, Tailwind imports, and tsup build configs. `pnpm install && pnpm build` passes — the build is the authoritative wiring check covering all scaffolded files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env file | Contains secrets; must not be committed to git |
| npm registry | Published packages must not include src/ or secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | .env | mitigate | .gitignore includes `.env` and `.env.local`; .env.example has no real secrets |
| T-01-02 | Information Disclosure | npm publish | mitigate | All SDK packages have `"files": ["dist"]` to exclude src and tests from published tarball |
| T-01-03 | Tampering | pnpm lockfile | mitigate | CI uses `--frozen-lockfile` to prevent lockfile tampering |
</threat_model>

<verification>
- `pnpm install` exits 0 from repo root
- `pnpm build` exits 0 (all packages then apps build in order)
- `packages/config/typescript/tsconfig.base.json` has `strict: true`
- `.env.example` documents every required env var
- All app tsconfigs extend from `@uploadkit/config/typescript/nextjs`
- SDK package.json files have correct `exports`, `sideEffects`, `files` fields
</verification>

<success_criteria>
Running `pnpm install && pnpm build` from a clean clone succeeds. The monorepo has 4 apps and 7 packages all correctly wired through workspace protocol. Shared TypeScript, ESLint, and Tailwind configs are consumed by all apps.
</success_criteria>

<output>
After completion, create `.planning/phases/01-monorepo-infrastructure/01-01-SUMMARY.md`
</output>
