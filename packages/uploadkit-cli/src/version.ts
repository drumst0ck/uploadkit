// The CLI version is injected here at publish time by a later plan.
// For now we hard-code a value that matches package.json; a future plan may
// replace this with a generated file written by `prepublishOnly`.
export const VERSION = '0.0.0';
