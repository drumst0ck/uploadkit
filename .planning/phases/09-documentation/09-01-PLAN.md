---
phase: 09-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/docs/source.config.ts
  - apps/docs/next.config.ts
  - apps/docs/src/app/layout.tsx
  - apps/docs/src/app/global.css
  - apps/docs/src/app/docs/layout.tsx
  - apps/docs/src/app/docs/[[...slug]]/page.tsx
  - apps/docs/src/app/api/search/route.ts
  - apps/docs/src/lib/source.ts
  - apps/docs/src/lib/layout.shared.tsx
  - apps/docs/src/components/mdx.tsx
  - apps/docs/src/app/page.tsx
  - apps/docs/package.json
  - apps/docs/tsconfig.json
  - apps/docs/content/docs/index.mdx
  - apps/docs/content/docs/meta.json
  - apps/docs/content/docs/getting-started/meta.json
  - apps/docs/content/docs/core-concepts/meta.json
  - apps/docs/content/docs/sdk/meta.json
  - apps/docs/content/docs/sdk/core/meta.json
  - apps/docs/content/docs/sdk/react/meta.json
  - apps/docs/content/docs/sdk/next/meta.json
  - apps/docs/content/docs/api-reference/meta.json
  - apps/docs/content/docs/guides/meta.json
  - apps/docs/content/docs/dashboard/meta.json
autonomous: true
requirements:
  - DOCS-01
must_haves:
  truths:
    - "Fumadocs site runs on localhost:3003 with styled layout, sidebar, and search"
    - "Sidebar shows 2-level hierarchy matching GSD 6.2 structure"
    - "MDX content renders with Tabs, code blocks, callouts, and cards"
    - "Search API returns results for content queries"
  artifacts:
    - path: "apps/docs/source.config.ts"
      provides: "Fumadocs MDX collection definition"
      contains: "defineDocs"
    - path: "apps/docs/src/lib/source.ts"
      provides: "Content source loader"
      contains: "loader"
    - path: "apps/docs/src/app/docs/[[...slug]]/page.tsx"
      provides: "Dynamic docs page route"
      contains: "DocsPage"
    - path: "apps/docs/src/app/api/search/route.ts"
      provides: "Built-in search endpoint"
      contains: "createFromSource"
  key_links:
    - from: "apps/docs/source.config.ts"
      to: "content/docs/"
      via: "defineDocs dir"
      pattern: "dir.*content/docs"
    - from: "apps/docs/src/app/docs/layout.tsx"
      to: "apps/docs/src/lib/source.ts"
      via: "source.getPageTree()"
      pattern: "source\\.getPageTree"
---

<objective>
Bootstrap the Fumadocs documentation site with full MDX pipeline, sidebar navigation, search, and content skeleton.

Purpose: Establish the complete docs infrastructure so subsequent plans only need to write MDX content files.
Output: Working Fumadocs site at localhost:3003 with sidebar, search, theming, and all section meta.json files defining the 2-level hierarchy per D-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-documentation/09-CONTEXT.md
@apps/docs/package.json
@apps/docs/src/app/layout.tsx
@apps/docs/next.config.ts
@apps/docs/tsconfig.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fumadocs MDX pipeline and app shell</name>
  <files>
    apps/docs/source.config.ts,
    apps/docs/next.config.ts,
    apps/docs/tsconfig.json,
    apps/docs/package.json,
    apps/docs/src/lib/source.ts,
    apps/docs/src/lib/layout.shared.tsx,
    apps/docs/src/components/mdx.tsx,
    apps/docs/src/app/layout.tsx,
    apps/docs/src/app/global.css,
    apps/docs/src/app/page.tsx,
    apps/docs/src/app/docs/layout.tsx,
    apps/docs/src/app/docs/[[...slug]]/page.tsx,
    apps/docs/src/app/api/search/route.ts
  </files>
  <action>
    Set up Fumadocs MDX pipeline in the existing apps/docs skeleton.

    **package.json** — Add missing deps: `fumadocs-mdx` and `@types/mdx` (fumadocs-ui, fumadocs-core already present). Run `pnpm install` from monorepo root after.

    **source.config.ts** (new, project root of apps/docs):
    ```ts
    import { defineDocs, defineConfig } from 'fumadocs-mdx/config';
    export const docs = defineDocs({ dir: 'content/docs' });
    export default defineConfig();
    ```

    **next.config.ts** — Replace existing config with createMDX wrapper:
    ```ts
    import { createMDX } from 'fumadocs-mdx/next';
    const config = { reactStrictMode: true };
    const withMDX = createMDX({});
    export default withMDX(config);
    ```
    Remove the `output: 'standalone'` — docs site is static/ISR, not containerized.

    **tsconfig.json** — Add path alias: `"collections/*": ["./.source/*"]` in compilerOptions.paths.

    **src/lib/source.ts**:
    ```ts
    import { docs } from 'collections/server';
    import { loader } from 'fumadocs-core/source';
    export const source = loader({ baseUrl: '/docs', source: docs.toFumadocsSource() });
    ```

    **src/lib/layout.shared.tsx** — Shared layout options:
    ```tsx
    import type { BaseLayoutProps } from 'fumadocs-ui/layouts/shared';
    export function baseOptions(): BaseLayoutProps {
      return {
        nav: { title: 'UploadKit', url: '/' },
        githubUrl: 'https://github.com/uploadkit/uploadkit',
        links: [
          { text: 'Docs', url: '/docs', active: 'nested-url' },
        ],
      };
    }
    ```

    **src/components/mdx.tsx** — MDX component registry. Import `defaultMdxComponents` from `fumadocs-ui/mdx` and spread. Also import and spread `* as TabsComponents` from `fumadocs-ui/components/tabs` (per D-02 — framework-specific tabs everywhere). Export both `getMDXComponents` and `useMDXComponents`.

    **src/app/layout.tsx** — Wrap in `RootProvider` from `fumadocs-ui/provider/next`. Import global.css. Set `suppressHydrationWarning` on html tag.

    **src/app/global.css**:
    ```css
    @import 'tailwindcss';
    @import 'fumadocs-ui/css/neutral.css';
    @import 'fumadocs-ui/css/preset.css';
    ```

    **src/app/page.tsx** — Redirect `/` to `/docs` using `redirect` from `next/navigation`.

    **src/app/docs/layout.tsx** — Render `DocsLayout` with `tree={source.getPageTree()}` and `{...baseOptions()}`. Set `sidebar.defaultOpenLevel` to 1 per D-01 (2-level hierarchy visible).

    **src/app/docs/[[...slug]]/page.tsx** — Standard Fumadocs page: get page from `source.getPage(params.slug)`, render with `DocsPage`, `DocsTitle`, `DocsDescription`, `DocsBody`. Include `generateStaticParams` and `generateMetadata`. Use `getMDXComponents` with `createRelativeLink`.

    **src/app/api/search/route.ts** — Built-in search:
    ```ts
    import { source } from '@/lib/source';
    import { createFromSource } from 'fumadocs-core/search/server';
    export const { GET } = createFromSource(source, { language: 'english' });
    ```
  </action>
  <verify>
    <automated>cd /Users/drumstock/Developer/GitHub/uploadkit && pnpm install && pnpm --filter @uploadkit/docs build 2>&1 | tail -20</automated>
  </verify>
  <done>Fumadocs app builds without errors, source.config.ts, source.ts, layouts, and search route all in place</done>
</task>

<task type="auto">
  <name>Task 2: Content skeleton with sidebar hierarchy and index page</name>
  <files>
    apps/docs/content/docs/index.mdx,
    apps/docs/content/docs/meta.json,
    apps/docs/content/docs/getting-started/meta.json,
    apps/docs/content/docs/core-concepts/meta.json,
    apps/docs/content/docs/sdk/meta.json,
    apps/docs/content/docs/sdk/core/meta.json,
    apps/docs/content/docs/sdk/react/meta.json,
    apps/docs/content/docs/sdk/next/meta.json,
    apps/docs/content/docs/api-reference/meta.json,
    apps/docs/content/docs/guides/meta.json,
    apps/docs/content/docs/dashboard/meta.json
  </files>
  <action>
    Create the full content directory structure matching GSD 6.2 with meta.json files for 2-level sidebar navigation (per D-01).

    **content/docs/meta.json** — Root ordering:
    ```json
    {
      "pages": [
        "index",
        "---Getting Started---",
        "getting-started",
        "---Core Concepts---",
        "core-concepts",
        "---SDK---",
        "sdk",
        "---API Reference---",
        "api-reference",
        "---Dashboard---",
        "dashboard",
        "---Guides---",
        "guides"
      ]
    }
    ```

    **content/docs/getting-started/meta.json**:
    ```json
    {
      "title": "Getting Started",
      "pages": ["quickstart", "nextjs", "react", "api-only"]
    }
    ```

    **content/docs/core-concepts/meta.json**:
    ```json
    {
      "title": "Core Concepts",
      "pages": ["file-routes", "presigned-urls", "byos", "security"]
    }
    ```

    **content/docs/sdk/meta.json**:
    ```json
    {
      "title": "SDK",
      "pages": ["core", "react", "next"]
    }
    ```

    **content/docs/sdk/core/meta.json**:
    ```json
    {
      "title": "@uploadkit/core",
      "pages": ["installation", "configuration", "upload", "delete", "api-reference"]
    }
    ```

    **content/docs/sdk/react/meta.json**:
    ```json
    {
      "title": "@uploadkit/react",
      "pages": ["installation", "upload-button", "upload-dropzone", "upload-modal", "file-list", "file-preview", "use-uploadkit", "theming", "api-reference"]
    }
    ```

    **content/docs/sdk/next/meta.json**:
    ```json
    {
      "title": "@uploadkit/next",
      "pages": ["installation", "file-router", "middleware", "type-safety", "api-reference"]
    }
    ```

    **content/docs/api-reference/meta.json**:
    ```json
    {
      "title": "API Reference",
      "pages": ["rest-api", "authentication", "upload", "files", "projects", "webhooks", "errors"]
    }
    ```

    **content/docs/guides/meta.json**:
    ```json
    {
      "title": "Guides",
      "pages": ["image-upload", "avatar-upload", "document-upload", "multipart-upload", "custom-styling", "migration-from-uploadthing"]
    }
    ```

    **content/docs/dashboard/meta.json**:
    ```json
    {
      "title": "Dashboard",
      "pages": ["overview", "projects", "files", "api-keys", "billing"]
    }
    ```

    **content/docs/index.mdx** — The docs landing/introduction page. Write a welcoming introduction to UploadKit:
    - Title: "Introduction"
    - Description: "UploadKit is a file upload service for developers. Add beautiful, type-safe file uploads to any app in minutes."
    - Sections: What is UploadKit (brief), Key Features (managed storage, BYOS, premium components, type safety, direct uploads, dashboard), Quick Links (cards linking to Quickstart, SDK Reference, API Reference, Guides)
    - Use Fumadocs `Card` and `Cards` components from `fumadocs-ui/components/card` for the quick links section. Import them in the MDX file.
    - Keep tone professional but approachable — like Vercel/Supabase docs.
  </action>
  <verify>
    <automated>cd /Users/drumstock/Developer/GitHub/uploadkit && pnpm --filter @uploadkit/docs build 2>&1 | tail -20</automated>
  </verify>
  <done>All meta.json files exist defining 2-level sidebar per D-01, index.mdx renders as docs landing page, build succeeds</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Search API | Public search endpoint processes user query strings |
| MDX content | Static content, no user input at build time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | T (Tampering) | Search API route | accept | Static site search over pre-built index — no database, no secrets; query is read-only against static index |
| T-09-02 | D (DoS) | Search API route | accept | Fumadocs built-in search is lightweight in-memory; Vercel/hosting rate limits apply at infra level |
</threat_model>

<verification>
- `pnpm --filter @uploadkit/docs build` completes without errors
- `pnpm --filter @uploadkit/docs dev` shows docs site at localhost:3003 with sidebar navigation
- Sidebar displays all sections from GSD 6.2 in correct 2-level hierarchy
- Search input is visible in the navbar
</verification>

<success_criteria>
Fumadocs site builds and runs with complete sidebar structure, search endpoint, MDX pipeline with Tabs component, and a polished introduction page. All subsequent plans only write MDX content files.
</success_criteria>

<output>
After completion, create `.planning/phases/09-documentation/09-01-SUMMARY.md`
</output>
