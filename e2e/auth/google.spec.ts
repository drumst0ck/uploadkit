import { test } from '@playwright/test';

test.describe('Google OAuth (AUTH-02)', () => {
  test.skip('Google OAuth login creates user and redirects to /dashboard', async () => {
    // Requires live Google OAuth credentials. Run manually via Task 3 checkpoint.
  });
});
