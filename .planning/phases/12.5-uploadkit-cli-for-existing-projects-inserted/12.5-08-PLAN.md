---
phase: 12.5-uploadkit-cli-for-existing-projects-inserted
plan: 08
type: execute
wave: 4
depends_on: [12.5-04, 12.5-05, 12.5-06, 12.5-07]
files_modified:
  - packages/uploadkit-cli/src/__tests__/e2e/init-next-app.e2e.test.ts
  - packages/uploadkit-cli/src/__tests__/e2e/init-sveltekit.e2e.test.ts
  - packages/uploadkit-cli/src/__tests__/e2e/init-remix.e2e.test.ts
  - packages/uploadkit-cli/src/__tests__/e2e/init-vite-react.e2e.test.ts
  - packages/uploadkit-cli/src/__tests__/e2e/add-and-restore.e2e.test.ts
  - packages/uploadkit-cli/src/__tests__/helpers/scaffold.ts
  - packages/uploadkit-cli/src/__tests__/__snapshots__/** (auto-generated)
autonomous: true
requirements: [GROW-15, GROW-16, GROW-17]
must_haves:
  truths:
    - "End-to-end tests scaffold each framework fixture into tmpdir, run `init`, and snapshot the output"
    - "`add` + `restore` flow tested on the Next.js fixture"
    - "Idempotency verified for every framework (second run produces zero diffs)"
    - "Tests run without network via `--skip-install`"
  artifacts:
    - path: "packages/uploadkit-cli/src/__tests__/helpers/scaffold.ts"
      provides: "scaffold(fixtureName): Promise<{ root, cleanup }>"
    - path: "packages/uploadkit-cli/src/__tests__/e2e/*.e2e.test.ts"
      provides: "One e2e per framework + one for add/restore"
  key_links:
    - from: "packages/uploadkit-cli/src/__tests__/e2e/*.e2e.test.ts"
      to: "packages/uploadkit-cli/dist/index.js"
      via: "execa child process"
      pattern: "execa\\(|spawn\\("
---

<objective>
Fixture-driven end-to-end tests that exercise the compiled CLI binary
against tmpdir-copied projects for each supported framework plus the
add/restore flow. These tests become the CI smoke matrix payload in
plan 09.

Purpose: Prove the commands work end-to-end across frameworks and make
regressions visible before publish.
Output: 5 e2e test files + scaffold helper + committed snapshots.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12.5-uploadkit-cli-for-existing-projects-inserted/12.5-CONTEXT.md
@.planning/phases/12.5-uploadkit-cli-for-existing-projects-inserted/12.5-04-SUMMARY.md
@.planning/phases/12.5-uploadkit-cli-for-existing-projects-inserted/12.5-05-SUMMARY.md
@.planning/phases/12.5-uploadkit-cli-for-existing-projects-inserted/12.5-06-SUMMARY.md
@.planning/phases/12.5-uploadkit-cli-for-existing-projects-inserted/12.5-07-SUMMARY.md

<interfaces>
Helper signature:
```typescript
async function scaffold(fixtureName: "next-app" | "sveltekit" | "remix" | "vite-react") {
  const root = await fs.mkdtemp(path.join(os.tmpdir(), `uploadkit-e2e-${fixtureName}-`));
  await fs.cp(fixturesDir/fixtureName, root, { recursive: true });
  return { root, cleanup: () => fs.rm(root, { recursive: true, force: true }) };
}
```

E2e invocation:
```typescript
await execa("node", [pathToDistIndex, "init", "--yes", "--skip-install"], {
  cwd: root,
  env: { ...process.env, CI: "1" },
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold helper + init e2e per framework</name>
  <files>packages/uploadkit-cli/src/__tests__/helpers/scaffold.ts, packages/uploadkit-cli/src/__tests__/e2e/init-next-app.e2e.test.ts, packages/uploadkit-cli/src/__tests__/e2e/init-sveltekit.e2e.test.ts, packages/uploadkit-cli/src/__tests__/e2e/init-remix.e2e.test.ts, packages/uploadkit-cli/src/__tests__/e2e/init-vite-react.e2e.test.ts</files>
  <behavior>
    Each init-*.e2e.test.ts:
    1. Builds the CLI (assumes `pnpm build` already ran — vitest
       `globalSetup` runs `tsup` once)
    2. scaffold() the fixture
    3. Run `node dist/index.js init --yes --skip-install` in root
    4. Assert expected files exist (route handler path per D-05)
    5. Assert `.env` contains the expected env key
    6. Snapshot mutated files (e.g. app/layout.tsx) with Vitest's inline
       snapshot where possible; otherwise file snapshots
    7. Run init a second time; assert stdout contains "already
       configured" and mutated files' mtimes unchanged
    8. cleanup()

    Tests can mark tag "e2e" via `test.concurrent.skip.if(!process.env.RUN_E2E)`
    gate if needed; default is to run.
  </behavior>
  <action>
    Add a `vitest.config.ts` globalSetup that runs `pnpm build` once
    before the suite. Use `describe.sequential` for e2e files to avoid
    tmpdir pressure. Time-budget each test to &lt;30s.
  </action>
  <verify>
    <automated>pnpm --filter uploadkit-cli build &amp;&amp; pnpm --filter uploadkit-cli test -- e2e/init</automated>
  </verify>
  <done>All 4 framework e2e tests green; snapshots committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add + restore e2e</name>
  <files>packages/uploadkit-cli/src/__tests__/e2e/add-and-restore.e2e.test.ts</files>
  <behavior>
    1. scaffold("next-app"); run `init --yes --skip-install`
    2. Run `add dropzone --target app/page.tsx --yes` (assume fixture
       has app/page.tsx; if not, create one in the fixture)
    3. Assert `app/page.tsx` has `UploadDropzone` import + usage bounded
       by markers
    4. Run `restore --yes` (picks most recent session if only one)
    5. Assert `app/page.tsx` returns to pre-add contents
    6. Run `restore --yes` again (next session — the init one)
    7. Assert route handler deleted, layout restored, env cleaned
  </behavior>
  <action>
    Seed the next-app fixture with an `app/page.tsx` stub. Keep the test
    deterministic by selecting the session via `--timestamp` flag
    captured from backup dir listing (don't rely on prompt picker in
    non-interactive mode).
  </action>
  <verify>
    <automated>pnpm --filter uploadkit-cli test -- e2e/add-and-restore</automated>
  </verify>
  <done>Full init → add → restore → restore roundtrip green.</done>
</task>

</tasks>

<verification>
- `pnpm --filter uploadkit-cli test -- e2e` all green
- All 5 e2e tests complete in &lt; 60s wall clock locally
</verification>

<success_criteria>
End-to-end coverage proves GROW-15, 16, 17 on every supported framework.
Snapshots catch future regressions. Zero network calls required.
</success_criteria>

<output>
Create `12.5-08-SUMMARY.md` with test runtime numbers, any flaky-test
mitigations, and the list of snapshot files committed.
</output>
