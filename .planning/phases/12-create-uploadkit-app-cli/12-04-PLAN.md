---
phase: 12-create-uploadkit-app-cli
plan: 04
type: execute
wave: 3
depends_on: [12-03]
files_modified:
  - packages/create-uploadkit-app/templates/next/package.json
  - packages/create-uploadkit-app/templates/next/tsconfig.json
  - packages/create-uploadkit-app/templates/next/next.config.ts
  - packages/create-uploadkit-app/templates/next/postcss.config.mjs
  - packages/create-uploadkit-app/templates/next/app/layout.tsx
  - packages/create-uploadkit-app/templates/next/app/page.tsx
  - packages/create-uploadkit-app/templates/next/app/globals.css
  - packages/create-uploadkit-app/templates/next/app/api/uploadkit/route.ts
  - packages/create-uploadkit-app/templates/next/README.md
  - packages/create-uploadkit-app/templates/next/_gitignore
  - packages/create-uploadkit-app/templates/next/_env.local
autonomous: true
requirements: [GROW-02]
complexity: M

must_haves:
  truths:
    - "User runs `pnpm create uploadkit-app demo --template next` then `cd demo && pnpm dev` and sees an upload UI at http://localhost:3000"
    - "Dropzone uploads use UPLOADKIT_API_KEY from .env.local"
    - "README tells the user exactly 3 things: run dev, get a real API key, where to put it"
  artifacts:
    - path: packages/create-uploadkit-app/templates/next/app/page.tsx
      provides: "Home page with UploadDropzone from @uploadkitdev/react"
      contains: "UploadDropzone"
    - path: packages/create-uploadkit-app/templates/next/app/api/uploadkit/route.ts
      provides: "Route handler from @uploadkitdev/next"
      contains: "createRouteHandler"
    - path: packages/create-uploadkit-app/templates/next/package.json
      provides: "deps on next, react, @uploadkitdev/react, @uploadkitdev/next, tailwindcss v4"
      contains: "@uploadkitdev/next"
  key_links:
    - from: app/page.tsx
      to: app/api/uploadkit/route.ts
      via: "UploadKitProvider endpoint prop → /api/uploadkit"
      pattern: "/api/uploadkit"
---

<objective>
Ship the `next` template: Next.js 16 App Router + Tailwind v4 + the
UploadKit React SDK + the UploadKit Next.js route handler. One page
renders a working dropzone against the placeholder API key.

Purpose: Next.js is the flagship template — if only one works, this one
must work.

Output: `packages/create-uploadkit-app/templates/next/` ready to be copied
by the engine.
</objective>

<context>
@CLAUDE.md
@.planning/phases/12-create-uploadkit-app-cli/CONTEXT.md
@packages/react/src/index.ts
@packages/next/src/index.ts
@apps/web/package.json
@apps/web/next.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Next.js project files, Tailwind v4, layout</name>
  <files>
    packages/create-uploadkit-app/templates/next/package.json,
    packages/create-uploadkit-app/templates/next/tsconfig.json,
    packages/create-uploadkit-app/templates/next/next.config.ts,
    packages/create-uploadkit-app/templates/next/postcss.config.mjs,
    packages/create-uploadkit-app/templates/next/app/layout.tsx,
    packages/create-uploadkit-app/templates/next/app/globals.css,
    packages/create-uploadkit-app/templates/next/_gitignore,
    packages/create-uploadkit-app/templates/next/_env.local,
    packages/create-uploadkit-app/templates/next/README.md
  </files>
  <action>
    Mirror `apps/web` for Next/TS/Tailwind conventions. `package.json`:
    - `"name": "{{name}}"`
    - scripts: `dev`, `build`, `start`, `lint`
    - deps at `workspace:*` for `@uploadkitdev/react` + `@uploadkitdev/next`
      (rewritten to `^latest` at publish time per D-06). All other deps
      (`next`, `react`, `react-dom`, `tailwindcss`, `@tailwindcss/postcss`,
      `typescript`) declared without explicit versions — the publish step
      resolves them. For local dev (template loaded from workspace), use
      explicit `"latest"` strings; the prepublish script rewrites to
      pinned `^x.y.z`.

    `globals.css` uses Tailwind v4 syntax (`@import "tailwindcss"`).

    `_env.local`:
    ```
    # UploadKit — create your project at https://uploadkit.dev/signup
    # Then replace this value with the key from your dashboard.
    UPLOADKIT_API_KEY=uk_test_placeholder
    ```

    `_gitignore` — standard Next.js + node_modules + .env.local.

    README mentions:
    1. `{{pkgManager}} dev`
    2. Open http://localhost:3000
    3. Replace `UPLOADKIT_API_KEY` in `.env.local`
    4. Link to https://docs.uploadkit.dev/nextjs

    Reference D-07 (single demo page, no auth/DB).
  </action>
  <verify>
    <automated>ls packages/create-uploadkit-app/templates/next/ &amp;&amp; node -e "require('./packages/create-uploadkit-app/templates/next/package.json')"</automated>
  </verify>
  <done>Template project files exist and package.json is valid JSON.</done>
</task>

<task type="auto">
  <name>Task 2: Dropzone page + route handler + end-to-end smoke</name>
  <files>
    packages/create-uploadkit-app/templates/next/app/page.tsx,
    packages/create-uploadkit-app/templates/next/app/api/uploadkit/route.ts,
    packages/create-uploadkit-app/src/__tests__/template-next.test.ts
  </files>
  <action>
    `app/api/uploadkit/route.ts` — uses the route-handler helper from
    `@uploadkitdev/next` (inspect `packages/next/src/index.ts` for the
    actual export name; fall back to a thin wrapper that calls the SDK
    server helpers if no handler factory exists).

    `app/page.tsx` (Client Component — wrap with `UploadKitProvider`):
    ```tsx
    'use client';
    import { UploadKitProvider, UploadDropzone } from '@uploadkitdev/react';

    export default function Home() {
      return (
        <UploadKitProvider endpoint="/api/uploadkit">
          <main className="min-h-screen grid place-items-center p-8">
            <div className="w-full max-w-lg space-y-4">
              <h1 className="text-2xl font-semibold tracking-tight">UploadKit demo</h1>
              <p className="text-sm text-zinc-500">Drop a file below to test your setup.</p>
              <UploadDropzone onComplete={(files) => console.log('uploaded', files)} />
            </div>
          </main>
        </UploadKitProvider>
      );
    }
    ```

    Adjust import shape to match the real exported API — confirm from
    `packages/react/src/index.ts`.

    Smoke test (`template-next.test.ts`): runs the engine against this
    template into a tmpdir, asserts expected files exist, and reads
    `package.json` to confirm deps include `@uploadkitdev/react`,
    `@uploadkitdev/next`, `next`, `react`.
  </action>
  <verify>
    <automated>pnpm --filter create-uploadkit-app test -- template-next</automated>
  </verify>
  <done>
    - Smoke test green
    - Manual: scaffolding the template locally and running `pnpm install &amp;&amp; pnpm build` succeeds
    - The dropzone renders in `pnpm dev`
  </done>
</task>

</tasks>

<verification>
- `pnpm --filter create-uploadkit-app test` green
- Manual scaffold + `pnpm build` works
</verification>

<success_criteria>
Next.js template produces a runnable app with a visible upload UI out of the box.
</success_criteria>

<output>
Write `12-04-SUMMARY.md` noting the exact `@uploadkitdev/next` export used and any compatibility shims needed.
</output>
