---
phase: 12-create-uploadkit-app-cli
plan: 06
type: execute
wave: 3
depends_on: [12-03]
files_modified:
  - packages/create-uploadkit-app/templates/remix/package.json
  - packages/create-uploadkit-app/templates/remix/react-router.config.ts
  - packages/create-uploadkit-app/templates/remix/vite.config.ts
  - packages/create-uploadkit-app/templates/remix/tsconfig.json
  - packages/create-uploadkit-app/templates/remix/app/root.tsx
  - packages/create-uploadkit-app/templates/remix/app/routes/_index.tsx
  - packages/create-uploadkit-app/templates/remix/app/routes/api.sign.ts
  - packages/create-uploadkit-app/templates/remix/app/entry.client.tsx
  - packages/create-uploadkit-app/templates/remix/app/entry.server.tsx
  - packages/create-uploadkit-app/templates/remix/README.md
  - packages/create-uploadkit-app/templates/remix/_gitignore
  - packages/create-uploadkit-app/templates/remix/_env.local
autonomous: true
requirements: [GROW-02]
complexity: M

must_haves:
  truths:
    - "Scaffolded project runs `pnpm dev` and serves an upload page at http://localhost:5173"
    - "Presigned URL is returned via a `loader` or `action` export from app/routes/api.sign.ts"
    - "UploadDropzone from @uploadkitdev/react renders and posts to /api/sign"
  artifacts:
    - path: packages/create-uploadkit-app/templates/remix/app/routes/_index.tsx
      provides: "Index route with UploadKitProvider + UploadDropzone"
      contains: "UploadDropzone"
    - path: packages/create-uploadkit-app/templates/remix/app/routes/api.sign.ts
      provides: "action() that returns presigned PUT URL JSON"
      contains: "export async function action"
  key_links:
    - from: app/routes/_index.tsx
      to: app/routes/api.sign.ts
      via: "UploadKitProvider endpoint='/api/sign'"
      pattern: "/api/sign"
---

<objective>
Ship the `remix` template using React Router v7 framework mode (the
successor to Remix).

Purpose: React Router v7 is where the Remix community has migrated; we
brand the CLI option as `remix` for SEO + familiarity, but ship RR v7
underneath.

Output: `packages/create-uploadkit-app/templates/remix/`.
</objective>

<context>
@CLAUDE.md
@.planning/phases/12-create-uploadkit-app-cli/CONTEXT.md
@packages/react/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: React Router v7 framework scaffold</name>
  <files>
    packages/create-uploadkit-app/templates/remix/package.json,
    packages/create-uploadkit-app/templates/remix/react-router.config.ts,
    packages/create-uploadkit-app/templates/remix/vite.config.ts,
    packages/create-uploadkit-app/templates/remix/tsconfig.json,
    packages/create-uploadkit-app/templates/remix/app/root.tsx,
    packages/create-uploadkit-app/templates/remix/app/entry.client.tsx,
    packages/create-uploadkit-app/templates/remix/app/entry.server.tsx,
    packages/create-uploadkit-app/templates/remix/README.md,
    packages/create-uploadkit-app/templates/remix/_gitignore,
    packages/create-uploadkit-app/templates/remix/_env.local
  </files>
  <action>
    Base on the canonical `react-router` create command output (v7, framework mode). `package.json`:
    - deps `latest`: `react-router`, `@react-router/node`, `@react-router/serve`, `react`, `react-dom`, `isbot`
    - `@uploadkitdev/react` as `workspace:*` / rewritten at publish time
    - `@aws-sdk/client-s3` + `@aws-sdk/s3-request-presigner` `latest`
    - devDeps `latest`: `vite`, `@react-router/dev`, `typescript`, `@types/react`, `@types/react-dom`

    Scripts: `dev` (react-router dev), `build` (react-router build), `start` (react-router-serve ./build/server/index.js), `typecheck`.

    README steps: install → dev → open localhost:5173 → replace API key.
  </action>
  <verify>
    <automated>node -e "require('./packages/create-uploadkit-app/templates/remix/package.json')"</automated>
  </verify>
  <done>Scaffold files present and valid JSON/TS.</done>
</task>

<task type="auto">
  <name>Task 2: Index route with Dropzone + sign action + smoke test</name>
  <files>
    packages/create-uploadkit-app/templates/remix/app/routes/_index.tsx,
    packages/create-uploadkit-app/templates/remix/app/routes/api.sign.ts,
    packages/create-uploadkit-app/src/__tests__/template-remix.test.ts
  </files>
  <action>
    `app/routes/api.sign.ts` — `action({ request })` returns
    `Response.json({ url, key, method: 'PUT' })` using
    `@aws-sdk/s3-request-presigner`. Same contract as 12-05.

    `app/routes/_index.tsx`:
    ```tsx
    import { UploadKitProvider, UploadDropzone } from '@uploadkitdev/react';
    export default function Index() {
      return (
        <UploadKitProvider endpoint="/api/sign">
          <main style={{ padding: 48, maxWidth: 640, margin: '0 auto' }}>
            <h1>UploadKit — React Router v7</h1>
            <UploadDropzone onComplete={(files) => console.log(files)} />
          </main>
        </UploadKitProvider>
      );
    }
    ```

    Smoke test asserts file tree + that `_index.tsx` imports from
    `@uploadkitdev/react` and that `api.sign.ts` exports an `action`.
  </action>
  <verify>
    <automated>pnpm --filter create-uploadkit-app test -- template-remix</automated>
  </verify>
  <done>Scaffolded project builds; page renders.</done>
</task>

</tasks>

<verification>
- Smoke test green
- Manual build passes
</verification>

<success_criteria>
Remix (RR v7) template scaffolds a working dropzone out of the box.
</success_criteria>

<output>
Write `12-06-SUMMARY.md` including the exact React Router version used and whether any module resolution quirks arose.
</output>
