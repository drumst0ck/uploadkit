---
phase: 12.5-uploadkit-cli-for-existing-projects-inserted
plan: 09
type: execute
wave: 5
depends_on: [12.5-08]
files_modified:
  - .github/workflows/uploadkit-cli-smoke.yml
  - packages/uploadkit-cli/scripts/smoke-matrix.ts
  - packages/uploadkit-cli/package.json
autonomous: true
requirements: [GROW-15]
must_haves:
  truths:
    - "CI runs a matrix (next-app, sveltekit, remix, vite-react) that scaffolds a real upstream starter, runs `uploadkit init`, then `pnpm typecheck` + `pnpm build` in the scaffolded project"
    - "Matrix runs on PR to master and on push to master"
    - "Job fails if any framework leg fails"
    - "Linux only (Windows deferred per D-10)"
  artifacts:
    - path: ".github/workflows/uploadkit-cli-smoke.yml"
      provides: "Matrix smoke workflow gating the CLI"
    - path: "packages/uploadkit-cli/scripts/smoke-matrix.ts"
      provides: "Script invoked by the workflow to scaffold + init + verify one framework"
  key_links:
    - from: ".github/workflows/uploadkit-cli-smoke.yml"
      to: "packages/uploadkit-cli/scripts/smoke-matrix.ts"
      via: "pnpm run smoke -- <framework>"
      pattern: "smoke-matrix"
---

<objective>
Ship a CI smoke matrix that gates the CLI before publish. For each
supported framework, the workflow scaffolds a real upstream starter
(using `create-next-app`, `sv create`, `create-remix`, and `create-vite`),
runs the built `uploadkit` CLI's `init` command against it, then
typechecks and builds the scaffolded project to confirm nothing broke.

Purpose: Reuse the Phase 12 smoke-test pattern (D-11) to ensure every
release actually works end-to-end against real starter templates.
Output: `.github/workflows/uploadkit-cli-smoke.yml` + driver script.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12.5-uploadkit-cli-for-existing-projects-inserted/12.5-CONTEXT.md
@.planning/phases/12.5-uploadkit-cli-for-existing-projects-inserted/12.5-08-SUMMARY.md

<interfaces>
Script contract:
```
pnpm run smoke -- <framework>
# framework: next-app | sveltekit | remix | vite-react
```

Script steps:
1. Scaffold a fresh project in tmpdir using the framework's official
   starter (npx + --yes + --skip-install if supported; otherwise
   allow install with pnpm)
2. Link-install the local `uploadkit` CLI (via `pnpm pack` + `npm i -g`
   in the workflow, OR use `node ../uploadkit-cli/dist/index.js`)
3. Run `uploadkit init --yes` (allow install this time — real PM)
4. cd into scaffolded project; `pnpm typecheck` (or tsc) + `pnpm build`
5. Exit 0 on success

Workflow:
- name: uploadkit-cli-smoke
- on: { pull_request: [master], push: [master] }
- jobs.smoke.strategy.matrix.framework: [next-app, sveltekit, remix, vite-react]
- runs-on: ubuntu-latest
- steps: checkout, pnpm/action-setup@v4, node setup 22, pnpm install,
  pnpm --filter uploadkit-cli build, pnpm --filter uploadkit-cli run
  smoke -- ${{ matrix.framework }}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Smoke driver script</name>
  <files>packages/uploadkit-cli/scripts/smoke-matrix.ts, packages/uploadkit-cli/package.json</files>
  <behavior>
    - Add `smoke` npm script: `tsx scripts/smoke-matrix.ts`
    - Script takes framework name from argv; switches on it to pick
      scaffold command
    - Uses execa to run each step; streams stdout so CI logs are useful
    - Uses a tmpdir under `runner.temp` on CI (via `os.tmpdir()`)
    - After `uploadkit init`, cd into scaffolded project and run `pnpm
      typecheck && pnpm build` (or the project's equivalent — for SvelteKit
      it's `pnpm check && pnpm build`)
    - Exit non-zero on any failure

    Add `tsx@latest` as devDep.
  </behavior>
  <action>
    Keep the scaffold commands using official templates. Pin the flag
    set per framework to make output deterministic (e.g.
    `create-next-app --app --typescript --no-tailwind --no-eslint
    --no-src-dir --import-alias "@/*" --use-pnpm --skip-install`).
    Resolve each framework's non-interactive flags BEFORE coding to
    avoid hangs.
  </action>
  <verify>
    <automated>pnpm --filter uploadkit-cli build &amp;&amp; pnpm --filter uploadkit-cli run smoke -- next-app</automated>
  </verify>
  <done>Local run of smoke next-app passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GitHub workflow</name>
  <files>.github/workflows/uploadkit-cli-smoke.yml</files>
  <behavior>
    - Matrix over 4 frameworks
    - Steps: checkout → pnpm setup → node 22 → pnpm install → build
      uploadkit-cli → run smoke for the matrix framework
    - Job name includes framework for PR status readability
    - Timeout 10 minutes per leg
    - No secrets required (smoke uses `UPLOADKIT_API_KEY=uk_test_placeholder`)
  </behavior>
  <action>
    Mirror the Phase 12 workflow file structure. If Phase 12 smoke
    workflow name is similar, use a DIFFERENT file name
    (`uploadkit-cli-smoke.yml`) to avoid collision.
  </action>
  <verify>
    <automated>test -f .github/workflows/uploadkit-cli-smoke.yml &amp;&amp; node -e "require('js-yaml').load(require('fs').readFileSync('.github/workflows/uploadkit-cli-smoke.yml','utf8'))"</automated>
  </verify>
  <done>YAML parses; workflow triggers on PR + push to master.</done>
</task>

</tasks>

<verification>
- Local `pnpm --filter uploadkit-cli run smoke -- next-app` passes
- Workflow YAML is valid
- After PR is opened, all 4 matrix legs green before merge
</verification>

<success_criteria>
CI gate is enforceable. Regressions in any framework break the PR.
Ready for publish prep in plan 10.
</success_criteria>

<output>
Create `12.5-09-SUMMARY.md` with the exact scaffold flags per framework
and a note on observed smoke runtime per leg.
</output>
