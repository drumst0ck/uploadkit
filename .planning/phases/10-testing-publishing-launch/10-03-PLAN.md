---
phase: 10-testing-publishing-launch
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - e2e/auth/github.spec.ts
  - e2e/auth/magic-link.spec.ts
  - e2e/upload/upload-flow.spec.ts
  - e2e/dashboard/crud.spec.ts
  - e2e/billing/checkout.spec.ts
  - e2e/docs/navigation.spec.ts
  - e2e/helpers/auth.ts
  - e2e/helpers/storage-state.ts
  - playwright.config.ts
autonomous: true
requirements:
  - INFRA-08

must_haves:
  truths:
    - "Playwright E2E tests exist for auth, upload, dashboard CRUD, billing, and docs navigation"
    - "E2E auth helper uses storage state to avoid re-authentication on every test"
    - "Tests are structured to run against a local dev environment"
  artifacts:
    - path: "e2e/auth/github.spec.ts"
      provides: "GitHub OAuth E2E test with mock/skip strategy"
      contains: "GitHub OAuth"
    - path: "e2e/upload/upload-flow.spec.ts"
      provides: "Upload flow E2E (file upload via dashboard)"
      contains: "upload"
    - path: "e2e/dashboard/crud.spec.ts"
      provides: "Dashboard CRUD E2E (project, files, API keys)"
      contains: "project"
    - path: "e2e/billing/checkout.spec.ts"
      provides: "Billing checkout E2E"
      contains: "billing"
    - path: "e2e/docs/navigation.spec.ts"
      provides: "Docs site navigation E2E"
      contains: "docs"
  key_links:
    - from: "e2e/helpers/auth.ts"
      to: "e2e/auth/github.spec.ts"
      via: "shared auth helper import"
      pattern: "import.*helpers/auth"
    - from: "playwright.config.ts"
      to: "e2e/"
      via: "testDir config"
      pattern: "testDir.*e2e"
---

<objective>
Create multi-flow Playwright E2E test files (6 files per D-02) covering auth, upload, dashboard CRUD, billing checkout, and docs navigation. Tests use storage state for auth persistence and are structured for local dev environment execution.

Purpose: Per D-02, E2E tests validate the full user journey across the application. These tests serve as smoke tests that can run in CI when services are available, or locally during development.
Output: 6 E2E spec files + 2 helper files, Playwright config updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@playwright.config.ts
@e2e/auth/github.spec.ts
@e2e/auth/magic-link.spec.ts
@e2e/auth/google.spec.ts
@e2e/auth/route-protection.spec.ts
@e2e/auth/session-persistence.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auth helpers, config update, and auth E2E specs</name>
  <files>
    playwright.config.ts
    e2e/helpers/auth.ts
    e2e/helpers/storage-state.ts
    e2e/auth/github.spec.ts
    e2e/auth/magic-link.spec.ts
  </files>
  <action>
    Read the existing E2E stubs first:
    - e2e/auth/github.spec.ts
    - e2e/auth/magic-link.spec.ts
    - e2e/auth/google.spec.ts
    - e2e/auth/route-protection.spec.ts
    - e2e/auth/session-persistence.spec.ts
    - playwright.config.ts

    1. Update playwright.config.ts:
       - Add a setup project that runs before all tests to establish auth state
       - Add webServer config to start the dashboard app (`pnpm --filter @uploadkit/dashboard dev`) on port 3001
       - Add a `storageState` path in the use config: `e2e/.auth/user.json`
       - Keep existing chromium project, add dependency on setup project
       - Add timeout: 30_000 for slow local dev servers

    2. Create e2e/helpers/auth.ts:
       - Export a `login` helper function that navigates to /login
       - For CI/local: use email magic link flow as the testable auth path (OAuth requires real providers)
       - The helper should fill email input, submit, and handle the magic link verification
       - Export a STORAGE_STATE_PATH constant pointing to `e2e/.auth/user.json`
       - Add `e2e/.auth/` to .gitignore if not already there

    3. Create e2e/helpers/storage-state.ts:
       - This is the Playwright setup file (globalSetup or setup project)
       - Uses the login helper to authenticate once
       - Saves storage state to STORAGE_STATE_PATH
       - All subsequent tests reuse this state (no re-login per test)

    4. Update e2e/auth/github.spec.ts:
       - Replace the test.skip stub with a proper test structure
       - Mark as `test.skip` with descriptive reason: "Requires live GitHub OAuth — run manually with GITHUB_TEST_* env vars"
       - Add a conditional: if process.env.GITHUB_TEST_EMAIL is set, run the test (navigate to /login, click GitHub, complete OAuth flow)
       - Verify: user lands on /dashboard after successful auth

    5. Update e2e/auth/magic-link.spec.ts:
       - Implement a real E2E test (this is the most CI-friendly auth test)
       - Navigate to /login, enter test email, submit
       - Since magic link requires email delivery, mark test as conditional on E2E_MAGIC_LINK_URL env var
       - When skipped, add descriptive reason: "Set E2E_MAGIC_LINK_URL to test magic link flow"
       - When available: navigate to the magic link URL, verify redirect to dashboard

    OAuth strategy per Claude's Discretion: OAuth tests are test.skip by default with clear env var gates. Magic link is the primary testable auth flow. Storage state setup assumes a pre-authenticated session exists (seeded or from a previous auth run).
  </action>
  <verify>
    <automated>cd /Users/drumstock/Developer/GitHub/uploadkit && npx playwright test --list 2>&1 | head -20</automated>
  </verify>
  <done>Playwright config updated with setup project and webServer. Auth helpers exist at e2e/helpers/. Auth spec files have real test structures (conditional on env vars). `npx playwright test --list` shows all test files.</done>
</task>

<task type="auto">
  <name>Task 2: Upload, dashboard CRUD, billing, and docs E2E specs</name>
  <files>
    e2e/upload/upload-flow.spec.ts
    e2e/dashboard/crud.spec.ts
    e2e/billing/checkout.spec.ts
    e2e/docs/navigation.spec.ts
  </files>
  <action>
    Create the 4 remaining E2E spec files. Each uses the authenticated storage state from Task 1's setup. Read the dashboard app's page structure first:
    - Check apps/dashboard/src/app/ for route paths used in tests

    1. Create e2e/upload/upload-flow.spec.ts (~4 tests):
       - test: "navigates to a project and sees upload area"
         - Go to /dashboard, click into first project, verify file browser loads
       - test: "uploads a single file via the dashboard UI"
         - Use page.setInputFiles() on the file input to upload a test file
         - Wait for upload progress indicator
         - Verify file appears in the file browser table
       - test: "uploaded file shows correct name and size"
         - After upload, verify the row displays the file name and approximate size
       - test: "deletes an uploaded file"
         - Select the uploaded file, click delete action
         - Confirm deletion dialog
         - Verify file is removed from the table

    2. Create e2e/dashboard/crud.spec.ts (~5 tests):
       - test: "creates a new project"
         - Navigate to /dashboard, click "New Project" button
         - Fill project name, submit
         - Verify project appears in sidebar or project list
       - test: "generates an API key for the project"
         - Navigate into the project, go to API Keys tab/page
         - Click create key, verify masked key (uk_live_xxx...xxx) appears
         - Verify copy button is present
       - test: "views upload logs page"
         - Navigate to logs page
         - Verify table headers are visible (timestamp, status, file, route)
       - test: "views usage page with charts"
         - Navigate to usage page
         - Verify progress bars or chart containers render
       - test: "deletes the project"
         - Navigate to project settings
         - Click delete project, confirm dialog
         - Verify redirect back to dashboard overview

    3. Create e2e/billing/checkout.spec.ts (~2 tests):
       - test: "billing page shows current plan"
         - Navigate to /dashboard/billing
         - Verify "Free" plan card is visible (default for test user)
       - test: "upgrade button links to Stripe Checkout"
         - Click "Upgrade to Pro" button
         - Verify either: redirect URL contains checkout.stripe.com, or Stripe Checkout iframe loads
         - Use `test.skip` if STRIPE_SECRET_KEY is not set (no Stripe in test env)

    4. Create e2e/docs/navigation.spec.ts (~3 tests):
       - Configure baseURL override for docs app (localhost:3003 or wherever docs runs)
       - test: "docs homepage loads with search"
         - Navigate to docs URL
         - Verify heading and search input are visible
       - test: "quickstart page is accessible from sidebar"
         - Click "Quickstart" in sidebar navigation
         - Verify page content loads with installation instructions
       - test: "SDK reference pages load"
         - Navigate to /docs/sdk/core (or actual path)
         - Verify createUploadKit section is visible

    Each spec file should:
    - Import { test, expect } from '@playwright/test'
    - Use test.describe() for grouping
    - Use page.goto() with relative paths (baseURL from config)
    - Use expect(locator).toBeVisible() for assertions
    - Add reasonable timeouts for page loads
  </action>
  <verify>
    <automated>cd /Users/drumstock/Developer/GitHub/uploadkit && npx playwright test --list 2>&1 | tail -20</automated>
  </verify>
  <done>4 E2E spec files created: upload (4 tests), dashboard CRUD (5 tests), billing (2 tests), docs navigation (3 tests). Combined with auth specs = 6 total E2E files. `npx playwright test --list` shows all tests. Tests are structured to run against local dev servers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| E2E storage state file | Contains session cookies — must be gitignored |
| Stripe test mode | E2E billing tests must use test mode keys only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | I (Information Disclosure) | e2e/.auth/user.json | mitigate | Add e2e/.auth/ to .gitignore; storage state contains session cookies that must not be committed |
| T-10-05 | I (Information Disclosure) | e2e/billing/checkout.spec.ts | mitigate | Test only checks redirect URL pattern, never logs or asserts on Stripe secret keys; conditional skip when no Stripe env |
</threat_model>

<verification>
Run from repo root:
```bash
npx playwright test --list
```
All 6 spec files are discovered. Individual spec execution requires running local dev servers.
</verification>

<success_criteria>
- 6 E2E spec files: auth/github, auth/magic-link, upload/upload-flow, dashboard/crud, billing/checkout, docs/navigation
- Auth helper and storage state setup exist in e2e/helpers/
- Playwright config has webServer and setup project configured
- e2e/.auth/ is in .gitignore
- `npx playwright test --list` discovers all spec files
</success_criteria>

<output>
After completion, create `.planning/phases/10-testing-publishing-launch/10-03-SUMMARY.md`
</output>
