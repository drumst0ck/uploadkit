---
phase: 06-dashboard
plan: 05
type: execute
wave: 3
depends_on: [06-02, 06-03, 06-04]
files_modified:
  - apps/dashboard/src/components/command-palette.tsx
  - apps/dashboard/src/app/dashboard/layout.tsx
  - apps/dashboard/src/app/dashboard/projects/[slug]/settings/page.tsx
  - apps/dashboard/src/app/api/internal/projects/[slug]/route.ts
autonomous: false
requirements:
  - DASH-11

must_haves:
  truths:
    - "Pressing cmd+k (or ctrl+k) opens the command palette"
    - "Command palette has navigation items that route to all dashboard pages"
    - "Command palette supports file search by name"
    - "Command palette has action items: create project, toggle dark mode, sign out"
    - "Arrow keys navigate results, Enter selects, Escape closes"
    - "Project settings page allows editing project name and deleting project"
  artifacts:
    - path: "apps/dashboard/src/components/command-palette.tsx"
      provides: "cmdk-powered global command palette"
      contains: "Command.Dialog"
    - path: "apps/dashboard/src/app/dashboard/projects/[slug]/settings/page.tsx"
      provides: "Project settings with edit/delete"
      contains: "AlertDialog"
  key_links:
    - from: "apps/dashboard/src/app/dashboard/layout.tsx"
      to: "apps/dashboard/src/components/command-palette.tsx"
      via: "render + cmd+k listener"
      pattern: "CommandPalette"
    - from: "apps/dashboard/src/components/command-palette.tsx"
      to: "cmdk"
      via: "Command.Dialog"
      pattern: "Command\\.Dialog"
---

<objective>
Build the command palette (cmd+k) with navigation, file search, and actions. Add project settings page. Final verification checkpoint for the complete dashboard.

Purpose: The command palette (D-10, D-11) is a power-user feature that ties the entire dashboard together. Project settings completes the project management feature set. The checkpoint confirms all 14 requirements are met.
Output: Working command palette. Project settings. Dashboard visually verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-dashboard/06-CONTEXT.md
@.planning/phases/06-dashboard/06-RESEARCH.md
@.planning/phases/06-dashboard/06-01-SUMMARY.md
@.planning/phases/06-dashboard/06-02-SUMMARY.md
@.planning/phases/06-dashboard/06-03-SUMMARY.md
@.planning/phases/06-dashboard/06-04-SUMMARY.md

<interfaces>
From cmdk (1.1.1):
```typescript
import { Command } from 'cmdk';
// Command.Dialog — portal-based dialog with search input + list
// Command.Input — search input
// Command.List — results container
// Command.Empty — shown when no results
// Command.Group — groups results with heading
// Command.Item — individual result, onSelect callback
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Command palette with navigation, file search, and actions + project settings page</name>
  <files>
    apps/dashboard/src/components/command-palette.tsx,
    apps/dashboard/src/app/dashboard/layout.tsx,
    apps/dashboard/src/app/dashboard/projects/[slug]/settings/page.tsx,
    apps/dashboard/src/app/api/internal/projects/[slug]/route.ts
  </files>
  <read_first>
    apps/dashboard/src/app/dashboard/layout.tsx,
    apps/dashboard/src/components/layout/sidebar-nav.tsx,
    apps/dashboard/src/hooks/use-projects.ts,
    apps/dashboard/src/providers/theme-provider.tsx,
    packages/db/src/models/project.ts,
    apps/dashboard/auth.ts
  </read_first>
  <action>
1. Create `apps/dashboard/src/components/command-palette.tsx` ('use client') per D-10 and D-11:
   - Uses cmdk: Command.Dialog with open state and onOpenChange.
   - Props: open (boolean), onOpenChange ((open: boolean) => void).
   - Command.Input placeholder="Type a command or search...".
   - Command.List with Command.Empty "No results found."
   - Navigation group (Command.Group heading="Navigation"):
     - Dashboard Overview -> /dashboard
     - Projects -> /dashboard/projects
     - Usage -> /dashboard/usage
     - Billing -> /dashboard/billing
     - Settings -> /dashboard/settings
     - For each user project (fetched via useProjects hook): "{project.name} Files" -> /dashboard/projects/{slug}/files, "{project.name} Keys" -> /dashboard/projects/{slug}/keys, "{project.name} Logs" -> /dashboard/projects/{slug}/logs
   - Each nav item has a lucide icon matching sidebar icons.
   - Actions group (Command.Group heading="Actions"):
     - "Create Project" -> opens create project dialog (set state)
     - "Toggle Dark Mode" -> calls setTheme from useTheme()
     - "Sign Out" -> calls signOut server action
   - File search: when input has 3+ characters, add a "Files" Command.Group that searches files via /api/internal/search?q=query (create a simple search endpoint, or filter in the existing files data). For simplicity, can use SWR with the search query as key, debounced.
   - On any Command.Item select: router.push(href), then onOpenChange(false) to close.
   - Style the dialog: dark bg-[#0a0a0b], border border-white/[0.06], Command.Input text-white bg-transparent, items text-zinc-400 hover:bg-white/[0.06] hover:text-white, selected item bg-white/[0.06] text-white.
   - Handle Pitfall 5: wire open and onOpenChange to same state. Test focus restoration.

2. Update `apps/dashboard/src/app/dashboard/layout.tsx`:
   - Add CommandPalette component render with state: const [cmdkOpen, setCmdkOpen] = useState(false).
   - Register global cmd+k listener via useEffect:
     ```typescript
     useEffect(() => {
       const handler = (e: KeyboardEvent) => {
         if ((e.metaKey || e.ctrlKey) && e.key === 'k') {
           e.preventDefault();
           setCmdkOpen(prev => !prev);
         }
       };
       document.addEventListener('keydown', handler);
       return () => document.removeEventListener('keydown', handler);
     }, []);
     ```
   - Since the layout is a server component with auth guard, the cmd+k state and CommandPalette must be in a client wrapper component. Create a small client component (e.g., DashboardShell) that wraps the Sidebar + Header + CommandPalette + main children, and render that from the server layout which provides session data.

3. Create `apps/dashboard/src/app/api/internal/projects/[slug]/route.ts`:
   - GET: auth() + connectDB(). Return project details by slug + userId.
   - PUT: auth() + connectDB(). Update project name. Re-derive slug if name changes. Validate name 1-50 chars.
   - DELETE: auth() + connectDB(). Delete project and all associated files, API keys, file routers. Redirect guidance: return 200 with { redirect: '/dashboard/projects' }.
   - `export const dynamic = 'force-dynamic'`.

4. Create `apps/dashboard/src/app/dashboard/projects/[slug]/settings/page.tsx`:
   - `export const dynamic = 'force-dynamic'`.
   - Server component: auth() + connectDB(). Fetch project by slug + userId.
   - "Project Settings" heading.
   - General section: project name Input (pre-filled), slug display (read-only with copy button). "Save" button calls PUT /api/internal/projects/[slug].
   - Danger zone: "Delete Project" with AlertDialog. Warning: "This will permanently delete this project and all its files, API keys, and routes. This action cannot be undone." User must type project name to confirm. On confirm: DELETE /api/internal/projects/[slug], redirect to /dashboard/projects.
  </action>
  <verify>
    <automated>cd /Users/drumstock/Developer/GitHub/uploadkit && pnpm turbo build --filter=@uploadkit/dashboard 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - apps/dashboard/src/components/command-palette.tsx imports Command from 'cmdk' and renders Command.Dialog
    - Command palette has Navigation group with items for all dashboard pages
    - Command palette has Actions group with "Create Project", "Toggle Dark Mode", "Sign Out"
    - apps/dashboard/src/app/dashboard/layout.tsx registers cmd+k / ctrl+k keydown listener
    - apps/dashboard/src/app/dashboard/projects/[slug]/settings/page.tsx renders name edit form and danger zone with delete
    - apps/dashboard/src/app/api/internal/projects/[slug]/route.ts exports GET, PUT, DELETE with auth() guard
    - `pnpm turbo build --filter=@uploadkit/dashboard` passes
  </acceptance_criteria>
  <done>Command palette opens on cmd+k with navigation, file search, and actions. Project settings page supports name edit and project deletion with typed confirmation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete dashboard</name>
  <files>apps/dashboard/src/app/dashboard/layout.tsx</files>
  <action>
    Verify the complete dashboard visually and functionally. All 14 DASH requirements must be met:
    1. Start the dashboard: `cd apps/dashboard and pnpm dev`
    2. Visit http://localhost:3001/dashboard - verify overview shows 4 metric cards and upload chart
    3. Click sidebar toggle - verify it collapses to icon-only rail and expands back
    4. Navigate to Projects - verify project list loads, try creating a new project
    5. Navigate to a project's Files page - verify DataTable renders with columns, try search/filter
    6. Navigate to API Keys - verify masked keys display, try creating a key (one-time display)
    7. Navigate to File Routes - verify routes table, try creating a route
    8. Navigate to Logs - verify table loads, look for "Live" indicator, wait 5+ seconds for refresh
    9. Navigate to Usage - verify 3 progress bars against tier limits, historical chart
    10. Navigate to Billing - verify plan card and placeholder upgrade button
    11. Navigate to Settings - verify profile form, danger zone
    12. Press cmd+k - verify palette opens, navigate with arrows, select with Enter
    13. Toggle dark/light mode via palette or toggle button
    14. Resize browser to mobile - verify sidebar becomes off-canvas drawer
    15. Verify breadcrumbs update correctly on each page
  </action>
  <verify>
    <automated>cd /Users/drumstock/Developer/GitHub/uploadkit && pnpm turbo build --filter=@uploadkit/dashboard 2>&1 | tail -5</automated>
  </verify>
  <done>All 14 DASH requirements verified. Dashboard is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> /api/internal/projects/[slug] | Project edit/delete scoped to user |
| keyboard -> command palette | Global shortcut triggers UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-17 | Elevation of Privilege | Project DELETE | mitigate | Verify project belongs to session.user.id before deletion. Cascade delete all associated resources. |
| T-06-18 | Denial of Service | Command palette file search | mitigate | Debounce search input (300ms). Limit API results to 10 items. No expensive full-text queries. |
| T-06-19 | Tampering | Project PUT (rename) | mitigate | Validate name length 1-50. Re-derive slug server-side. userId from session only. |
</threat_model>

<verification>
1. `pnpm turbo build --filter=@uploadkit/dashboard` passes
2. cmd+k opens palette, arrow keys navigate, Enter selects, Escape closes
3. Palette navigation routes to correct pages
4. Palette actions (toggle theme, sign out) work
5. Project settings: name edit saves, delete with confirmation works
6. All 14 DASH requirements visually verified
</verification>

<success_criteria>
- Command palette accessible via cmd+k/ctrl+k with navigation, search, and actions
- Project settings page with name edit and typed-confirmation delete
- All 14 DASH requirements implemented and verified
- Dashboard fully functional: sidebar, breadcrumbs, overview, projects, files, keys, routes, logs, usage, billing, settings
</success_criteria>

<output>
After completion, create `.planning/phases/06-dashboard/06-05-SUMMARY.md`
</output>
