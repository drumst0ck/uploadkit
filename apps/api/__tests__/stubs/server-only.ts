// Empty stub for the Next.js `server-only` module. In test runs we don't have
// the server/client boundary — this stub lets modules guarded by
// `import 'server-only'` load under vitest.
export {};
