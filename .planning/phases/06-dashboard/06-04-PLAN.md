---
phase: 06-dashboard
plan: 04
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - apps/dashboard/src/app/dashboard/projects/[slug]/logs/page.tsx
  - apps/dashboard/src/app/api/internal/projects/[slug]/logs/route.ts
  - apps/dashboard/src/app/dashboard/usage/page.tsx
  - apps/dashboard/src/app/api/internal/usage/route.ts
  - apps/dashboard/src/app/dashboard/billing/page.tsx
  - apps/dashboard/src/app/dashboard/settings/page.tsx
  - apps/dashboard/src/app/api/internal/settings/route.ts
  - apps/dashboard/src/components/upload-logs-table.tsx
  - apps/dashboard/src/components/charts/usage-bar-chart.tsx
  - apps/dashboard/src/components/usage-progress-bar.tsx
  - apps/dashboard/src/hooks/use-logs.ts
  - apps/dashboard/src/hooks/use-usage.ts
autonomous: true
requirements:
  - DASH-07
  - DASH-08
  - DASH-09
  - DASH-10

must_haves:
  truths:
    - "Upload logs page auto-refreshes every 5 seconds showing new events without page reload"
    - "Upload logs can be filtered by date range, status, and route"
    - "Usage page shows progress bars with percentage of tier limit for storage, bandwidth, and uploads"
    - "Usage page shows historical usage bar chart via Recharts"
    - "Billing page shows current plan card with upgrade button placeholder"
    - "Settings page allows profile update and has delete account with confirmation"
  artifacts:
    - path: "apps/dashboard/src/components/upload-logs-table.tsx"
      provides: "Polling log viewer with 5s refresh"
      contains: "refreshInterval"
    - path: "apps/dashboard/src/components/usage-progress-bar.tsx"
      provides: "Progress bar with tier limit percentage"
      contains: "TIER_LIMITS"
    - path: "apps/dashboard/src/components/charts/usage-bar-chart.tsx"
      provides: "Usage breakdown bar chart"
      contains: "BarChart"
    - path: "apps/dashboard/src/app/dashboard/billing/page.tsx"
      provides: "Billing shell page"
      contains: "Upgrade"
  key_links:
    - from: "apps/dashboard/src/hooks/use-logs.ts"
      to: "/api/internal/projects/[slug]/logs"
      via: "SWR with refreshInterval:5000"
      pattern: "refreshInterval.*5000"
    - from: "apps/dashboard/src/components/usage-progress-bar.tsx"
      to: "@uploadkit/shared"
      via: "import TIER_LIMITS"
      pattern: "TIER_LIMITS"
---

<objective>
Build the upload logs page with 5s polling, usage page with progress bars and charts, billing page (thin shell for Phase 7), and settings page with profile management and account deletion.

Purpose: These four pages complete the dashboard feature set. Logs provide real-time upload monitoring. Usage shows tier consumption. Billing is a shell for Phase 7 Stripe integration. Settings handles account management.
Output: Four fully functional pages with live data.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-dashboard/06-CONTEXT.md
@.planning/phases/06-dashboard/06-RESEARCH.md
@.planning/phases/06-dashboard/06-01-SUMMARY.md

@packages/db/src/models/usage-record.ts
@packages/db/src/models/file.ts
@packages/db/src/models/user.ts
@packages/shared/src/constants.ts
@packages/shared/src/types.ts

<interfaces>
From packages/db:
```typescript
export interface IUsageRecord extends Document {
  userId: string;
  period: string;          // "YYYY-MM"
  storageUsed: number;
  bandwidthUsed: number;
  uploadsCount: number;
  createdAt: Date;
  updatedAt: Date;
}

export interface IUser extends Document {
  name?: string;
  email: string;
  emailVerified?: Date;
  image?: string;
  tier: 'FREE' | 'PRO' | 'TEAM' | 'ENTERPRISE';
}

// File model has: status, createdAt, projectId, name, size, type, url, metadata
```

From packages/shared:
```typescript
export const TIER_LIMITS = {
  FREE: { maxStorageBytes: 5368709120, maxBandwidthBytes: 2147483648, maxUploadsPerMonth: 1000, maxFileSizeBytes: 52428800 },
  // PRO, TEAM, ENTERPRISE...
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upload logs page with 5s polling and usage page with progress bars + charts</name>
  <files>
    apps/dashboard/src/app/dashboard/projects/[slug]/logs/page.tsx,
    apps/dashboard/src/app/api/internal/projects/[slug]/logs/route.ts,
    apps/dashboard/src/components/upload-logs-table.tsx,
    apps/dashboard/src/hooks/use-logs.ts,
    apps/dashboard/src/app/dashboard/usage/page.tsx,
    apps/dashboard/src/app/api/internal/usage/route.ts,
    apps/dashboard/src/components/charts/usage-bar-chart.tsx,
    apps/dashboard/src/components/usage-progress-bar.tsx,
    apps/dashboard/src/hooks/use-usage.ts
  </files>
  <read_first>
    packages/db/src/models/file.ts,
    packages/db/src/models/usage-record.ts,
    packages/db/src/models/user.ts,
    packages/shared/src/constants.ts,
    apps/dashboard/src/lib/format.ts,
    apps/dashboard/src/lib/fetcher.ts
  </read_first>
  <action>
1. Create `apps/dashboard/src/app/api/internal/projects/[slug]/logs/route.ts`:
   - GET: auth() + connectDB(). Find project by slug + userId.
   - Query params: since (ISO timestamp), status (UPLOADED|FAILED|UPLOADING), route (file router slug).
   - Query File model: `{ projectId, createdAt: { $gte: since }, ...(status && { status }), ...(routeSlug && { routeSlug }) }`.
   - Sort by _id desc, limit 100.
   - Return array of log entries: { _id, name, size, type, status, createdAt, routeSlug }.
   - `export const dynamic = 'force-dynamic'`.

2. Create `apps/dashboard/src/hooks/use-logs.ts`:
   - Uses SWR with refreshInterval: 5000 per DASH-07 and D-09.
   - Key: `/api/internal/projects/${slug}/logs?since=${since}&status=${status}&route=${route}`.
   - since defaults to 24 hours ago.
   - Returns { logs, isLoading, error }.

3. Create `apps/dashboard/src/components/upload-logs-table.tsx` ('use client'):
   - Uses useLogs hook.
   - Renders Table with columns: Status (Badge: green for UPLOADED, yellow for UPLOADING, red for FAILED), File Name, Size (formatBytes), Type, Route, Time (relative: "2m ago", "just now").
   - Filter controls at top: date range picker (or simple "Last hour" / "Last 24h" / "Last 7d" Select), status Select, route Select.
   - Shows "Live" indicator (pulsing green dot) to indicate auto-refresh is active.
   - New entries should appear at the top without full table re-render.

4. Create `apps/dashboard/src/app/dashboard/projects/[slug]/logs/page.tsx`:
   - `export const dynamic = 'force-dynamic'`.
   - Heading "Upload Logs" + description "Real-time upload activity for this project."
   - Renders UploadLogsTable with slug from params.

5. Create `apps/dashboard/src/app/api/internal/usage/route.ts`:
   - GET: auth() + connectDB().
   - Returns current period usage, last 6 months history, user tier.
   - Current: UsageRecord.findOne({ userId, period: currentPeriod }).lean().
   - History: UsageRecord.find({ userId }).sort({ period: -1 }).limit(6).lean().
   - User tier: read from User model or session (check what's available).
   - Return { current: { storageUsed, bandwidthUsed, uploadsCount }, history: [...], tier }.
   - `export const dynamic = 'force-dynamic'`.

6. Create `apps/dashboard/src/hooks/use-usage.ts`:
   - useSWR('/api/internal/usage', fetcher).
   - Returns { usage, isLoading, error }.

7. Create `apps/dashboard/src/components/usage-progress-bar.tsx`:
   - Props: label (string), used (number), limit (number), formatFn (function).
   - Calculates percentage: limit === Infinity ? 0 : Math.min(100, (used/limit)*100).
   - Renders: label + used/limit text, progress bar div (h-1.5 rounded-full bg-white/[0.06] overflow-hidden), inner div (bg-indigo-500, width percent).
   - Color changes: >80% bg-amber-500, >95% bg-red-500.

8. Create `apps/dashboard/src/components/charts/usage-bar-chart.tsx` ('use client') per D-07:
   - Props: data (array of { period: string, storage: number, bandwidth: number, uploads: number }).
   - Uses Recharts BarChart inside ResponsiveContainer (height 300).
   - 3 bars: Storage (indigo-500), Bandwidth (violet-500), Uploads (emerald-500).
   - XAxis shows month labels. YAxis with formatBytes for storage/bandwidth, plain number for uploads.
   - Tooltip dark-styled like the area chart.
   - Legend at bottom.

9. Create `apps/dashboard/src/app/dashboard/usage/page.tsx`:
   - `export const dynamic = 'force-dynamic'`.
   - Server component: auth() + connectDB(). Fetch current usage + user tier.
   - Render heading "Usage" + current billing period.
   - 3 UsageProgressBars: Storage (formatBytes), Bandwidth (formatBytes), Uploads (formatNumber) — each against TIER_LIMITS[tier] values per DASH-08.
   - Below: UsageBarChart client component for historical data.
   - Per-project breakdown: aggregate usage by project (query files grouped by projectId).
  </action>
  <verify>
    <automated>cd /Users/drumstock/Developer/GitHub/uploadkit && pnpm turbo build --filter=@uploadkit/dashboard 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - apps/dashboard/src/hooks/use-logs.ts uses SWR with refreshInterval:5000
    - apps/dashboard/src/components/upload-logs-table.tsx renders Table with Status Badge, File Name, Size, Type, Route, Time columns
    - apps/dashboard/src/components/upload-logs-table.tsx shows pulsing green "Live" indicator
    - apps/dashboard/src/components/usage-progress-bar.tsx changes color at >80% (amber) and >95% (red)
    - apps/dashboard/src/components/charts/usage-bar-chart.tsx wraps Recharts BarChart in ResponsiveContainer
    - apps/dashboard/src/app/dashboard/usage/page.tsx renders 3 progress bars using TIER_LIMITS
    - `pnpm turbo build --filter=@uploadkit/dashboard` passes
  </acceptance_criteria>
  <done>Upload logs page auto-refreshes every 5s with status/date/route filters and live indicator. Usage page shows 3 progress bars against tier limits plus historical bar chart.</done>
</task>

<task type="auto">
  <name>Task 2: Billing page (thin shell) and Settings page (profile + delete account)</name>
  <files>
    apps/dashboard/src/app/dashboard/billing/page.tsx,
    apps/dashboard/src/app/dashboard/settings/page.tsx,
    apps/dashboard/src/app/api/internal/settings/route.ts
  </files>
  <read_first>
    packages/db/src/models/user.ts,
    packages/db/src/models/subscription.ts,
    packages/shared/src/constants.ts,
    apps/dashboard/auth.ts
  </read_first>
  <action>
1. Create `apps/dashboard/src/app/dashboard/billing/page.tsx` (server component) per DASH-09:
   - `export const dynamic = 'force-dynamic'`.
   - auth() + connectDB(). Fetch user's subscription if exists: Subscription.findOne({ userId }).lean().
   - Render "Billing" heading.
   - Current plan card: rounded-xl border bg-[#141416]. Shows tier name (from user.tier or "Free"), Badge for plan, description of limits.
   - "Upgrade to Pro" button (disabled, styled as primary indigo-500 button). Tooltip or note: "Stripe integration coming in Phase 7."
   - "Manage Billing" button (disabled). Links to Stripe Billing Portal in Phase 7.
   - Invoice history section: empty state "No invoices yet." Placeholder table.
   - This is intentionally a shell — Stripe Checkout, webhook processing, and real invoice data come in Phase 7.

2. Create `apps/dashboard/src/app/api/internal/settings/route.ts`:
   - GET: auth() + connectDB(). Return User.findById(session.user.id).select('name email image').lean().
   - PUT: auth() + connectDB(). Parse body { name: string }. Validate name 1-100 chars. Update user. Return updated user.
   - DELETE: auth() + connectDB(). Soft delete or mark user for deletion. This is account deletion — sign out the user after. For now, just delete the user document and all their projects/files (or mark as deleted). Call signOut() server-side after deletion.
   - `export const dynamic = 'force-dynamic'`.

3. Create `apps/dashboard/src/app/dashboard/settings/page.tsx` per DASH-10:
   - `export const dynamic = 'force-dynamic'`.
   - auth() + connectDB(). Fetch user data.
   - "Settings" heading.
   - Profile section: name Input (pre-filled), email (read-only, displayed as text). "Save" button calls PUT /api/internal/settings via fetch.
   - Danger zone section at bottom with red border: "Delete Account" heading, warning text "This will permanently delete your account, all projects, files, and API keys. This action cannot be undone.", "Delete Account" Button variant="destructive" triggers AlertDialog with typed confirmation (user must type their email to confirm).
   - On delete confirm: call DELETE /api/internal/settings, then redirect to /login.
   - Notifications section (simple): toggle for email notifications (stored as user preference). Can be a simple checkbox for now — "Email me about usage alerts" and "Email me about product updates".
  </action>
  <verify>
    <automated>cd /Users/drumstock/Developer/GitHub/uploadkit && pnpm turbo build --filter=@uploadkit/dashboard 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - apps/dashboard/src/app/dashboard/billing/page.tsx renders current plan card with tier name and disabled upgrade button
    - apps/dashboard/src/app/dashboard/billing/page.tsx contains text "Upgrade" or "Manage Billing"
    - apps/dashboard/src/app/dashboard/settings/page.tsx renders profile form with name Input and save button
    - apps/dashboard/src/app/dashboard/settings/page.tsx has danger zone with AlertDialog for account deletion
    - apps/dashboard/src/app/api/internal/settings/route.ts exports GET, PUT, DELETE with auth() guard
    - Settings DELETE requires typed email confirmation before executing
    - `pnpm turbo build --filter=@uploadkit/dashboard` passes
  </acceptance_criteria>
  <done>Billing page shows current plan card with placeholder upgrade/manage buttons and empty invoice history. Settings page allows profile name update and account deletion with typed email confirmation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> /api/internal/projects/[slug]/logs | Log data scoped to project+user |
| client -> /api/internal/usage | Usage data scoped to user session |
| client -> /api/internal/settings DELETE | Destructive account deletion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-13 | Denial of Service | Logs polling | accept | 5s polling is lightweight. Internal routes only accessible from dashboard (same-origin). Rate limiting via Next.js if needed. |
| T-06-14 | Elevation of Privilege | Settings PUT | mitigate | Only allows updating name field. userId from session, never from request body. Email cannot be changed. |
| T-06-15 | Repudiation | Account deletion | mitigate | Require typed email confirmation in the AlertDialog. Log deletion event before executing. |
| T-06-16 | Information Disclosure | Usage data | mitigate | All queries filter by userId from session. No cross-user data access. |
</threat_model>

<verification>
1. `pnpm turbo build --filter=@uploadkit/dashboard` passes
2. Logs page shows entries and auto-refreshes (new entries appear without reload)
3. Usage page shows 3 progress bars with correct tier limits
4. Billing page renders plan card
5. Settings page profile update works; delete account has confirmation
</verification>

<success_criteria>
- Upload logs page polls every 5s, shows status badges, supports date/status/route filters
- Usage page shows storage/bandwidth/uploads progress bars against TIER_LIMITS
- Usage page shows 6-month historical bar chart
- Billing page shows current plan and placeholder upgrade buttons
- Settings page supports profile name update and account deletion with typed confirmation
</success_criteria>

<output>
After completion, create `.planning/phases/06-dashboard/06-04-SUMMARY.md`
</output>
