---
phase: 12.5-uploadkit-cli-for-existing-projects-inserted
plan: 10
type: execute
wave: 5
depends_on: [12.5-09]
files_modified:
  - .changeset/uploadkit-cli-initial.md
  - packages/uploadkit-cli/package.json
  - packages/uploadkit-cli/README.md
  - apps/docs/content/docs/guides/cli-existing-projects.mdx
  - apps/docs/content/docs/guides/meta.json
autonomous: false
requirements: [GROW-15, GROW-16, GROW-17]
must_haves:
  truths:
    - "Changeset created for `uploadkit@0.1.0` as an initial release"
    - "Package README documents install, init, add, restore with framework tabs"
    - "Docs MDX page at /docs/guides/cli-existing-projects covers Next.js, SvelteKit, Remix, Vite flows"
    - "Release workflow smoke-gates before publish"
    - "npm name `uploadkit` confirmed available or fallback plan documented"
  artifacts:
    - path: ".changeset/uploadkit-cli-initial.md"
      provides: "Changeset releasing uploadkit@0.1.0 minor"
    - path: "apps/docs/content/docs/guides/cli-existing-projects.mdx"
      provides: "Public docs guide for the CLI"
    - path: "packages/uploadkit-cli/README.md"
      provides: "npm-page README"
  key_links:
    - from: ".github/workflows/release.yml"
      to: ".github/workflows/uploadkit-cli-smoke.yml"
      via: "needs: smoke"
      pattern: "needs: .*smoke"
---

<objective>
Prepare the `uploadkit` package for its first npm release. Create the
Changeset, finalize the README, write the docs guide, and wire the
smoke workflow as a blocking required-check for the release workflow.
Includes a human checkpoint to confirm npm name availability and to
approve the publish.

Purpose: Ship the CLI. This is the last plan in the phase.
Output: A publish-ready package + public-facing docs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12.5-uploadkit-cli-for-existing-projects-inserted/12.5-CONTEXT.md
@.planning/phases/12.5-uploadkit-cli-for-existing-projects-inserted/12.5-08-SUMMARY.md
@.planning/phases/12.5-uploadkit-cli-for-existing-projects-inserted/12.5-09-SUMMARY.md
@packages/create-uploadkit-app/README.md
@.github/workflows/release.yml

<interfaces>
Changeset file format:
```
---
"uploadkit": minor
---

Initial release of the `uploadkit` CLI. Run `npx uploadkit init` inside
an existing Next.js, SvelteKit, Remix, or Vite+React project to wire up
UploadKit — deps installed, route handler created, provider mounted,
env vars scaffolded. `npx uploadkit add <component>` inserts individual
components shadcn-style. Backed up to `.uploadkit-backup/`; roll back
with `uploadkit restore`.
```

Docs frontmatter:
```yaml
---
title: CLI for Existing Projects
description: Add UploadKit to an existing app in under 60 seconds.
---
```

Release workflow integration: add `needs: uploadkit-cli-smoke` to the
publish job OR add a required-check rule in branch protection (document
in SUMMARY which path was taken).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Confirm npm name availability</name>
  <what-built>Plan to publish the CLI under the npm name `uploadkit` per D-01.</what-built>
  <how-to-verify>
    Run: `npm view uploadkit`
    - If `npm view` returns "404 Not Found" or no publisher, the name is
      free — proceed with `uploadkit`.
    - If the name is taken by another project, fall back to
      `@uploadkitdev/cli` and update D-01 rationale in a CONTEXT.md
      patch (document the fallback).
    - If taken but appears unused/abandoned, open a dispute or proceed
      with the scoped name.
    Also confirm npm 2FA credentials exist for the maintainer account
    that `release.yml` uses.
  </how-to-verify>
  <resume-signal>Type "available" (proceed with `uploadkit`), "scoped" (use `@uploadkitdev/cli` + update package.json), or describe other outcome.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: README + Changeset + release workflow gate</name>
  <files>packages/uploadkit-cli/README.md, .changeset/uploadkit-cli-initial.md, packages/uploadkit-cli/package.json</files>
  <behavior>
    README.md (npm-page quality):
    - Hero line: "Add UploadKit to your existing app in under 60 seconds."
    - Quickstart for each framework (tabs-like sections)
    - Command reference: init / add / restore with flags
    - Example of add: `npx uploadkit add dropzone`
    - Link to /docs/guides/cli-existing-projects
    - Keywords in package.json: uploadkit, cli, upload, shadcn, next,
      sveltekit, remix, vite

    Changeset: minor release of `uploadkit@0.1.0` (or
    `@uploadkitdev/cli@0.1.0` if task 1 resolves to scoped).

    Release workflow: add `needs: smoke-cli` (or whatever the job name
    is) to the publish job so the matrix MUST pass before publish.
  </behavior>
  <action>
    If task 1 returned "scoped", update package.json `name` to
    `@uploadkitdev/cli` and add a `bin` alias `uploadkit` that points to
    the same dist/index.js. Do NOT publish two packages — one is enough.
    Ensure `publishConfig.access = "public"` regardless.
  </action>
  <verify>
    <automated>pnpm changeset status &amp;&amp; node -e "JSON.parse(require('fs').readFileSync('packages/uploadkit-cli/package.json','utf8'))"</automated>
  </verify>
  <done>Changeset valid; README renders; release workflow references smoke.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Public docs guide</name>
  <files>apps/docs/content/docs/guides/cli-existing-projects.mdx, apps/docs/content/docs/guides/meta.json</files>
  <behavior>
    MDX page includes:
    - Intro: when to use (existing projects) vs create-uploadkit-app
    - Quickstart: `npx uploadkit init` + per-framework expected output
      (use fumadocs Tabs component if available)
    - `add` workflow with component list (the 6 from D-09) + example
    - Backup + restore section
    - Troubleshooting: idempotency, framework not detected, how to undo
    - Link back to package README
    meta.json: add `cli-existing-projects` to the guides sidebar order
    (placed near existing `cli.mdx` from Phase 12 if it exists).
  </behavior>
  <action>
    Reuse any Tabs/Callout components already used elsewhere in
    apps/docs to stay consistent. If a Phase 12 `cli.mdx` exists,
    cross-link it and note the distinction (scaffolding vs insertion).
  </action>
  <verify>
    <automated>pnpm --filter docs build</automated>
  </verify>
  <done>Docs build passes; new page is reachable from the guides sidebar.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Publish approval</name>
  <what-built>Package ready to publish via Changesets. Smoke matrix green, docs live in staging, README reviewed.</what-built>
  <how-to-verify>
    1. Open the PR containing this phase's work and confirm all 4
       smoke-matrix legs are green.
    2. Preview the docs site deployment and verify the new
       /docs/guides/cli-existing-projects page renders correctly.
    3. Spot-check the npm README by running
       `pnpm --filter uploadkit-cli pack` and inspecting the tarball
       contents.
    4. When ready, merge the PR to master — `release.yml` will create
       the version PR; merging that publishes to npm.
  </how-to-verify>
  <resume-signal>Type "approved" to greenlight publish, or describe issues to fix.</resume-signal>
</task>

</tasks>

<verification>
- `pnpm changeset status` lists the new changeset
- `pnpm --filter docs build` succeeds
- `.github/workflows/release.yml` requires the smoke workflow to pass
- After merge, the Changesets release PR successfully publishes
  `uploadkit@0.1.0` to npm
- `npx uploadkit --version` on a fresh machine returns 0.1.0
</verification>

<success_criteria>
Phase 12.5 ships. `uploadkit` is on npm. Docs document both flows. CI
gate protects future releases. GROW-15, GROW-16, GROW-17 all delivered
and verifiable.
</success_criteria>

<output>
Create `12.5-10-SUMMARY.md` with: npm name outcome (available vs
scoped), publish timestamp, docs URL, and any follow-ups for a 0.2.0
release (Vue/Angular, `upgrade` command, Pages Router, Windows smoke).
</output>
